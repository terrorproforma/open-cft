# Devlog

## 2026-09-03 23:15 AEST - Orbit wall-loss geometry screening v2 (catalogue cells, scrambled Sobol, two-stage allocation): preregistered, executed, recorded

- Task summary:
  - Worktree `C:\Users\Angus\Desktop\projects\uni-project-orbit-geo2`, branch
    `exp/orbit-wall-loss-geometry-screening-v2` from `origin/feat/sota-foundation` (`9abbd537`,
    rebased onto `beb4772c` before the preregistration push; LF verified). CPU only (12 workers);
    GPU untouched; main tree untouched except the memory files. Commits: `a7a884bf` code/tests,
    `cef1ee59` preregistration (path-isolated), `26029b72` result (results only), `bb756418`
    experiment_runtime fix + recovery + `POSTHOC_FINALIZATION.md`, `eef7ac82` dashboard,
    `066234d9` no-ff merge of the concurrent upstream (L1a sweep v3, pic2d v2.0, paper v3.1
    admission; disjoint paths). Pushed; `feat/sota-foundation` fast-forwarded
    `1fb8561d..066234d9` (is-ancestor held; no force).
- Changes:
  - `modern/experiments/orbit_wall_loss_geometry_screening_v2/`: `sobol.py` (Joe-Kuo scrambled
    Sobol, LMS + digital shift, dependency-free), `cells.py` (hash-bound v3.1 catalogue cells,
    midpoint launch planes, 8 strata x scrambled-Sobol radius band / radius / gyrophase, frozen
    two-stage rule Wilson width > 0.10 -> 128 -> 512, 1/8 control selection, Wilson / Jeffreys
    floors, wall-area pooling), `designs.py` (v1 re-solve pipeline reused by import with the 2x
    refined diagnostic for EVERY design; P2 divergent-exit row via the v4 adapter), `experiment.py`
    (one orbit_mc case per (design, cell, 128-launch block) because orbit_mc v1.7 refuses
    zero-count categories at Wilson-inexact n; per-design worker stage1 -> rule -> stage-2 blocks
    -> control at 2N -> flags -> sealing; allocation replay, catalogue binding, pooled control
    gate, dataset + per-cell CSV, v1 comparison, consumer record), `consumer.py`, `run.py`,
    `protocol.json`, README/DEVLOG/LEARNING, `POSTHOC_FINALIZATION.md`; tests
    `modern/tests/experiments/orbit_wall_loss_geometry_screening_v2` (51).
  - `modern/src/cft_revival/experiment_runtime`: `MAX_PINNED_DESCRIPTORS = 4096` cap in
    `_finalize_manifest`, `seal_files(limit=...)`, new `recovery.finalize_unpublished_attempt`
    (fail-closed manifest publication for complete-but-unpublished attempts); tests
    `tests/experiment_runtime/test_recovery.py` (4).
  - `modern/visualization/generate_wall_loss_geometry_screening_v2_dashboard.py` + template ->
    `wall-loss-geometry-screening-v2.html` (776,625 B); `tests/visualization/
    test_wall_loss_geometry_screening_v2_dashboard.py` (4).
- Validation:
  - Shakedowns 1-3 caught uint64 seeds in canonical JSON, orbit_mc's launch-id grammar and the
    orbit_mc v1.7 Wilson ordering defect (`wilson_interval(0, n).lower > 0` for 734 of the first
    4000 n, incl. 384); shakedown 4 passed (4 designs incl. P2, 34 cases, both rule branches,
    replay, 113 s). Execution 21:03-22:29 AEST: `accepted_result` / `accepted_screening_dataset`,
    97 designs, 377 cells, 1105 cases, 104,832 orbits (48,256 / 44,928 / 11,648), 16,549
    validators passed, 0 failed, 0 timeouts, energy drift 0. The runtime's manifest publication
    then failed with EMFILE (16,957 files > 8192 CRT descriptors) after the terminal record;
    manifest published post hoc by the recovery module (`876dc7e1...`), `validate` ->
    `accepted_result` (16,968 artifacts); disclosed.
  - Headline: 117 cells topped up (32 anode-side, 83 exit-side, 2 P2), 260 saturated (all 181
    interior cells at 128/128 wall hits); per-cell P(wall) anode-side median 0.984, interior
    1.000, exit-side median 0.50 (90 divergent-exit designs; 1.0 for the 6 straight ones);
    reflections 10,407 (11.2 %), 65/96 exit-side cells, 66 designs; Jeffreys floor median 0.0055,
    max 0.0242; 294/377 cells (78 %) surrogate-v3 ready (topped-up cells at p ~ 0.5 sit at 0.022);
    N -> 2N paired control 2 discordant / 11,648, bias -0.9e-4 +- 0.9e-4, 97/97 flags true, all
    sealed; v1-vs-v2 pooled Spearman 0.15 (launch-weighted), mean Delta +0.038; P2 row 0.605 /
    1.0 / 1.0 / 0.17 with 350 reflections in its 28 um exit-side cell.
  - Tests after the merge: experiment_runtime 136 + v2 51 + dashboard 4 = 191 passed, 1 skipped.
    Headless Edge screenshots `%TEMP%\owlgs2_dashboard\wall-loss-geometry-screening-v2-{full,top}.png`.
- Follow-up notes / risks:
  - Surrogate v3 must model the partial cells; interior cells are constants (1.0) under this
    launch design. The 0.02 readiness floor is missed by the 77 topped-up cells at p ~ 0.5.
  - Paper admission of v2 must cite `POSTHOC_FINALIZATION.md`. Detached run worktree
    `uni-project-orbit-geo2-run` and the Git-common lock stay. Main tree needs `git pull --ff-only`.

## 2026-09-03 21:35 AEST - Paper admission: cusp topology search v3.1 (GATE-CUSP-TOPOLOGY-V3-1) and topology Discussion amendment

- Task summary:
  - Worktree `C:\Users\Angus\Desktop\projects\uni-project-paper-topo31`, branch
    `paper/topology-v31-claim` from `origin/feat/sota-foundation` (`9abbd537`), LF verified
    (only the three pre-existing `-text` files show `w/crlf`). Paper-owned paths and
    `modern/docs/workstreams/paper-*` only; `results/**`, frozen prereg files, `FYP/` and
    the GPU untouched. Commit `726c8a69` pushed on the feature branch; rebased twice over
    concurrent pushes (`beb4772c` TWT/PPM literature; `b04d5935..44d0c63c` L1a sweep v3
    campaign + dashboard; disjoint paths) to `13d8ac6a`; `feat/sota-foundation`
    fast-forwarded `44d0c63c..13d8ac6a` (is-ancestor held, no force).
- Changes:
  - Fifth `numerical-screening` gate `GATE-CUSP-TOPOLOGY-V3-1` at a new recorded outcome
    `accepted-topology-screening`; new manifest type
    `paper-separatrix-topology-screening-manifest` (25 roles, 125 metrics); manifest binds
    the v3.1 bundle at `cec47f12` (52 source files), dashboard at `9abbd537`, the v3
    recorded rejection (`8cbcdbe6`), frozen v3 protocol (`69159934`) and audit (`9fa6359a`)
    as 65 lineage files, the sealed v1/v2 datasets and sweep manifest as 3 references, the
    literature review at `66879e00` as definition source.
  - `paper/scripts/generate_cusp_topology_v3_1_evidence.py` (430 `\Ctv` macros, four
    ArtifactClaim tables: histogram per set, sweep by stage count, P2 vs two recorded
    ungated references, v3->v3.1 lineage); re-derives headline + every per-set estimand
    from the rows, cross-checks all 281 design records / field grids / catalogue entries /
    CSV rows, byte-verifies the rejected v3 bundle and reproduces its audit (26/206 dropped,
    22 in channel, exactly the 14 failing cases, 56/56 at 17.6 um).
  - Claims CLM-061..068; CLM-028 and CLM-044 amended (trigger A of LITERATURE_SYNTHESIS s7:
    frozen wall-null definition non-standard; N-1 wall cusps 83/96 and 19/96 exactly four
    under the literature definition; cells exist as geometric field structures, plasma
    physics undemonstrated). Manuscript: Section 13 (pp 35-40), Section 8 forward
    reference, Section 11 launch-cell scope note, Discussion rewrite (pp 42-44),
    Limitations, data availability, Conclusion; bib `Gildea2012`, `Koch2011`,
    `Lewerentz2023`. `check_paper.py`: `_check_cusp_topology_screening` with batched
    revision-bound file groups (`git ls-tree -r -z` + `cat-file --batch`).
  - Tests: 32 new (`test_cusp_topology_v3_1_{admission,evidence}.py`), two earlier tests
    updated (five outcomes; CLM-044 manifest set).
- Validation:
  - `check_paper.py` green after commit and after both rebases; `paper/tests` 197 OK
    (165 + 32); affected modules (86) rerun green on the rebased HEAD; two clean builds
    byte-identical, PDF 49 pages, 626,275 bytes, SHA-256 `34e11c8e...` (unchanged across
    the rebases); pages rendered to `%TEMP%\paper-topo31-pages\` and inspected.
- Follow-up notes / risks:
  - The brief's "every vector null sits on the axis" is NOT what the sealed v1 dataset
    records (20 of 200 in-channel X roots are off-axis bilinear roots at 0.16-0.54 r_w,
    excluded by the frozen rule, none at the wall); the paper states the split.
  - The catalogue has no admitted consumer; screening v2 launched from catalogue cells is
    named future work in Section 11. Canvas not updated from this subagent.
  - Feature branch `origin/paper/topology-v31-claim` stays at pre-rebase `726c8a69`.
    Main tree `uni-project` is behind; needs `git pull --ff-only`.

## 2026-09-03 20:35 AEST - literature review: TWT/PPM focusing physics inherited by the HEMP design

- Task summary:
  - Worktree `C:\Users\Angus\Desktop\projects\uni-project-lit-twt`, branch
    `docs/literature-twt-ppm` from `origin/feat/sota-foundation` (`fb5408bf`). New files
    `modern/docs/literature/twt-ppm-physics-for-hemp.md` (983 lines, ASCII, LF),
    `modern/docs/literature/scripts/ppm_axis_field_check.py` (read-only check, numpy + the
    hash-bound v4 `BoundP2Evaluator`) and its output
    `scripts/ppm_axis_field_check.output.json`. Commit `beb4772c` (rebased over the
    concurrent topology v3.1 landing `9abbd537`), pushed; `feat/sota-foundation`
    fast-forwarded `9abbd537..beb4772c` (is-ancestor held after the rebase, no force).
    No code, spec, experiment or paper file changed.
- Content:
  - Five questions with sources / theory / check-against-our-fields: heritage (Kornfeld
    patents US 6,523,338 B1 and US 6,803,705 B2, IVEC 2003, IEPC-2007-108/-110,
    IEPC-2011-236: pole shoes and Fe pole rings in the early hardware; TWT period and
    Brillouin rules replaced by Koch's cusp ratio rho, Table 1 of IEPC-2007-110: 1.53/1.70/3.07
    DM9-1 -> 4.07/4.2/10.57 DM10); PPM field theory (odd-harmonic series, Bessel extension,
    cusps at the gap planes, planar separatrix, I_1(x_w) and I_1/I_0 wall ratios, harmonic
    attenuation a_k, end effects); electron dynamics (Mathieu bands from Carlsten 2005,
    Busch/Brillouin, cusp adiabaticity from Schmidt, Sinnis-Schmidt, Leffel-Gray, Howard,
    Rhee-Destler); design rules; pole pieces vs linear-vacuum L1a. 51 verified references
    (6 [T], 14 [R], 24 [C], 5 [B], 2 [P]); every DOI resolved through Crossref, patents
    through Google Patents, books through publisher/library/archive.org records.
  - Script results (Tables A-G2, verbatim in the doc): fitted pitch = geometric pitch to
    0.6 % (P2) / 1.9 % (5 stages), +11-15 % for 3 stages (end effect); wall B_r from the
    fundamental to 0-10 %, paraxial to 4-14 %; wall harmonics 0.2-0.5 %, FWHM 0.96-1.16 x
    2L/3 for L1a AND the iron P2; recorded nulls 0.07-0.25 mm from the gaps (L1a), 0.03 mm
    (P2); v3.1 z_c within 0.025 mm of the nulls, separatrix angle <= 1.6 deg; wall cusp /
    axis peak 0.45-0.61 (theory I_1(x_w) 0.50-0.65), wall cusp / wall magnet-centre
    0.37-0.52 (theory 0.41-0.49); launch field lines reach the wall before the cusp with
    falling |B|; epsilon at the wall cusp 0.05-0.75 orders the mu-variation medians
    exactly; reflections: 0-1/128 for cells within 0.17 pitch of a magnet centre,
    32-88/128 for cells 0.22-0.48 pitch away, median B_turn/B_launch 1.08-1.14.
- Validation:
  - Regex cross-check: 51 contiguous entries, every [n] cited has an entry, every entry
    cited; 41 DOIs all in the Crossref-verified record set (chapter DOI via ISBN filter);
    ASCII and LF on all three files; the 99 table rows in the doc equal the script stdout;
    the script gives identical rows against the rejected v3 bundle (`8cbcdbe6`) and the
    accepted v3.1 bundle (`cec47f12`).
- Follow-up notes / risks:
  - Koch's rho is unreachable in the current catalogue (max 1.03 of 96 designs); any
    HEMP-like study needs r_w/L >= 0.6 (and then iron matters) or tapered stacks.
  - Screening v2: launch by v3.1 cell position and stratify by direction toward the nearer
    magnet centre; record |B| and field rotation at turning points.
  - Main tree `uni-project` needs `git pull --ff-only`.

## 2026-09-03 20:05 AEST - Cusp topology search v3 (recorded rejection) and v3.1 (accepted): literature cusp/cell definition

- Task summary:
  - Worktree `C:\Users\Angus\Desktop\projects\uni-project-topo-v3`, branch
    `exp/cusp-topology-search-v3` from `origin/feat/sota-foundation` (`66879e00`), LF
    verified (only the three pre-existing `-text` files show `w/crlf`). CPU only (12
    workers, `solve_problem_cpu`); GPU untouched. Main tree not touched (memory files
    only). Commits: `bce595dc` v3 code/tests/shakedown, `69159934` v3 preregistration,
    `8cbcdbe6` v3 result (RECORDED `assessment_rejection`), `9fa6359a` v3 post-hoc audit
    + tests binding the recorded outcome, `ca811d11` v3.1 code/tests/shakedown, `988220f3`
    no-ff merge of upstream (docs/pic2d/plasma_v2 only; no bound scope touched),
    `1600cfd3` v3.1 preregistration, `cec47f12` v3.1 result (`accepted_result`),
    `9abbd537` dashboard. Pushed; `feat/sota-foundation` fast-forwarded
    `fb5408bf..9abbd537` (is-ancestor held, no force).
- Changes:
  - `modern/experiments/cusp_topology_search_v3/`: `topology.py` (axis nulls by bisection
    on the axis-regular bicubic `PsiBicubicField`, v1 Jacobian/winding classification
    imported unchanged + analytic `diag(-g_z, 2 g_z)`, event-aware RK4 separatrix trace
    with flux-root cross-check and the v4 bilinear step as comparison, wall cusps / cells
    / mirror descriptors, refinement stability), `fields.py` (4 design sets with identity
    proofs: 96 sweep-v2 via the screening pipeline, 128 sealed v2 candidates with the
    recorded CRLF-era protocol hash substituted into the geometry evidence note, 56 v1
    cases, P2 level-1/level-2 through the v4 adapter), `experiment.py`/`run.py`
    (CampaignPlan, shakedown -> prepare -> one detached execution, 8 binding integrity
    gates, estimands, dataset/CSV, catalogue), `catalogue.py` (consumer contract +
    manifest-bound loader), `protocol.json` (definition with Gildea 2012, Kornfeld 2007,
    Koch 2011, Lewerentz & Schneider 2023 DOI 10.3390/app13063491, Parnell 1996, Haynes
    2010, Murphy 2015), `audit_held_out.py` + `POSTHOC_AUDIT.md`, README/DEVLOG/
    LEARNING_SCRATCHPAD; tests `modern/tests/experiments/cusp_topology_search_v3` (45).
  - `modern/experiments/cusp_topology_search_v3_1/`: byte-copy of v3 with
    `fields.v1_axis_reference` (sealed v1 axis root := cluster containing an
    `axis_sign_change`/`axis_grid` member), `prior_campaign_disclosure.v3`, held-out
    wording, `topology-s05-p0-r0-neg` in the shakedown; tests (42).
  - `modern/visualization/generate_cusp_topology_search_v3_dashboard.py` + template ->
    `cusp-topology-search-v3.html` (957,519 B; v3.1 bundle + v3 lineage + sealed v1/v2
    datasets); `tests/visualization/test_cusp_topology_search_v3_dashboard.py` (9).
- Validation:
  - v3 shakedown 1 caught the v2 `artifact_sha256` convention (artifact bytes, not
    payload) and the P2 `exit_start_m` binary64 mismatch; shakedown 3 on the frozen code
    passed (8 designs, 63 s). v3 execution (797 s, 281/281 resolved and stable, replay
    bit-identical) ended `assessment_rejection`: `held_out_correspondence` false for 14
    v1 designs because the reference kept only clusters with centroid `r_m == 0.0`
    (26/206 sealed axis clusters dropped, 22 in-channel in exactly the 14 designs); the
    read-only audit shows 56/56 under the intended filter (max 17.6 um).
  - v3.1 shakedown (9 designs incl. the defect case) passed; execution 728 s + 72 s
    assessment: `accepted_topology_screening`, 281/281 stable (max wall-intersection
    shift 33 um vs 0.25 mm), held-out 56/56 (180 nulls, max 17.6 um) and 96/96 (479
    nulls, max 27 um), replays bit-identical, bundle 28.9 MB / 1212 files.
  - Headline (v3.1, reported not gated): wall-cusp count 0:6 / 1:140 / 2:36 / 3:56 /
    4:25 / 5:6 / 6:6 / 7:6. Sweep-v2 (96): 2 cusps 30, 3 cusps 47, 4 cusps 19
    (four-wall-cusp fraction 0.198, four-cell fraction 0.490; N stages -> N-1 cusps for
    83/96); cusps at the inter-magnet gaps (|z_c - gap| median 0.14 mm, max 0.26 mm,
    2.4 mm from stage centres); interior cell length 0.91-1.12 pitch; interior wall
    mirror ratio 1.000-1.017; axis mirror ratio 0.20-1.2; separatrix angle median 0.7
    deg (max 18 deg). Four-cell v2 (128): exactly one wall cusp each (z_c/L 0.68-0.84,
    angles 18-51 deg): the weak even stages leave one channel axis null.
    Characterization v1 (56): 0-7 cusps by stage count, cusps at the gaps (median 0.05
    mm). P2: 6.028 / 12.000 / 17.972 mm, axis nulls within 31 um of the PIC cusp planes
    (6.0/12.0/17.95), z_c within 0.02/0.05/0.18 mm of the dashboard wall |B_r| maxima
    (6.05/12.05/18.15); the third cusp is 28 um inside the 18 mm straight end
    (boundary-ambiguous).
  - Tests: v3 45, v3.1 42, `tests/visualization` 138 (incl. 9 new) green. Headless Edge
    screenshots `%TEMP%\cts_v3_dashboard\cusp-topology-search-v3-{desktop,top,
    representatives,narrow}.png`.
- Follow-up notes / risks:
  - Paper Section 8 nulls remain true under their frozen definition; a paper admission
    of v3.1 (numerical-screening gate, Discussion rewording "non-standard definition")
    is the next step and was not done here.
  - The catalogue (`cusp_topology_search_v3_1/results/artifacts/cusp-cell-catalogue.json`,
    loader `catalogue.load_catalogue`) is ready for the screening's launch design and
    the MDO closures under its label; no consumer has ingested it yet.
  - Detached run worktrees `uni-project-topo-v3-run`, `uni-project-topo-v31-run` and the
    Git-common locks stay by design. Main tree `uni-project` needs `git pull --ff-only`.

## 2026-09-03 19:05 AEST - pic2d model v1.4 (recycling, peak gate, triad, CUDA-graph step)

- Task summary:
  - Worktree `uni-project-pic2d`, branch `feat/pic-2d-axisymmetric`; commit `112bb250`
    `feat(pic2d): v1.4 wall-ion recycling, peak-node Debye gate, CUDA-graph step`, rebased on
    `origin/feat/sota-foundation` `8674cc5a`, pushed, and `feat/sota-foundation` fast-forwarded
    `8674cc5a -> 112bb250` (is-ancestor held; no force).
  - Source: `modern/docs/literature/pic-mcc-blockers.md` (blockers 1/2/3/5). Spec
    `modern/spec/pic2d/pic2d-model-v1.4.json`; foundation spec reference added.
- Changes:
  - `neutrals.py`: wall + anode Xe+ recycled as thermal neutrals (gamma 1.0, wall 400 K,
    mixture effusion), fifth ledger, gross/net utilisation, relaxation optional (None =
    physical V/c); v1.3 checkpoints load.
  - `simulation.py`: `PeakDebyeGateConfig` fail-closed on the densest node with a particle
    floor (axis nodes read 1.4e18 per macro-particle); peak-node sample in every record;
    Bohm hook and SEE scaffold (`sensitivity.py`, default OFF); provenance `v1_4_options`.
  - `warp_backend.py`: whole step as one CUDA graph per (ion, inject, mcc) variant; seeds,
    injection carry/count and live counts device-side; bitwise vs direct over 200 steps incl.
    resume. Contended ms/step: 7.8 -> 1.5 (9 k), 8.2 -> 3.0 (0.2 M); 1-2 M pending (v3 run).
  - Runner: v1.4 protocol blocks, `evaluate_triad` (residual/electrode work, dense-cell T_e,
    S, omega_pe dt drifts; soft = plateau precondition, hard = stop after one transit),
    peak_node/recycled/utilisation/triad in status, series.npz, summary; v1.3 series still load.
  - `experiments/pic2d_cft_steady_state_v3/` protocol (recycled fixed point n_g* ~ 4.1e19,
    S ~ 5.4e16, net 25 %, peak gate 4.5, triad, 3.5 h). Campaign proposal revision 2 with a
    review-mapping section. pic2d devlog/scratchpad appended.
- Validation:
  - `tests/pic2d`: 111 passed (twice, after the last edit). All touched files LF.
- Follow-up notes / risks:
  - W x 0.7 (PID 9856) still running at 19:00 (step 2.50 M, 1.56 transits, 2.0 ms/step,
    5880 s budget left; ETA ~20:35 AEST). Per instruction, returned without waiting; the
    W x 0.7 comparison and the v3 launch (`python -m experiments.pic2d_cft_steady_state_v3.run run`,
    detached, 3.5 h) are the next actions once `results-w-0.7/run_state.json` says finished.
  - 60 x 480 grid resolves the peak at 3-4 cells/lambda_D (declared); SEE BN parameters are
    provisional; ms/step at plateau counts not yet measured.
## 2026-09-03 19:40 AEST - plasma v2: sheath-closed four-cell power balance (development)

- Task summary:
  - Worktree `C:\Users\Angus\Desktop\projects\uni-project-plasma-v2`, branch
    `feat/plasma-network-v2-sheath` from `origin/feat/sota-foundation` (`b6bb6215`). New
    package `modern/src/cft_revival/plasma_v2` (constants, models, residuals, manifold,
    solver, targets, pic_context, verification), spec
    `modern/spec/plasma_v2/four-cell-sheath-closure-v2.json`, docs
    `plasma-v2-formulation.md` / `plasma-v2-devlog.md` / `plasma-v2-learning.md`, record
    `plasma-v2-verification.json`, `tests/plasma_v2` (53), REFERENCES.md section. The
    hash-bound v1 package is untouched (bytes equal the paper manifest). Commit `e75151ce`,
    rebased over the concurrent `8674cc5a` and `112bb250` (disjoint paths) to `fb5408bf`;
    `origin/feat/plasma-network-v2-sheath` stays at the pre-rebase `ea798971` (never force);
    `feat/sota-foundation` fast-forwarded `112bb250..fb5408bf`.
- Model:
  - v1 rows R00-R26 verbatim (parity 2.3e-13), R27 with the two corrections (identity,
    4.8e-15), R28-R30 floating-dielectric sheath `Dphi_s,k = c_s T_k` (L&L 6.2.17 /
    Hobbs-Wesson; no-emission 5.27, space-charge-limited 1.02), R31 anode sheath or
    declared fall, R32-R34 declared CL-3-potentials, R35-R37 cusp loss (CL-1 / CL-3
    `A_k exp(-Dphi/T)` / CL-4 hybrid area). Rank 21 (core) -> 28 (+sheath/anode) -> 31
    (+declared). Cusp wall-energy margin with closed form `c_s <= (1 + I_k/Je_k)/CT`.
- Validation:
  - `tests/plasma_v2` 53 passed; `tests/plasma` 58 and `tests/plasma_network` 66 passed
    unchanged; `python paper/scripts/check_paper.py` passed before commit (and re-run at
    HEAD); v1 files hash-match the manifest; verification runner 184 s (192-solve grid:
    CL-3 SCL 73/80, CL-1 SCL 16/16, no-emission 0/96 all blocked by the energy margin;
    Kornfeld DM9.2 reproduced to <= 0.05 eV / 0.006 A with phi_1 solved at 14.07 V; Puca
    2024 states far from a root; CL-4 closes only at 1 kV/1 A, c = 1; PIC v2 context
    table extracted from maps.npz, wall totals within 0.4 % of the run summary).
- Follow-up notes / risks:
  - Development model only; corrections remain PROPOSED_NOT_ACCEPTED; potentials declared;
    PIC shows a staircase; CL-4 needs a cusp sheath-edge density; mode B admissible band
    is a hairline. Main tree `uni-project` needs `git pull --ff-only`.

## 2026-09-03 18:35 AEST - Literature synthesis and revised roadmap; canvas updated to the 18:10 brief

- Task summary:
  - Worktree `C:\Users\Angus\Desktop\projects\uni-project-lit-synth`, branch
    `docs/literature-synthesis` from `origin/feat/sota-foundation` (`b6bb6215`). One new file
    `modern/docs/LITERATURE_SYNTHESIS.md` (569 lines, ASCII, LF, no BOM). Commit `11a10873`
    pushed on the branch; rebased over the concurrent `a3793c27` (MDO L0 campaign v2 paper
    admission) to `8674cc5a`; `feat/sota-foundation` fast-forwarded `a3793c27..8674cc5a`
    (is-ancestor held after the rebase, no force). Docs only; no code, spec, result or paper
    file changed.
- Content:
  - Decision tables for every summary-table row of the three 2026-09-03 reviews
    (`pic-mcc-blockers.md` ccb22d5d, 116 refs; `reduced-models-cusp-topology-blockers.md`
    66879e00, 72 refs; `surrogate-mdo-validation-blockers.md` b6bb6215, 157 refs; 345 total):
    60 atomic recommendations after de-duplication, 53 ADOPT / 6 DEFER / 1 REJECT (screening v2
    option B), each with owner, roadmap step, and the review's own effort and risk.
  - Ordered roadmap: (1) cusp topology search v3 [running] -> (2) screening v2 (n = 512 in
    non-saturated v3 cells, scrambled Sobol, 10 % 2N control, two-stage frozen allocation) ->
    (3) surrogate v3 (binomial-likelihood GP + GLM, gates <= 1.5x binomial floor, split-half
    reliability ceiling) -> (4) MDO v3 (CL-1..CL-4, LVGP kernel, >= 10 seeds, HV + IGD+ + eps,
    Ide-Schoebel sets); (5) plasma network v2 sheath closure [running]; (6) PIC v1.4 ->
    steady-state v3 -> >= 50 us case -> breathing decision -> campaign v1 [running]; (7)
    validation v0 (Brandt 2016 code-to-code) -> v1 (Koch 2011 RPA peak / angle, V&V 20) -> v2
    (Keller 2015 thrust-voltage); (8) preregistration hygiene. Each step: inputs, outputs,
    gates, GPU/CPU estimate, stop conditions; six deferred items with triggers.
  - Corrections section (AST 2024 DOI is FEEP; no TU Berlin dataset; topology nulls were of a
    non-standard definition; utilisation gross not net, ~17-18 % net; "geometric access
    fraction") and 14 paper amendments listed by trigger (paper not edited).
  - Canvas `open-cft-roadmap-status.canvas.tsx`: new `Literature roadmap` tab; seven new ladder
    rows (35): topology v3 RUNNING at rung 4/5 (caveat cells, zero commits), PIC v1.4 +
    steady-state v3 RUNNING at rung 2, plasma network v2 RUNNING at rung 2, screening v2 /
    surrogate v3 / MDO v3 / validation v0-v1-v2 at rung 1 NOT STARTED; MDO v2 row -> In the
    paper (a3793c27); paper row eight gates; ss-v2 row seed-b comparison landed; header,
    merge truth (8674cc5a; main 129 behind), Actions queue, Evidence (review commits, 345 refs)
    at 18:10 AEST; stage summary recounted in Node.
- Validation:
  - Python check on the doc: 0 CR bytes, no BOM, 0 non-ASCII, 61 table rows with 61 unique ids,
    decision counts 53/7/1 before and 53/6/1 after de-duplicating S7 = R2c.
    `git ls-files --eol`: i/lf w/lf.
  - Canvas TypeScript check "no errors" after every edit; Node recount of `ladderRows`
    (35 rows; stage 7: 13 (10 caveat), 6: 0, 5: 6 (4), 4: 8 (3), 3: 1, 1: 7 (1); RUNNING 4,
    FAILED GATES 6, NULL RESULT 2; merged 24 / 2 / 1 / 8).
- Follow-up notes / risks:
  - The canvas is being edited concurrently by another agent (an action row appeared at 18:27
    while editing); re-read anchors before each edit.
  - The three launched streams had zero commits at 18:10; their rows earn nothing beyond
    Specified until a commit exists.
  - Main tree `uni-project` is behind origin (`8674cc5a`); needs `git pull --ff-only`.

## 2026-09-03 18:05 AEST - MDO L0 campaign v2 admitted to the paper claim matrix and manuscript

- Task summary:
  - Worktree `C:\Users\Angus\Desktop\projects\uni-project-paper-mdo2`, branch
    `paper/mdo-l0-v2-claim` from `origin/feat/sota-foundation` (`0ea33a7e`), LF
    verified. Commit `a3793c27` (`paper: admit MDO L0 campaign v2 to the claim
    matrix and manuscript`; rebased over the concurrent literature-review and
    pic2d seed-b commits `41ccb1ef..b6bb6215`, no overlapping paths). Pushed; origin/feat/sota-foundation fast-forwarded b6bb6215..a3793c27 (is-ancestor held after the rebase, no force).
    Main tree not touched (memory files only). No GPU work; nothing under
    `results/`, frozen prereg files or `FYP/` modified.
- Changes:
  - Gate `GATE-MDO-L0-V2` (`numerical-campaign`, `opens_level: null`), new
    manifest type `paper-mdo-catalogue-campaign-manifest` 1.0 (37 roles, 134
    metrics), manifest `paper/evidence/manifests/mdo-l0-v2.json` binding 53
    files by blob + SHA-256 at `a003f766` (bundle, frozen files, screening
    dataset + manifest, v1 bundle files, v1 audit) and the dashboard at
    `0ea33a7e`; 58 gate metric constraints incl. classification, both closure
    ids, catalogue 96, 1440 evaluations, 12/12 gates, import scope 28 = 28,
    duplicates 0, front `[49, 50, 94]`, CL-2 Jaccard 0.0, 6 disclosures.
  - `paper/scripts/generate_mdo_l0_v2_evidence.py` (611 `\Mdb` macros, 168
    derived; four ArtifactClaim tables: HV per optimiser x seed with Pareto
    designs; dense-front catalogue designs with geometry, P(wall), own HV;
    CL-1 widths + CL-2; v1 vs v2), `paper/sections/mdo-l0-v2.tex` + standalone,
    claims CLM-053..060, CLM-035/CLM-052 amended, Section 12 in
    `manuscript.tex` + abstract/contributions/boundary/L1 note/Discussion/
    Limitations/data availability/Conclusion, `check_paper.py`
    `_check_mdo_family` + `_check_mdo_catalogue_campaign`, tests 18 + 8,
    README/checklist/outline S4g/notation, paper devlog + ledger.
- Validation:
  - Numbers verified from the artifacts: 1440 evaluations, 91 infeasible (88
    BO + 3 NSGA-III, not "all BO"), 12/12 gates, HV 9.269e-4 / 2.159e-3 /
    2.151e-3 (0.49/1.13/1.13 of dense 1.907e-3), NSGA-III 5.864e-4 / 6.435e-4
    / 4.652e-4, LHS 1.184e-4 / 1.983e-4 / 2.692e-4, 3/3 and 3/3; seed 101 on
    design 50 (119/160 evaluations), 49 never evaluated and absent from its
    initial design; robust front 96 on 49/50/94 (ranks 1/3/2), nominal 86 on
    49/50/74/94, 75 shared (0.70); 77/96 own dense HV < 1e-9 (73 saturated);
    CL-2 50 points on 25 designs, 0 shared; widths 15/91/94 (0.03/0.82/0.79);
    F9 148 files 0 outside results/; F10 28 = 28; F27 0; F28 15 checks.
  - `check_paper.py` passed after the commit and after the rebase (pre-commit
    only "manifest not committed at HEAD"). `unittest discover -s paper/tests`:
    165 OK (139 + 26). `verify_reproducible_build.py`: two byte-identical
    builds, 578,527 B, SHA-256 `867962101b0eff10f8023c44b96f36fa8dea5c1633678a1d197bf8e321348431`, 41 pages, no errors/overfull.
    Renders `%TEMP%\paper-mdo2-pages\p-01..41.png`: abstract p. 2, Section 12
    p. 27-34 (Table 19 p. 31, Tables 20-21 p. 32, Table 22 p. 33, scope box
    p. 34), Discussion CLM-060 p. 37, Limitations p. 38.
- Follow-up notes / risks:
  - The v2 checker now binds the v1 results tree, the v1 audit blob and the
    screening dataset blob; any change there requires re-admission.
  - Main tree `feat/sota-foundation` is behind; needs `git pull --ff-only`.

## 2026-09-03 18:30 AEST - Literature review: surrogate, MDO and external-validation blockers

- Task summary:
  - Worktree `C:\Users\Angus\Desktop\projects\uni-project-lit-mdo`, branch
    `docs/literature-mdo` from `origin/feat/sota-foundation` (`96220ffc`). Read-only on the
    repository; one new file `modern/docs/literature/surrogate-mdo-validation-blockers.md`
    (LF, 89 kB, 823 lines). Commit `af98b3dd` pushed; rebased over the concurrent
    `66879e00` (reduced-model/topology review) and `ccb22d5d` (PIC-MCC review) to `b6bb6215`;
    `feat/sota-foundation` fast-forwarded `ccb22d5d..b6bb6215` (no force).
- Content:
  - Four blockers (surrogates on binomial labels; MDO under closure uncertainty; external
    validation 0 %; preregistration practice), each with SOTA, pitfalls and recommendations
    mapped to experiments: screening v2 allocation (replicate non-saturated cells to n = 512,
    or 2048 launches on 40 designs; QMC launch sets; N with 2N control), surrogate v3 gates
    (RMSE <= 1.5x binomial floor from actual n, binomial-predictive coverage, split-half
    reliability ceiling instead of 2x baseline, binomial-likelihood GP + binomial GLM),
    MDO v3 (LVGP/descriptor kernel, >= 10 seeds, HV + IGD+ + epsilon, Ide-Schoebel robust
    sets across closures, explicit efficacy gate), validation v0-v3 (code-to-code vs
    Greifswald PIC; ion-energy peak and divergence angle vs Koch 2011 HEMP-T RPA and HIT RPA
    in ASME V&V 20 form; thrust-voltage vs mu-HEMPT / HIT low-power HEMP; geometry trends vs
    HIT magnet-length data), and reproducibility (external registry receipt, RO-Crate,
    gate genealogy, ADEMP fields, shakedowns on excluded designs).
  - Dataset table (Thales HEMP-T 3050/HTM, Greifswald PIC, mu-HEMPT, HIT MCFT/HEMP, MIT DCFT,
    Stanford/Princeton, Leung 1976 cusp) and a quantity-to-measurement mapping table.
- Validation:
  - 157 references; 124 DOIs resolved through the Crossref API (bibliography generated from
    the records), 18 arXiv IDs resolved through the arXiv API, 15 URLs opened (IEPC, ISTS,
    JMLR, PMLR, ASME, NASA). Regex cross-check of prose citations vs bibliography: all
    author-year pairs matched. `git ls-files --eol`: i/lf w/lf; 0 CR bytes, no BOM.
- Follow-up notes / risks:
  - The brief's "Ma 2024 AST" is Yeo, Gadisa, Ogawa, Bang 2024 (FEEP); no TU Berlin HEMP
    dataset exists; Liu 2015 "Plume control" and Courtney IEPC-2007-39 omitted (404).
  - HEMP conference data carry no numerical uncertainties; any V&V 20 comparison must
    declare an assumed u_D.
  - Main tree `uni-project` is behind; needs `git pull --ff-only`.

## 2026-09-03 18:10 AEST - PIC-MCC blockers literature review (docs/literature-pic)

- Task summary:
  - Worktree `C:\Users\Angus\Desktop\projects\uni-project-lit-pic`, branch
    `docs/literature-pic` from `origin/feat/sota-foundation` (`96220ffc`). One document,
    no code/spec/protocol/result change: `modern/docs/literature/pic-mcc-blockers.md`
    (773 lines, LF, UTF-8, no BOM). Commit `bf43a7fa`, rebased over the concurrent
    `66879e00` (reduced-model literature review, different file in the same new directory)
    to `ccb22d5d`; pushed; `feat/sota-foundation` fast-forwarded `66879e00..ccb22d5d`
    (is-ancestor held after the rebase, no force). Main tree untouched (memory files only).
- Content:
  - Six blocker sections (Debye resolution / grid heating; cost; neutral model; missing
    physics = CEX, SEE, anomalous transport, exit boundary, cusp sheaths; plateau and
    statistics; validation targets), each with key references, SOTA, pitfalls, a concrete
    change mapped to `pic2d-model-v1.3.json` / `protocol.json` / the campaign proposal, and
    a "literature is silent" list; summary table (blocker -> change -> effort -> risk);
    116-entry bibliography with DOI or DSpace handle for every entry.
  - Closest analogue found: Brandt et al. 2016 (Trans. JSASS Aerospace Tech. Japan 14,
    Pb_235; Greifswald code, downscaled HEMPT 14 mm x 1.5 mm, 400 V, 0.27 sccm; full text
    read) - nearly our operating point; reports anode current, net ionisation 24 %, beam
    current, cusp potential steps, 160 eV cusp ions, 76 us to quasi-steady state at
    Dx = 2 lambda_D. Also: Kahnfeld et al. 2018 (HEMP breathing ~100 kHz in PIC), Matthias
    et al. 2019/2020 (PIC of the Ogawa-MDO design; similarity-scaling limits), Liu et al.
    2015 (2 mm radius CFT PIC), Savard et al. 2025 and Adams et al. 2025 (implicit-EC ppc
    penalty; grid-heating growth-rate map).
  - The user-cited downstream paper doi:10.1016/j.ast.2024.109516 (Yeo, Gadisa, Ogawa,
    Bang, AST 2024) is about FEEP, not CFT PIC; abstract retrieved from the KAIST Pure
    record (publisher blocked); reported as methodology context only.
- Validation:
  - Every DOI resolved through `api.crossref.org/works/<DOI>` or a bibliographic query with
    `select=`; abstracts via OpenAlex `works/doi:`; theses by DSpace@MIT handle; a scratch
    regex cross-check of in-text citations vs bibliography (fixed a wrong heading count and
    five uncited entries before commit); `git ls-files --eol` clean; 0 CR bytes.
- Follow-up notes / risks:
  - Recommendations that change the fixed point immediately: wall-ion recycling in the 0-D
    inventory (n_g* 2.97e19 -> ~4.5e19 before S responds); peak-node Debye gate; CUDA-graph
    capture of the whole step. The proposal's 3-transit/5-transit plateau is a property of
    the neutral closure; a >= 50 us development case with physical neutral transport is
    recommended before freezing the campaign.
  - Main tree `uni-project` is at `96220ffc`; needs `git pull --ff-only`. Worktree
    `uni-project-lit-pic` can be pruned (fully pushed).

## 2026-09-03 18:40 AEST - Literature review of the reduced-model, cusp-loss and topology blockers

- Task summary:
  - Worktree `C:\Users\Angus\Desktop\projects\uni-project-lit-models`, branch
    `docs/literature-models` from `origin/feat/sota-foundation` (`96220ffc`).
    Commit `66879e00 docs(literature): reduced-model, cusp-loss and topology
    blockers review` pushed; `feat/sota-foundation` fast-forwarded
    `96220ffc..66879e00` (is-ancestor held, no rebase, no force). Main tree not
    touched (memory files only). No code, spec, experiment or paper file changed.
- Changes:
  - New `modern/docs/literature/reduced-models-cusp-topology-blockers.md`
    (935 lines, LF, ASCII): per blocker (four-cell power balance, loss-cone vs
    collisionless wall-hit, wall-cusp/cell definition, geometry->performance
    closure dependence) the verified references with one-line relevance, the
    state of the art, pitfalls, recommendations mapped to ledger rows
    (R28-R31 sheath/anode closures), closures (CL-3 sheath-limited, CL-4
    hybrid-area, CL-3-potentials), a topology-search v3 definition, MDO v3 trend
    gates; summary table (blocker -> change -> effort -> risk); honest gaps;
    73-entry bibliography (72 verified references + one documented DOI
    misattribution).
- Validation:
  - Every DOI resolved through the Crossref works API (title/authors/venue/year
    printed and compared); MIT theses through DSpace handles; IEPC papers through
    the ERPS PDF headers. Full texts read for Kornfeld IEPC-2007-108, Koch
    IEPC-2011-236, Puca 2024, Yeo 2020, Brandt 2016, Lewerentz 2023, Liu 2021,
    Gildea 2012 thesis. `git ls-files --eol` shows `i/lf w/lf`; 0 CR bytes; no BOM.
- Follow-up notes / risks:
  - The supplied "downstream paper" DOI 10.1016/j.ast.2024.109516 is a FEEP paper
    (Yeo, Gadisa, Ogawa, Bang 2024); the brief's "Ma et al. 2024 AST" was not
    found. Recorded in the review, Section 0 and entry 73.
  - Full texts of Kahnfeld 2019 (Rev. Mod. Plasma Phys.), Matyash 2010 (IEEE TPS)
    and Kalentev 2014 (CPP) were not retrievable (timeouts); no figure numbers are
    cited from them.
  - Recommendations are proposals for new preregistrations / a ledger revision;
    nothing recorded is changed.

## 2026-09-03 17:00 AEST - MDO L0 campaign v2 (screened catalogue x operating point) preregistered, executed, recorded, dashboarded

- Task summary:
  - Worktree `C:\Users\Angus\Desktop\projects\uni-project-mdo-v2`, branch
    `exp/mdo-l0-campaign-v2` from `origin/feat/sota-foundation` (`783a82c6`), LF verified
    (only the three pre-existing `-text` files show `w/crlf`). CPU only (`.venv-sota`,
    torch 12 threads, dense pool 12 workers); GPU untouched. Main tree untouched (memory
    files only). Commits: `19c91a90` code/protocol/shakedown/tests/spec index,
    `99914dc2` preregistration (authorities.json only), `a003f766` result (148 files
    under `results/` only, 7.4 MB, from the clean detached worktree
    `uni-project-mdo-v2-run`), `0ea33a7e` dashboard + docs + spec pointer. Pushed;
    `feat/sota-foundation` fast-forwarded `783a82c6..0ea33a7e` (is-ancestor held, no force).
- Changes:
  - `modern/experiments/mdo_l0_campaign_v2/`: `catalogue.py` (screening dataset bound by
    bytes/Git blob/manifest entry/ancestry; 96 designs with accepted-2N per-cell counts;
    pure-Python incomplete beta + quantile; frozen 96 x 64 Jeffreys-posterior sample on
    v1's unit rows), `model.py` (design = catalogue index + Ua/Ia/mdot; closures CL-1
    `prod(1-p_k)` and CL-2 `1-p_pooled`; v1 L0 chain, CVaR, HV; blockwise nondominated
    filter), `optimizers.py` (LHS over catalogue; pymoo mixed NSGA-III with
    `MixedVariableDuplicateElimination`; BoTorch qLogNEHVI over `MixedSingleTaskGP`
    with `optimize_acqf_discrete` over all 96 designs x 8 LHS points then per-member
    `optimize_acqf(fixed_features)` refinement; labels generated from arguments /
    fitted kernels), `experiment.py` (12 binding integrity gates incl.
    `catalogue_binding`, `code_hash_scope_matches_imports`, `nsga3_duplicates_eliminated`,
    `labels_consistent`; parallel dense reference; CL-2 + width sensitivity; per-design
    separability), `run.py`, `protocol.json` (explicit 28-file import-bound hash scope;
    classification `l0_model_optimisation_over_screened_design_catalogue_with_test_particle_wall_loss_closure_not_thruster_performance`;
    closure identification disclosure; `v1_audit_disclosures_closed` F9/F10/F22/F26/F27/F28),
    README/DEVLOG/LEARNING_SCRATCHPAD; `spec/optimization/mdo-l0-campaign-v2.json`.
  - Tests `modern/tests/experiments/mdo_l0_campaign_v2` (53 incl. fresh-interpreter
    import-trace test and label tests) and
    `tests/visualization/test_mdo_l0_campaign_v2_dashboard.py` (7).
  - Dashboard `modern/visualization/mdo-l0-campaign-v2.html` (567 kB; v1 vs v2 panel from
    both pinned bundles; catalogue-front designs with geometry + Wilson P(wall); pooled
    P(wall) vs per-design dense HV scatter; CL-1 vs CL-2; width sensitivity).
  - Docs: `visualization/DEVLOG.md`, `docs/workstreams/optimization-devlog.md`.
- Validation:
  - Shakedown 1 (40 evals/run, seeds 900101/900202, dense 96 x 32): every stage ran;
    `hypervolume_monotone` failed on a -2.1e-16 relative roundoff decrease -> gate
    tolerance 1e-12 + recorded largest decrease; shakedown 2 on the frozen code:
    `accepted_result`, 12/12 gates, bundle validated, import scope == hash scope,
    BO results bit-identical between the two shakedowns.
  - Execution (lock 05:16:27Z, ~83 min wall at 100 % CPU load from concurrent agents):
    `accepted_result`, 12/12 binding gates, 1440 evaluations (91 infeasible, all in the
    BO runs), largest HV roundoff decrease 0.0, NSGA-III 0 duplicates, 28 imported files
    == scope. Dense reference 98,304 evaluations in 54 s: robust HV 1.9073e-3 (front 48 on
    designs 46, 49, 50, 73, 94), nominal 5.498e-3. Final robust HV (fraction of dense):
    qLogNEHVI 9.269e-4 (0.49) / 2.159e-3 (1.13) / 2.151e-3 (1.13); NSGA-III 5.864e-4 /
    6.435e-4 / 4.652e-4 (0.24-0.34); LHS 1.184e-4 / 1.983e-4 / 2.692e-4 (0.06-0.14).
    BO beats LHS 3/3, NSGA-III 3/3 (counts). Pooled robust front 96 points on catalogue
    49, 50, 94 (pooled P(wall) 0.375 / 0.430 / 0.379, the three lowest; 5-stage,
    divergent exit, L 20-29 mm); nominal front 86 on 49, 50, 74, 94; 75 shared (Jaccard
    0.70). CL-2 front: 50 points on 25 designs, HV 0.045, 0 shared with CL-1. Width: w=1/4
    front 15 (Jaccard 0.03), w=4 front 91 (0.82), point 94 (0.79). Per-design
    separability holds for all 96 designs. BO timing 1637/1784/1394 s per seed
    (candidate stage 946-1215 s, refinement 350-462 s, fits 91-105 s; 128/128
    refinements accepted).
  - Tests: venv `tests/visualization tests/optimization tests/experiments/mdo_l0_campaign_v2`
    242 passed / 16 skipped; system Python v2 + dashboard 40 passed / 20 skipped (torch
    absent by design). Headless Edge: 0 JS errors at 1440 px and 390 px; screenshots
    `%TEMP%\mdo2\mdo-l0-campaign-v2-{desktop,top,narrow}.png`.
- Follow-up notes / risks:
  - Every efficacy statement is reported-not-binding with seed counts; three seeds carry
    no significance. A design "wins" under CL-1 only; the CL-2 front is disjoint.
  - BO seed 101 never found design 49 (converged on 50): the categorical kernel treats
    the 96 designs as exchangeable, so finding the best design is exploration-limited at
    160 evaluations; a v3 could give the GP the screening P(wall) as design descriptors.
  - Paper admission of v2 (GATE-MDO-L0-V2) not done here; the paper's v1 Limitations
    ("geometry excluded because no map survives the audit") now has a recorded successor.
  - Main tree `uni-project` at `783a82c6`; needs `git pull --ff-only`. Run worktree and
    Git-common lock `mdo-l0-campaign-v2.execution.lock` kept by design.

## 2026-09-03 15:35 AEST - Wall-loss geometry surrogate v2 (derived features) recorded and dashboarded

- Task summary:
  - Worktree `C:\Users\Angus\Desktop\projects\uni-project-wl-surrogate-v2`, branch
    `exp/wall-loss-geometry-surrogate-v2` from `origin/feat/sota-foundation` (`bfe123d4`),
    LF verified, CPU only (`.venv-sota`, 8 threads). Commits: `21118507` code/protocol/tests,
    `503bf87f` preregistration, `a2b503be` result (from a clean detached worktree at the
    prereg commit), `783a82c6` dashboard + docs. Pushed; `feat/sota-foundation`
    fast-forwarded `3003325d..783a82c6` (is-ancestor held; no force). Main tree untouched
    (its local `feat/sota-foundation` is still at `3003325d`).
- Result (`rejected_surrogate`, terminal `assessment_rejection`):
  - Inputs = 31 derived geometry / field features (hash-bound `features.py`), v1 partition
    inherited byte-identical; selected `botorch-stgp-logit`. On the identical 16 designs:
    pooled RMSE 0.0562 (v1) -> 0.0337 (gate 0.05 PASS), cells 0.1332 -> 0.0904
    (floor-corrected 0.0836, gate FAIL), coverage 0.8125 PASS, 2x-baseline FAIL (ridge on
    the same features 0.0334); gbt 0.0368 did not beat the GP; extrapolation 0.1027
    (reported 0.10). Learning curve flat from 30 designs (0.0455 -> 0.0449 at 50).
  - Predictor contract written and replayed (1e-15), marked
    `not_usable_as_mdo_v2_input_rejected_surrogate`; v3 needs recorded in
    `campaign-result.json`.
  - Dashboard `modern/visualization/wall-loss-geometry-surrogate-v2.html` (v1 vs v2 panel,
    learning curve, feature provenance; 8 tests; headless Edge/Chrome 0 console errors,
    screenshots in `%TEMP%\wlgs2-dashboard`).
- Follow-up notes / risks:
  - The 2x-baseline gate is structurally unmeetable once baselines share informative
    features; a v3 protocol should gate against the binomial floor.
  - Cell labels (128 launches) have floor 0.035 against a 0.05 gate; more launches per
    design, not more designs, is the next unit of evidence.

## 2026-09-03 14:55 AEST - Orbit wall-loss geometry screening v1 admitted to the paper

- Task summary:
  - Worktree `C:\Users\Angus\Desktop\projects\uni-project-paper-geo`, branch
    `paper/geometry-screening-claim` from `origin/feat/sota-foundation`
    (`22e2156b`), LF verified. Commit `3003325d` (rebased over the concurrent
    surrogate commits `aa9349a9..bfe123d4`, no overlapping paths) pushed to the
    feature branch and fast-forwarded into `feat/sota-foundation`
    (`bfe123d4..3003325d`; is-ancestor held after the rebase, no force). Main
    tree not touched (memory files only). No GPU work; nothing under
    `results/`, frozen prereg files or `FYP/` modified.
- Changes (`paper: admit wall-loss geometry screening v1 to the claim matrix
  and manuscript`):
  - Gate `GATE-WALL-LOSS-GEOMETRY-SCREENING-V1` (`numerical-screening`,
    `opens_level: null`) at a fourth recorded outcome
    `accepted-screening-dataset` (added to `SCREENING_OUTCOMES`, kind
    description, notation); manifest type `paper-orbit-screening-manifest`
    1.0 (30 roles, 78 metrics); manifest binding 67 files by blob + SHA-256 at
    `ab7c2897` (results tree first exists there; the merge `22e2156b` adds
    nothing under the experiment), frozen protocol/authorities/shakedown/
    design-authorities equal at `c86bfca3`, dashboard generator/template/HTML
    at `ab7c2897`, 58 gate metric constraints, `posthoc_audit: null`.
  - `paper/scripts/generate_wall_loss_geometry_screening_v1_evidence.py`:
    verifies 2,835 bundle files + sidecars byte for byte, frozen == sealed,
    dataset vs all 196 summaries/handoffs/orbit sidecars (+ representatives'
    endpoint tables), recomputes every Wilson-95 interval exactly, convergence
    flags and least/most ordering, dashboard payload == sealed artifacts; 271
    `\Wlg...` macros (121 derived incl. Spearman rank correlations), four
    `\ArtifactClaim` tables (dataset/convergence; extremes with geometry;
    per-cell; terminations + escape sub-classes).
  - Claims CLM-045 (abstract), CLM-046 (results/convergence; non_claims),
    CLM-047 (tables), CLM-048 (reflections in all 96 designs, 22.8 % of
    orbits; escapes 1635/1127/862; per-cell means 0.65/0.82/0.77/0.55; 94/384
    saturated at 1, 0 at 0), CLM-049 (geometry association as observation:
    rho L -0.05, r_w -0.12, pitch +0.36, stages -0.31, min mirror +0.35, mu
    -0.37, reflection -0.79; "none is a design rule"), CLM-050 (first consumer
    of the coupling-v4.2 export; v4 export as labelled reference row), CLM-051
    (boxed scope), CLM-052 (Discussion: mirror statement field-specific;
    screening = geometry->wall-loss bridge at screening tier; design-dependent
    MDO future work). CLM-016 amended (no longer "no consumer model has
    ingested it"); v4 section + standalone driver updated.
  - `manuscript.tex`: `\GeometryScreeningEvidenceRevision`, Section 11 with
    subsection 11.1, abstract sentence, contributions, evidence boundary, L1
    note, Discussion (retitled paragraph + CLM-052; MDO "planned bridge"
    sentence rewritten), Limitations, data availability, Conclusion.
  - `check_paper.py` `_check_geometry_screening`; tests 17 + 11; README,
    checklist, outline S4f, notation; paper devlog + ledger entries.
- Validation:
  - `check_paper.py` passed after the commit and after the rebase (pre-commit
    only "manifest not committed at HEAD"). `unittest discover -s paper/tests`:
    139 OK (111 + 28), before and after the rebase.
    `verify_reproducible_build.py`: two byte-identical builds, 532,205 B,
    SHA-256 `67a531f9562f785f2eef7a5c6f053c3f2c4cb2918c4e80c240735939670fd720`,
    33 pages, no errors/undefined refs/overfull. Renders
    `%TEMP%\paper-geo-pages\p-01..33.png`: abstract p. 2, Section 11 p. 22-27
    (Table 15 p. 25, Tables 16-17 p. 26, Table 18 + scope box p. 27),
    Discussion p. 28-29, Limitations p. 30-31.
- Follow-up notes / risks:
  - The brief's geometry trend (long channel / large radius lose least) is
    not a population trend (rho -0.05 / -0.12); the paper reports the extremes
    and rank correlations as observations only.
  - Refined re-solve / cross-resolution diagnostic exists for the 4
    representatives only (protocol says every design; `designs.py` gates it on
    `include_refined`); disclosed in the paper; experiment README overstates.
  - The concurrent geometry surrogate v1 was REJECTED (`rejected_surrogate`);
    nothing of it is admitted, and the paper still says no surrogate or
    optimisation consuming the dataset is admitted.
  - Main tree `feat/sota-foundation` is behind; needs `git pull --ff-only`.

## 2026-09-03 14:45 AEST - wall-loss geometry surrogate v1 (preregistered, executed, REJECTED)

- Task summary:
  - Worktree `C:\Users\Angus\Desktop\projects\uni-project-wl-surrogate`, branch
    `exp/wall-loss-geometry-surrogate-v1` from `origin/feat/sota-foundation`
    (`22e2156b`, rebased onto `c32dd780` before the preregistration push; hash
    scope unchanged), LF verified (only the three pre-existing `-text` files
    show `w/crlf`). Main tree not touched (memory files only). CPU only
    (`.venv-sota`, torch 8 threads); GPU untouched.
- Changes:
  - `modern/experiments/wall_loss_geometry_surrogate_v1/`: `protocol.json`
    (gates verbatim from the brief), `data.py` (dataset binding by byte hash +
    Git blob `858de21a` at `ab7c2897` + manifest entry + ancestor check;
    count-ratio check; Haldane-Anscombe logit / Laplace direct known noise;
    seeded stratified partition 50/10/10/16 + top-decile chamber-length
    extrapolation hold-out of 10), `models.py` (pkg ExactGP logit/direct,
    BoTorch STGP fixed-noise Matern-5/2 ARD logit/direct, BoTorch MultiTaskGP
    ICM; baselines mean/kNN-3/ridge; ARD + permutation importance;
    uncertainty-sampling add-on), `predictor.py` (numpy-only consumer of
    `predictor.json`, one GP block format for all three libraries),
    `experiment.py` (callbacks, label reads recorded via
    `before_expensive(kind="label")`, 10 binding gates, 6 structural),
    `run.py`, README/DEVLOG/LEARNING_SCRATCHPAD.
  - Tests `modern/tests/experiments/wall_loss_geometry_surrogate_v1` (57;
    system Python runs the torch-free subset) and
    `modern/tests/visualization/test_wall_loss_geometry_surrogate_dashboard.py` (7).
  - Dashboard `modern/visualization/wall-loss-geometry-surrogate-v1.html`
    (generator + template; 112.7 kB; byte-verifies the bundle; renders the
    recorded REJECTED verdict).
  - Workstream docs: `surrogates-devlog.md`, `surrogates-learning-ledger.md`.
  - Commits `aa9349a9` code/tests, `b602d147` preregistration, `b400d924`
    record (150 bundle files, 776 kB), `bfe123d4` dashboard/docs; pushed;
    `origin/feat/sota-foundation` fast-forwarded `c32dd780..bfe123d4`.
- Validation:
  - Shakedown (final code, shakedown partition, 37 s): `accepted_result` on
    the structural gates; informational science gates 2 of 4 failed.
  - Execution (detached `uni-project-wl-surrogate-run` at `b602d147`, 169 s):
    `assessment_rejection` / `rejected_surrogate`. Selected `botorch-icm-logit`
    (ms mean RMSE 0.1035). Assessment role (16): pooled P(wall) RMSE 0.0562
    (floor-corr. 0.0525; gate 0.05 FAIL); cells floor-corr. 0.129 (raw 0.133,
    floor 0.034; gate 0.05 FAIL); best baseline ridge 0.0546 -> ratio 0.97
    (gate 2.0 FAIL); coverage 0.800 = 64/80 (gate [0.80, 0.97] PASS, variance
    scale 3.34). Extrapolation (10 longest chambers): pooled RMSE 0.093
    (reported gate 0.10 met), coverage 0.84, cells 0.170. Structural: dataset
    binding, single-use labels (access order fit -> method-selection ->
    calibration -> assessment -> extrapolation), no tautology (max
    single-input R^2 0.63), determinism bit-exact, predictor replay 3e-14,
    code contract - all PASS. AL add-on: active better only at budget 20.
  - Tests: venv 41 passed / 16 skipped (post-execution lifecycle), system
    38 / 19; `tests/visualization` + `tests/surrogates` 152 passed.
  - Headless Edge: 0 JS errors; screenshots
    `%TEMP%\wls_probe\shots\wall-loss-geometry-surrogate-v1-{desktop,desktop-tail,narrow}.png`.
- Follow-up notes / risks:
  - The surrogate is NOT an MDO v2 input; the recorded predictor.json is for
    audit. A v2 should use realised-geometry / cusp-relative inputs, more
    designs or launches, and a larger method-selection role.
  - Main tree `uni-project` still at `22e2156b`; needs `git pull --ff-only`.
  - Run worktree kept; Git-common lock stays by design.

## 2026-09-03 14:05 AEST - pic2d phase 4: v1.3 plateau finalized, convergence pair, steady-state dashboard

- Task summary:
  - Worktree `C:\Users\Angus\Desktop\projects\uni-project-pic2d`, branch
    `feat/pic-2d-axisymmetric` rebased onto `origin/feat/sota-foundation` (`d09ffee2`),
    later merged `e9f9af16` (orbit geometry screening landed meanwhile) and
    fast-forwarded sota-foundation to `c32dd780`. `cft_revival.plasma` untouched.
- Changes:
  - `24ab82f4 chore(pic2d): steady-state v2 plateau results (development)`: small
    artifacts of `pic2d_cft_steady_state_v2/results/` + the attempt-1 archive.
  - `a707fc1a feat(pic2d): steady-state dashboard with v2 plateau`:
    `modern/visualization/generate_pic2d_cft_steady_state.py` -> `pic2d-cft-steady-state.html`
    (1.8 MB, 9 tests), runner `--case` + `variants.json` (frozen protocol), README/devlog/scratchpad.
  - `5564480a docs(pic2d): preregistered PIC campaign v1 proposal (document only)`:
    `modern/docs/workstreams/pic2d-campaign-v1-proposal.md`.
- Validation:
  - Plateau recomputed from series.npz: drifts I_d +0.084 %, N_e +4.98 % (marginal),
    n_g -0.18 %, 3.2 transits. Window: I_d 3.44 mA, I_beam 2.29 mA (0.67), S 3.93e16/s
    (46 % utilisation), n_g 2.969e19 = (Q_in - S)/c to 0.03 %, n_e mean 2.13e17,
    peak 1.64e18 = 4.1 n_max at z = 14.3 mm (between the 12.0 / 17.95 mm cusps),
    dz/lambda_D = 3.0 at the peak (under-resolved, stated), energy residual -4.4 % of
    the electrode work, atom closure 7e-15.
  - Tests: pic2d CPU subset 69 + 8 passed (GPU tests skipped while seed-b runs);
    dashboard + runner 22 passed; headless Chrome screenshots under %TEMP%.
- Follow-up notes / risks:
  - `seed-b` running detached (PID 49716, launched 02:32:41Z, 3.5 h budget,
    `results-seed-b/`); at 1.27 us it tracks the base run within 1 %. ETA to 5.12 M
    steps ~3.4 h at 2.5 ms/step - inside the budget only if the average stays <= 2.5.
    `w-half` (W = 4.2e4, not W/2: budget) is to be launched only after seed-b ends.
  - The N_e drift passed by 0.02 % while omega_pe dt was still rising: the campaign
    proposal tightens the criterion (3 %, two consecutive checkpoints, >= 5 transits,
    peak density tracked).
  - ROADMAP_AUDIT.md (dated audit) still says "steady-state v2 result NOT DONE"; it
    predates the plateau and was not edited.

## 2026-09-03 13:55 AEST - orbit wall-loss geometry screening v1 (first wall-loss-vs-geometry dataset)

- Task summary:
  - Worktree `C:\Users\Angus\Desktop\projects\uni-project-orbit-geo`, branch
    `exp/orbit-wall-loss-geometry-screening-v1` from `origin/feat/sota-foundation`
    (`8babb31e`), LF verified. Classification everywhere:
    `SCREENING_L1A_FIELD_TEST_PARTICLE_WALL_LOSS` (L1a sweep-v2 fields, not
    P2-qualified; never accepted physical-orbit evidence).
- Changes:
  - `modern/experiments/orbit_wall_loss_geometry_screening_v1/`: `designs.py`
    (sweep-v2 binding, CPU re-solve of every design with the accepted L1a solver,
    identity proof by hash + QoI replay + stored-map agreement, bore
    `PsiBicubicField`, design-dependent `OrbitConfig`), `experiment.py` (v4
    mechanics imported with attribution; per-DESIGN worker tasks: accepted N/2N
    + refined-N for the four representatives, seal only after the design's own
    N->2N check, endpoint tables, dataset/CSV assembly), `consumer.py` (first
    consumer of the coupling-v4.2 export: Wilson/uncertainty re-derivation,
    closed schema, artifact-hash binding; consumes the v4 export as a labelled
    absent reference row), `run.py` (shakedown/prepare/execute/validate with
    orbit_mc + field-pipeline + experiment-code hash binding and the
    extension-batch timing rule), protocol, README, devlog/scratchpad.
  - Tests: `modern/tests/experiments/orbit_wall_loss_geometry_screening_v1`
    (49) and `modern/tests/visualization/test_wall_loss_geometry_screening_dashboard.py` (8).
  - Dashboard `modern/visualization/wall-loss-geometry-screening-v1.html`
    (generator + template; offline SVG; 970 340 B).
  - Commits `484335c2` tests, `c86bfca3` preregistration, `ce7cb895` dashboard
    code, `5f4a6426` docs, `ab7c2897` record, `22e2156b` clean merge of the
    concurrent MDO/paper commits (no overlapping paths). Pushed;
    `origin/feat/sota-foundation` fast-forwarded `d09ffee2..22e2156b`.
- Validation:
  - Shakedown (3 designs x 64, NON-EVIDENTIARY): run 1 caught `np.bool_` in the
    field evidence (CanonicalizationError); run 2+ `accepted_result`, 238
    validators / 0 failures, bundle validated.
  - Execution (`uni-project-orbit-geo-run`, detached at `c86bfca3`, 12 CPU
    workers, 95 min vs 70 min projected): `accepted_result` /
    `accepted_screening_dataset`; 96 designs, 196 cases, 100 352 orbits, 6664
    validators / 0 failures, 196/196 sealed + replayed + handoffs consumed, 0
    exclusions, 0 timeouts, all 96 converged (max |dP| 0.0059).
  - Headline: P(wall) 0.375-0.869 (median 0.702); reflections in EVERY design
    (32-282 of 512 at 2N; 22 % of orbits) where v4 had zero; escape 0-0.215;
    per-cell means 0.65/0.82/0.77/0.55; no v4-like bimodal cell structure in
    any design; bore interpolation rms <= 0.87 %, cross-resolution <= 0.66 %;
    refined-N within 0.008 of accepted-N.
  - Screenshots `%TEMP%\orbit_geo_probe\wall-loss-geometry-screening-v1-{desktop,narrow}.png`.
- Follow-up notes / risks:
  - Main tree `uni-project` still at `8babb31e`; needs `git pull --ff-only`.
  - The Git-common lock `orbit-wall-loss-geometry-screening-v1.execution.lock`
    stays by design; the run worktree is kept (validate_bundle root identity).
  - The dataset is screening input only: surrogate/MDO consumers must carry the
    label; the MDO's cusp-probability closure can now be made design-dependent.

## 2026-09-03 13:10 AEST - MDO L0 campaign v1 independent post-hoc audit

- Task summary:
  - Worktree `C:\Users\Angus\Desktop\projects\uni-project-mdo-audit`, branch
    `audit/mdo-l0-v1-posthoc` from `origin/feat/sota-foundation` (`ba6875f6`),
    LF verified (`git ls-files --eol`: only the three pre-existing
    `cft_wall_cusp_validation` `-text` files show `w/crlf`). Main tree not
    touched (memory files only). No GPU work (RTX 5090 occupied by the PIC run).
    Commit `6cb9a1af` pushed to `origin/audit/mdo-l0-v1-posthoc`; the base had
    moved (`ba6875f6..22e2156b`: geometry-screening result, four-cell paper
    admission, merge), rebased to `e9f9af16` (no MDO/package path touched in
    between), tests re-run, `feat/sota-foundation` fast-forwarded
    `22e2156b..e9f9af16` (no force; the audit branch stays at `6cb9a1af`).
- Changes (`add MDO L0 campaign v1 posthoc audit`; three files, nothing under
  `results/`, no frozen file, no `FYP/`):
  - `modern/experiments/mdo_l0_campaign_v1/POSTHOC_AUDIT.md`: verdict
    ACCEPTED WITH DISCLOSURES, 28-row findings table, replay bit-exactness
    summary, Git/hash-chain section, six disclosures, v2 change list,
    reviewer reproduction, interpretation section.
  - `modern/experiments/mdo_l0_campaign_v1/audit_replay.py`: read-only audit
    (refuses `--json` inside `results/`); bundle integrity, preregistration
    chain from Git blobs, independent re-implementation of CL-1 + L0 + CVaR +
    dominance + WFG hypervolume + stdlib LHS + radical inverse + Wilson,
    sensitivity/scenario tables, statistics, claim boundary, artifact labels,
    package replays (`--dense`, `--nsga3`, `--bo SEED`).
  - `modern/tests/experiments/mdo_l0_campaign_v1/test_posthoc_audit.py`: 19
    tests re-deriving the table; BO replay test skipped without torch or with
    `MDO_AUDIT_SKIP_BO=1`.
- Validation:
  - Bundle: 137/137 manifest files byte-exact, 0 EOL-only, 0 mismatch; 68
    sidecar pairs consistent; lock/terminal/transition hashes; 13 access
    records before their operations; run spacing >= recorded wall clocks;
    Git-common lock content `4898d0fd`, created 11 ms before the runtime lock.
  - Chain: protocol semantic `09755b85` == authorities == shakedown record;
    source hash `da21671f` recomputed from Git blobs at `4898d0fd`, `c553124b`,
    `e642f38c`, `ba6875f6` and from the working tree; frozen blobs unchanged;
    prereg commit 11 files, experiment-path isolated, no results, on
    `origin/exp/mdo-l0-campaign-v1`; result commit adds 138 results files + the
    spec-index pointer + a test key fix (disclosed).
  - Replay: sample/nominal, 9x16 shared initial designs, 3x96 LHS designs
    bit-exact from stdlib; 864 records bit-exact through the package and
    <= 3.1e-13 relative (objectives <= 5.8e-16) independently; Pareto sets 9/9;
    WFG hypervolumes <= 4.4e-16; BO beats LHS 3/3 and NSGA-III 3/3; pooled
    114/62/24, Jaccard 24/152; dense 8192: fronts 291/166 and every column
    bit-exact through the package (52 s); NSGA-III 3/3 seeds bit-exact
    (pymoo, 1.4-2.2 s); qLogNEHVI seed 101 bit-exact (96 records, curve, 20
    acquisition values) in 3708 s wall under heavy CPU contention (521.7 s
    recorded); prior table 397/480/644/687 feasible, fronts 74/77/114/103,
    common-set invariance 4/4; scenarios: Jeffreys S 6.858e-8, thrust max
    2.7027e-9 N, no_wall_loss 4/110; Wilson(330/512) exact.
  - Tests: system Python 17 passed / 2 skipped (torch absent; HEAD not yet the
    overlay commit); venv (pytest 9.1.1 installed into `.venv-sota` only)
    whole `tests/experiments/mdo_l0_campaign_v1`: 54 passed / 16 skipped.
- Follow-up notes / risks:
  - Disclosures for a v2: bind `experiment_runtime`/`models`/`kernels`/`run.py`
    into the hash scope (active_learning/surrogates are never imported);
    freeze unrounded scenario probabilities (Jeffreys rule gives S 8.06e-8 vs
    frozen 6.86e-8); fix the "sequential greedy batch" label (joint q was
    used) and pymoo's `generations_completed` 7; NSGA-III re-evaluated
    2/3/5 duplicates; binding gates are integrity gates only.
  - The 12 foreign python workers (started 10:56) still saturate the CPU.

## 2026-09-03 13:40 AEST - Four-cell power-balance closure analysis admitted to the paper

- Task summary:
  - Worktree `C:\Users\Angus\Desktop\projects\uni-project-paper-closure`,
    branch `paper/four-cell-closure-claim` from `origin/feat/sota-foundation`
    (`ba6875f6`). Commit `d09ffee2` pushed to the feature branch and
    fast-forwarded into `feat/sota-foundation` (`ba6875f6..d09ffee2`;
    is-ancestor held, no rebase, no force). Main tree not touched (memory
    files only). No GPU work; nothing under `results/`, frozen prereg files
    or `FYP/` modified.
- Changes (`paper: admit four-cell power-balance closure analysis to the claim
  matrix and manuscript`):
  - New gate kind `analytic-consistency` (admits a derivation whose closed
    form is verified numerically, pinned by tests and recomputed by the
    checker; opens no level; `accepted` = derivation + verification admitted
    as recorded; accepts no correction). Gate `GATE-FOUR-CELL-CLOSURE-V1`,
    manifest type `paper-analytic-consistency-manifest` 1.0 binding the
    analysis document, ledger, five `cft_revival.plasma` files, three test
    files, frozen MDO `protocol.json` (blob = prereg `4898d0fd`),
    `FYP/Power_B_EQs.m` (lineage), `AUDIT.md`, `REFERENCES.md` at `266d8a99`,
    verified unchanged at `ba6875f6`; the checkout's package must equal the
    blobs.
  - `paper/scripts/generate_four_cell_closure_evidence.py` recomputes:
    closed form vs residual 2.0e-13 over 400 seeded states (doc 1.9e-13);
    manifold residual 2.8e-13; anode-fall coefficient 2.0; ladder
    p=eps(1,1,1,1) at 300 V/1 A floors 1.35e-6..5.78e-3 vs doc
    1.28e-6..5.78e-3 (max departure 6%, tolerance 25%; floor/eps spread
    1.40; every rung iteration_limit, R27 dominant, rank 22/25, cond <= 20);
    anode-only p=(0,0,0,eps) 6/6 closed (1e-16..2e-13); DM9.2 misfit 1.47e-3
    (doc 1.47e-3, ledger 1.4866e-3); relaxed root 1.18 V below Ua, all rows
    2e-16, infeasible (doc 1.18 V). 13/80 probe read from the frozen protocol
    (= doc reproduction); DE (205 312 evals, 2.06e-2) and 200 LM starts
    (0/200, min 1.82e-3) documented only. 163 `Fcc` macros, 2 ArtifactClaim
    tables.
  - Claims CLM-036 (abstract), CLM-037 (closed form/no root), CLM-038
    (sub-region, ladder, search, Jacobian, probe), CLM-039 (tables), CLM-040
    (attribution: Kornfeld assumption 8, printed anode sign, legacy lines
    136-137, audit fixes cancel), CLM-041 (correction PROPOSED_NOT_ACCEPTED,
    rank 22->21), CLM-042 (claim boundary), CLM-043 (Discussion
    INTERPRETATION: legacy lsqnonlin flags 1-3 accepted by status, residual
    norm discarded, TolFun 1e-50 -> reported values read as residual floors;
    no legacy value claimed), CLM-044 (Discussion INTERPRETATION: mirror
    picture unsupported + four-cell topology undemonstrated + power balance
    inconsistent for interior p -> legacy chain not reproducible; pivot to
    declared closures and kinetic simulation, the latter not admitted).
  - `manuscript.tex`: Section 10 "Consistency of the four-cell power balance"
    after the MDO section with the closed form displayed (coefficient and
    row index macro-bound), abstract sentence, evidence boundary, L2 note,
    Discussion paragraph, Limitations, data availability, Conclusion.
  - `check_paper.py` `_check_four_cell_closure`; tests 19 + 9; README,
    checklist, outline S4e, notation; paper devlog + ledger entries.
- Validation:
  - `check_paper.py` passed after the commit (pre-commit only "manifest not
    committed at HEAD"). `unittest discover -s paper/tests`: 111 OK
    (83 + 28). `verify_reproducible_build.py`: two byte-identical builds,
    493,839 B, SHA-256
    `6ac978b29ab899092e0427c44bbe5f26f8608190589877b50bd729f16ede8a85`, 27
    pages, no errors/overfull. Renders `%TEMP%\paper-closure-pages\p-01..27`:
    abstract p. 1, Section 10 p. 18-19 (eq. 8 p. 19), Tables 13-14 p. 20-21,
    Discussion p. 24, Limitations p. 25.
- Follow-up notes / risks:
  - The brief said the legacy solver "accepted exit-flag-4 floors"; the audit
    and `Performance_est.m:91-128` say flags 1-3 accepted by status and flag
    4 rejected. The claim uses the audit's wording.
  - The doc's "line 137" is the anode term; the +IE cusp terms are on 136.
    The generator binds both and requires the documented line in the span.
  - Any change to `cft_revival/plasma` now fails the paper checker until the
    analysis is re-admitted at the new revision (by design).
  - check_paper now takes ~90 s (35 s recomputation, cached per process).
  - Main tree `feat/sota-foundation` is behind; needs `git pull --ff-only`.

## 2026-09-03 12:05 AEST - MDO L0 campaign v1 admitted to the paper claim matrix and manuscript

- Task summary:
  - Worktree `C:\Users\Angus\Desktop\projects\uni-project-paper-mdo`, branch
    `paper/mdo-l0-v1-claim` from `origin/feat/sota-foundation` (`e642f38c`).
    Commit `9f351776` pushed to the feature branch; rebased over the concurrent
    `266d8a99 fix(plasma)` to `ba6875f6` and fast-forwarded into
    `feat/sota-foundation` (`266d8a99..ba6875f6`, no force). Main tree not
    touched (memory files only). No GPU work.
- Changes (`paper: admit MDO L0 campaign v1 to the claim matrix and manuscript`;
  paper-owned paths + `modern/docs/workstreams/paper-*`; `results/**`, frozen
  prereg files and `FYP/` untouched):
  - Gate `GATE-MDO-L0-V1` reuses `numerical-campaign` (`opens_level: null`;
    `kind_justification`: one accepted campaign on the declared component model
    L0 + closure CL-1; optimiser evidence, not performance evidence); new
    manifest type `paper-mdo-campaign-manifest` 1.0.
  - `paper/scripts/generate_mdo_l0_v1_evidence.py`: 137 bundle files verified
    byte-for-byte (no EOL tolerance), sidecars re-checked, frozen files == sealed
    canonical copies, dashboard (`e642f38c`) payload == sealed artifacts,
    seed stats / attained fractions / Jaccard / scenario survivals / ranges /
    pair wins / invariance recomputed; 334 `\Mdo...` macros, three
    `\ArtifactClaim` tables (HV per optimiser x seed with mean ± std; robust vs
    nominal; priors + scenarios), evidence file + sidecar.
  - Manifest `paper/evidence/manifests/mdo-l0-v1.json`: 36 source files at
    `c553124b`, frozen blobs equal at `4898d0fd`, dashboard files at `e642f38c`,
    68 metrics from raw macro values + 10 fixed policy metrics.
  - Claims CLM-029 (abstract), CLM-030 (results + non_claims), CLM-031
    (tables), CLM-032 (robust vs nominal), CLM-033 (sensitivity; v4 wall-hit
    estimand is not the Kornfeld cusp probability; four-cell solver probe
    13/80), CLM-034 (scope), CLM-035 (Discussion, four readings); contract
    `TAB-MDO-L0-V1` (3 ArtifactClaims).
  - `check_paper.py`: `MDO_METRIC_MACROS`, `MDO_POLICY_METRICS`,
    `_check_mdo_campaign`, renderer, required Section 9, trackables.
  - `manuscript.tex`: `\MdoEvidenceRevision`, abstract sentence, contributions,
    evidence boundary, protocol pointer, Section 9 "Preregistered robust
    multi-objective optimisation of the L0 model" (CL-1 formula +
    `\input{sections/mdo-l0-v1.tex}`), L1-gate note, Discussion paragraph,
    Limitations rewritten, data availability, Conclusion.
  - Tests `test_mdo_l0_v1_admission.py` (17) + `test_mdo_l0_v1_evidence.py`
    (8); README, checklist, supplementary outline S4d, notation; paper devlog
    and learning ledger entries.
- Validation:
  - `check_paper.py`: passed after commit (pre-commit only the fail-closed
    "manifest not committed at HEAD"); re-passed after the rebase.
  - `unittest discover -s paper/tests`: 83 OK (58 + 25), before and after
    rebase.
  - `verify_reproducible_build.py`: two byte-identical builds, PDF 446,316 B,
    SHA-256 `e7900c1000e3d48ef02cc6e67e114dce946c9cadfa4cd7820414ee48bde0d4ff`,
    22 pages, no errors/undefined refs/warnings/overfull (same hash after
    rebase).
  - Renders `%TEMP%\paper-mdo-pages\p-01..22.png`: abstract p. 1; Section 9
    p. 14-18 (Table 10 p. 16, Table 11 + interpretation box p. 17, Table 12
    p. 18); Discussion CLM-035 p. 20.
- Follow-up notes / risks:
  - The concurrent `global-plasma-closure-analysis.md` reproduced the 13/80
    probe and proposes a ledger correction (`PROPOSED_NOT_ACCEPTED`); the
    paper's statements are scoped to the recorded probe and stay true.
  - Robust constraint enforces the worst *sampled* case (max S 0.704); stated
    in the section, Limitations and notation.
  - Main tree `feat/sota-foundation` behind; needs `git pull --ff-only`.

## 2026-09-03 11:35 AEST - Four-cell discharge closure analysis for p != 0

- Task summary:
  - Worktree `C:\Users\Angus\Desktop\projects\uni-project-plasma-closure`,
    branch `fix/plasma-network-closure` from `origin/feat/sota-foundation`
    (`e642f38c`); commit `266d8a99` pushed; `feat/sota-foundation`
    fast-forwarded `e642f38c..266d8a99` (is-ancestor held; no rebase, no
    force). Main tree not touched.
- Findings:
  - Exact probe reproduction: 13/80 closed (all p = 0), floors
    4.739e-4..1.960e-1, 2.9 s per failing multistart; R27 dominant for
    moderate p, current rows R03/R06/R07/R11 for p = 0.64 below 1000 V.
  - Rows R00-R26 are consistent and parametrized by the four potentials;
    substituting them into R27 gives exactly
    `2*(je3(1-p4)+I4)*(phi4-Ua) + EI*(p1 je0 + p2 je1 + p3 je2)` (1.9e-13
    relative over 400 random cases). Both terms >= 0 on the admissible region
    -> no root for any interior p > 0; solutions only for p1=p2=p3=0 (any p4)
    at phi4 = Ua. Continuation floor linear in eps (1.28e-6 at 1e-4 ... 5.8e-3
    at 0.3); anode-only p closes at every eps; DE (205k evals) and 200 random
    LM starts find nothing better; relaxing phi4 >= Ua exposes
    compensating-error roots 0.6-12.6 V below Ua (rejected). Jacobian rank
    22/25 everywhere, condition 9-200, |J^T r| ~ floor (constrained minimum).
  - Origin: Kornfeld IEPC-2007-108 assumption 8 (frozen ionization loss
    "except the recombination losses at boundaries") double-books EI for
    cusp-lost ions; printed anode electron term (phi4-Ua+T4) has an ion's
    sign. Legacy `Power_B_EQs.m` and the published DM9.2 table carry the same
    R27 misfit (1.49e-3). The 2026-09-01 audit corrections cancel exactly.
  - Classification: (a) genuine inconsistency for interior p != 0; (d)
    solution sub-region p1=p2=p3=0 (+ separate current-carrying infeasibility
    for large interior loss at low Ua); (b) secondary solver defect: `sorted()`
    ordering projection stalled 3/16 zero-cusp cases at 1000 V.
- Changes:
  - `plasma.solver.project_nondecreasing` (isotonic PAV) used by
    `solve_global_discharge` and `plasma_network.solver._projection`;
    zero-cusp grid 16/16 (was 13/16). `plasma.residuals.
    potential_parametrized_state`, `global_row_closed_form` (diagnostics).
  - `spec/plasma/equation-ledger.json#global_row_consistency`
    (PROPOSED_NOT_ACCEPTED: drop +EI from Pcusp; anode electron term
    (Ua-phi4+T4); consequence rank 21 -> needs a potential closure) + a
    limitation line; rows/power expressions unchanged.
  - `docs/workstreams/global-plasma-closure-analysis.md`; formulation, api,
    devlog, learning ledger, plasma-network devlog/learning entries.
  - Tests: `tests/plasma/test_closure_p_nonzero.py` (9),
    `tests/plasma_network/test_plasma_network_closure_p_nonzero.py` (2),
    `tests/plasma/test_solver.py` (+13).
- Validation:
  - `tests/plasma` 58, `tests/plasma_network` 66, `tests/physics` 86 (210 in
    one invocation, 27.7 s); consumers `l1a_plasma_coupling` 8,
    `four_cell_topology_search` 9, `_v2` 10, `test_integration_workflows`
    green, `mdo_l0_campaign_v1` 32 passed / 19 skipped (no torch in system
    Python). All changed tracked files LF, no BOM; no `results/`,
    preregistration, or `FYP/` file touched.
- Follow-up notes / risks:
  - MDO v1 disclosure remains historically correct; the solver is now usable
    only for anode-cusp-only loss. A v2 with interior cusp probabilities needs
    the proposal accepted together with a potential closure (model decision).
  - Pre-existing, not mine: `experiments/cft_topology_characterization_v1/
    tests/test_characterization.py::test_accepted_dependencies_match_coupling_v3_commit`
    fails on the base commit (spec dir diverged from `f80a360f`).
  - Main tree is behind 1 (`git pull --ff-only` needed; not done from here).

## 2026-09-03 10:25 AEST - MDO L0 campaign v1: first preregistered optimiser run on the corrected physics

- Task summary:
  - Worktree `C:\Users\Angus\Desktop\projects\uni-project-mdo-v1`, branch
    `exp/mdo-l0-campaign-v1` from `origin/feat/sota-foundation` (`cc7706b2`,
    rebased onto `8babb31e` before the preregistration push). Commits:
    `fdc6b37d` tests/adapter fix/instance index, `4898d0fd` preregistration,
    `c553124b` result, `e642f38c` dashboard + docs; `feat/sota-foundation`
    fast-forwarded `8babb31e..e642f38c` (no force). Main tree not touched.
- Changes:
  - `modern/experiments/mdo_l0_campaign_v1/` (protocol, model, optimizers,
    experiment, run, shakedown.json, authorities.json, README/DEVLOG/
    LEARNING_SCRATCHPAD, results/ 138 files 5.4 MB); 3 design variables
    (Ua, Ia, mdot), 7 uniform uncertain inputs (4 cusp probabilities
    U[0,0.45] calibrated to the v4 pooled survival 0.357, eta, Xe2+ share,
    divergence), closure CL-1 `S = prod(1-p_k)` on the ionised fraction,
    CVaR(0.25) robust objectives (thrust, Isp, thruster-electrical-to-beam
    efficiency, anode power), worst-case beam-current margin constraint;
    qLogNEHVI (cpu float64) vs NSGA-III vs two-stage LHS, 96 evaluations per
    run, seeds 101/202/303; 8 binding gates + 4 reported tests.
  - `cft_revival/optimization/botorch_adapter.py`: `optimize_acqf_list`
    resolved from `botorch.optim.optimize`; `build_qlognehvi(sampler=)`.
  - `spec/optimization/mdo-l0-campaign-v1.json` instance index with the
    recorded bundle pointer (campaign-v1.json `results` stays null).
  - `visualization/generate_mdo_l0_campaign_v1_dashboard.py` +
    `mdo-l0-campaign-v1.html`; tests `tests/experiments/mdo_l0_campaign_v1`
    (5 modules) and `tests/visualization/test_mdo_l0_campaign_dashboard.py`.
- Validation:
  - Shakedown (24 evaluations, seeds 900101/900202) passed on the frozen
    code: `accepted_result`, bundle validated, 8/8 gates, 142 s. `prepare`
    bound protocol `09755b85...`, source `da21671f...`, shakedown `8b5a8293...`.
  - Execution: `accepted_result`, manifest `2a326f3c...`, 143 entries,
    8/8 binding gates; robust HV qLogNEHVI 0.003863/0.003877/0.003860,
    NSGA-III 0.002926/0.003505/0.003271, LHS 0.002844/0.003213/0.002804;
    BO beats random 3/3, NSGA-III 3/3; dense 8192 reference 0.003798;
    design-set invariance holds for all four cusp priors on the common
    feasible set; robust vs nominal fronts 114 vs 62 (24 shared).
  - Tests: venv 119 passed / 14 skipped (campaign + dashboard +
    optimization); system Python 38 passed / 19 skipped (ML runtime absent
    by design). Dashboard: 0 JS errors headless at 1440 px and 390 px;
    screenshots `%TEMP%\mdo_scratch\mdo-dashboard-final-*.png`.
- Follow-up notes / risks:
  - Corrected four-cell solver (`cft_revival.plasma`) closes only for
    p = 0 (probe 13/80); nonzero cusp probabilities have no exact solution
    at 1e-8 - a model-level finding worth its own ticket.
  - The enforced worst case is the worst sampled case (max S 0.704); at
    S = 1 110/114 robust-Pareto designs violate the margin.
  - No geometry -> L0 map exists; geometry radii excluded. Any future MDO
    with geometry needs a validated geometry -> cusp/closure model.
  - `campaign-v1.json#benchmark.results` remains null by validator design.
  - Main tree `feat/sota-foundation` is behind 4; needs `git pull --ff-only`.

## 2026-09-03 09:20 AEST — PIC-2D phase 3b: model v1.3 neutral inventory, steady-state v2

- Task summary:
  - Worktree `uni-project-pic2d`, branch `feat/pic-2d-axisymmetric`. Commits
    `520e6b41` (v1.3 neutral inventory + tests), `67b04f87` (finalize + v2
    experiment), `3c9e606c` (finalize identity fix), `cb40f06d` (v1 no-ignition
    reference results), `8babb31e` (attempt-2 protocol/docs); rebased over
    `cc7706b2`, pushed, `feat/sota-foundation` fast-forwarded to `8babb31e`.
- Changes:
  - `cft_revival/pic2d/neutrals.py`: quasi-steady 0-D inventory (feed, measured
    ionisation, exit effusion, artificial relaxation with exact ledgers); MCC
    real-collision scale n_g/n_g0 under a fixed null ceiling; n_g in checkpoints,
    series, status, plateau rule; `instantaneous_maps`; runner `finalize`.
  - Spec `pic2d-model-v1.3.json`; 388 tests green.
- Runs:
  - v1.2 reference (1.5e19): stopped at 3.4 transits, finalized, no ignition
    (99.8 % beam return).
  - v1.3 attempt 1 (n_g0 5e19, seed 1e16): no ignition in 1.14 µs, archived.
  - v1.3 attempt 2 (n_g0 5.5e19, Q_in 8.55e16/s = 0.0186 mg/s, seed 5e16):
    PID 40636, igniting at 0.54 µs (N_e +24 %, S 2.0e16/s, T_e 8 eV,
    n_g 4.24e19 → fixed point), 2.4 ms/step; ETA 3 transits ≈ 3 h, budget-limited
    near 9–10 transits. Results uncommitted until it ends.

## 2026-09-03 07:10 AEST — PIC-2D phase 2: model v1.1 and snapshot v2

- Task summary:
  - Worktree `uni-project-pic2d`, branch `feat/pic-2d-axisymmetric`. Phase-1
    fast-forward `62de2ca3`; phase-2 commits `3a42bcd7` (all-GPU step),
    `a0fc4a20` (v1.1 budget/spec/docs), `1cdaae80` (snapshot v2 results +
    dashboard), rebased onto `f171e9ec`, pushed, `feat/sota-foundation`
    fast-forwarded to `1cdaae80` (no force).
- Changes:
  - `warp_backend.py`: device block-Thomas Poisson (`device-direct`) with the
    residual contract at sync, tile-reduced push/MCC kernels, device particle
    counts, host sync every `device_sync_steps`; `simulation.py`: ion
    subcycling, electrode-work ledger term; `pic2d-model-v1.1.json` spec;
    `pic2d_cft_snapshot_v2` protocol/runner/README/results; dashboard v2 with
    v1 history + budget panels; `test_pic2d_v11_step.py` (6 tests).
- Validation:
  - `tests/pic2d` 65, `tests/pic` + `tests/orbit_mc` + `tests/visualization`
    303 -> 368 total green on the rebased tree; `git ls-files --eol` clean.
  - Performance: v1 fine case 40.7 -> 5.46 ms/step (5.4 M); single-process
    2.04 ms/step at 1.53 M (target 1.5 ms not met, launch-bound on WDDM).
  - Snapshot v2: coarse pair gate-stopped at 0.93/1.14 µs, fine pair
    wall-budget at 1.20/1.53 µs; no plateau; peak n_e 3.7–5.9× the a-priori
    ceiling; not converged (18–57 % spreads); grid heating on the coarse pair.
- Follow-up notes / risks:
  - Next operating point must come from the kinetic loss fraction
    (0.10–0.35 after one transit time), not the 0-D Bohm bound; neutral
    depletion and ion–neutral CEX still absent; `protocol.json` of v2 is
    hash-bound and must not be edited post hoc.
  - A PowerShell `Set-Content -NoNewline` rewrite destroyed the untracked v2
    runner mid-session (recovered from indentation); WIP is now committed
    before any shell-side file rewrite.

## 2026-09-03 04:55 AEST — L1a sweep v2 + topology nulls admitted to the paper

- Task summary:
  - Worktree `C:\Users\Angus\Desktop\projects\uni-project-paper-topo`, branch
    `paper/topology-and-sweep-claims` from `origin/feat/sota-foundation`
    (`7a30fc2e`), LF verified. Two commits, both fast-forwarded into
    `feat/sota-foundation` (`7a30fc2e..f171e9ec`, no force).
- Changes:
  - `605be5ce add four_cell_topology_search_v2 posthoc EOL audit`: read-only
    `audit_sidecar_eol.py` + `POSTHOC_AUDIT.md` + 10 tests
    (`modern/tests/experiments/four_cell_topology_search_v2/`).
    `results/preregistered-protocol.json` and the bundle's `protocol_sha256`
    record the CRLF digest `ec2e9a73…` (10811 B) of the LF blob `5c195119…`
    (10580 B, 231 lines); payload `bd522269…` recomputes; 25 other entries
    byte-exact. Disclosed: `validate_results` refuses the bundle on LF
    checkouts; GPU replay recorded 2 of 4 diagnostic passes.
  - `f171e9ec paper: admit L1a sweep v2 and topology null results to the
    claim matrix`: gate kind `numerical-screening` (`opens_level: null`,
    `recorded_outcome` accepted-screening / preregistered-null /
    recorded-characterization); gates `GATE-L1A-SWEEP-V2`,
    `GATE-FOUR-CELL-V2`, `GATE-TOPOLOGY-CHAR-V1`; manifests
    `paper/evidence/manifests/{l1a-sweep-v2,four-cell-v2,
    topology-characterization-v1}.json` (20/17/35 blob+sha files, audits at
    `9e68df21`/`605be5ce`, six lineage files); generator
    `generate_topology_screening_evidence.py` (107/107/83 macros, six
    ArtifactClaim tables, audited LF→CRLF rule for exactly two files);
    sections 8.1–8.3; claims CLM-018..028 (abstract sentence, Discussion
    CLM-028 macro-bound); `check_paper.py` screening checker; 25 tests;
    README/checklist/outline/notation; paper devlog + ledger entries.
- Validation:
  - `check_paper.py` passed after the commit (pre-commit: only the three
    "manifest not committed at HEAD" errors). `unittest discover -s
    paper/tests`: 58 OK. `verify_reproducible_build.py`: two identical
    builds, PDF 391,734 B, SHA-256 `6b4c6978…`, 17 pages, 0 errors/overfull.
  - Renders `%TEMP%\paper-topo-pages\p-01..17.png`: Section 8 p. 8–13
    (Tables 4–9 on pp. 10–13), L1 gate p. 14, Discussion p. 14–15.
  - `pytest tests/experiments/{four_cell_topology_search_v2,
    l1a_geometry_sweep_v2} tests/visualization/test_plasma_topology_dashboard.py`:
    green once the trial-build `SOURCE_DATE_EPOCH` was unset.
- Follow-up notes / risks:
  - `four_cell_topology_search_v2/tests/test_search_v2.py::
    test_result_lifecycle_before_or_after_single_run` (experiment-local, not
    collected) fails on LF checkouts via `validate_results`; needs an
    audited-rule branch like the sweep's `protocol.py` if it is to be green.
  - Main tree still at `7a30fc2e`; needs `git pull --ff-only`.
  - Discussion now cites the nulls as evidence (CLM-028) with the labelled
    interpretation framing kept; GATE-L1 remains closed.

## 2026-09-03 03:15 AEST — PIC-2D axisymmetric PIC-MCC (feat/pic-2d-axisymmetric)

- Task summary:
  - Built `cft_revival.pic2d` (worktree `uni-project-pic2d`, branch
    `feat/pic-2d-axisymmetric`, commits 53ac3b02 → f44a7399 → d58fdca1 →
    dd5f2ff1, pushed; not merged).
- Changes:
  - New package: typed models, stair-step channel mesh (exact 2 mm bore),
    cylindrical FV Poisson (block-Thomas direct default + Jacobi-PCG),
    fixed-point bilinear deposition, relativistic-momentum Boris in orbit_mc
    operation order, wall surface charge, LXCat Biagi-v7.1 e-Xe null-collision
    MCC, exit-plane injection, seed plasma, fail-closed stability gate,
    ledgers, canonical artifacts/checkpoints, Warp CPU/CUDA backend.
  - P2 divergent-exit field bound by hash (spec/pic2d/p2-field-authority-v1),
    sampled through the orbit campaign's evaluator + PsiBicubicField.
  - Experiment `experiments/pic2d_cft_snapshot_v1` (development/screening,
    not preregistered) with 4 GPU cases; dashboard
    `visualization/pic2d-cft-snapshot.html`; docs `pic2d-formulation/-devlog/
    -learning-scratchpad.md`; root `.gitignore` negations for the snapshot
    results (checkpoint npz excluded).
- Validation:
  - `pytest tests/pic2d`: 58 passed (38 CPU/verification, 12 Warp parity on
    cpu+cuda:0, 8 dashboard). Poisson order 1.999; Gauss law 1e-9; Boris vs
    orbit_mc ≤ 8 ulp; uniform-field orbit ≤ 1e-6 gyroradii; P2 orbit error
    8e-3→2e-3→2.8e-4 gyroradii with Δx 250→125→62 µm; MCC rates within 4σ;
    checkpoint/resume bitwise on CPU and CUDA.
  - Snapshot: all four cases stopped fail-closed by the ω_pe Δt gate at
    49–60 ns (peak n_e ~1.5e18 m⁻³); I_d 58–74 mA still rising; between-case
    spread φ_max 0.5 %, ⟨T_e⟩ 5 %, peak n_e 8 %, ⟨n_e⟩ 29 %.
  - Screenshots: %TEMP%\pic2d_screens\pic2d-dashboard-desktop.png and
    pic2d-dashboard-narrow.png (headless Edge; cursor-ide-browser could not
    hold a tab from the subagent).
- Follow-up notes / risks:
  - Operating point over-drives the v1 model (no anomalous transport, static
    neutrals): density runs past the resolvable Debye/ω_pe envelope in
    <100 ns. Next: milder point or finer grid/Δt budget before any
    preregistered campaign; ion–neutral collisions, SEE, electrode-work ledger.
  - Review before merge into feat/sota-foundation.

## 2026-09-03 03:20 AEST

- Task summary:
  - Admitted the CFT full-orbit wall-loss v4 campaign into the paper's claim
    matrix, gate registry and manuscript. Worktree
    `C:\Users\Angus\Desktop\projects\uni-project-paper-v4`, branch
    `paper/wall-loss-v4-claim` from `origin/feat/sota-foundation` (`5b85d2ad`);
    LF verified (`git ls-files --eol` shows no `w/crlf`).
- Changes (`paper: admit wall-loss v4 campaign to the claim matrix and
  manuscript`; pushed as `0fabda2c`, rebased over `8466c37a` to `6f3e6dd5`;
  21 files, paper-owned paths + `paper-*` workstream docs only; `results/**`
  and `FYP/` untouched):
  - Evidence level: `numerical-campaign`, a new gate kind beside the
    `physics-level` L1--L3 gates (`opens_level: null`). Justification in
    CLM-013 and in the manifest: verification-tier numerical evidence about a
    collisionless prescribed-field test-particle model; not L1 (no reduced
    performance model / L0 mapping; coupling export is export-only), not L2,
    not L3.
  - `paper/evidence/manifests/wall-loss-v4.json`
    (`paper-test-particle-campaign-manifest` 1.0): 29 source files bound by
    blob + SHA-256 at `6922a3cf`, frozen prereg files (blobs equal at
    `757e365f`), `POSTHOC_AUDIT.md` at `258f69b2`, 35 metrics copied from
    the raw artifact values.
  - Gate `GATE-WALL-LOSS-V4` (accepted, 20 metric constraints) in
    `result-gates.json`; claims CLM-012..CLM-017 (abstract summary, campaign
    result with `non_claims`, generated tables, verification, scope
    limitation, Discussion interpretation); contract item `TAB-WALL-LOSS-V4`
    (`artifact_claim_count: 2`); schema registry type added.
  - `check_paper.py`: flattens `\input{sections/...}` before every prose,
    claim and citation check; `_check_wall_loss_campaign` (byte-identical
    regeneration of evidence/TeX/sidecar from the bundle, artifact hashes on
    disk, metric == raw macro value, results tree unchanged, prereg/audit
    revision chains, macro-only section with no literal digits,
    classification macro rendering, non-claims present, bindings exactly
    once, `\WallLossEvidenceRevision` spells the revision, claim
    binding/location); artifact contract dispatch by `generator_module`;
    claims.json manifest entries checked against their files.
  - `manuscript.tex`: new Section 7 "Accepted numerical campaign:
    collisionless electron wall loss" (after L0 timing, before the L1 gate)
    with `\input{sections/wall-loss-v4.tex}`; macro-bound abstract sentence
    (CLM-012); two-revision boundary text and date line; new Section 11
    "Discussion" (interpretation only: zero reflections -> mirror picture
    unsupported, CLM-017; four-cell wall-cusp topology as an open question,
    topology experiments not admitted so not cited); Limitations, data
    availability and Conclusion updated without new numbers.
  - Section: result/verification/interpretation sentences are exact
    `\EvidenceClaim` bodies; new derived macro `\WlfToleratedSidecars`.
- Validation:
  - `check_paper.py` passed after the commit (before: only the fail-closed
    "manifest not committed at HEAD" error). `unittest discover -s
    paper/tests`: 33 OK (19 + 14 new). `build.py`: 303,672-byte PDF, SHA-256
    `bdfdba4c…`, 11 pages, no errors/undefined refs/overfull boxes;
    `verify_reproducible_build.py`: two byte-identical builds. Same results
    after the rebase. `pdftoppm` renders of pp. 1, 5–9 inspected
    (`%TEMP%\paper-v4-pages\p-0N.png`): section pp. 5–8, Tables 2–3 p. 7,
    interpretation box p. 8, Discussion p. 9.
  - `origin/feat/sota-foundation` fast-forwarded `8466c37a..6f3e6dd5` (no
    force). `origin/paper/wall-loss-v4-claim` stays at pre-rebase `0fabda2c`
    (identical content; not force-pushed). Main tree still at `5b85d2ad`;
    needs `git pull --ff-only`.
- Follow-up notes / risks:
  - Pre-existing: `\EvidenceRevision` renders with a space inside the hash
    (newline in the `\newcommand` body); left untouched, now visible next to
    the new macro on the title date line.
  - Topology results remain unadmitted; the Discussion phrases the four-cell
    topology as an open question and cites nothing.

## 2026-09-03 02:50 AEST

- Task summary:
  - Fixed the pre-existing `tests/visualization` failures (stale
    `design-gallery.json` / `first-results.html`). Worktree
    `C:\Users\Angus\Desktop\projects\uni-project-vizfix`, branch
    `fix/visualization-stale-gallery` from `origin/feat/sota-foundation`
    (`16670281`), LF verified.
- Root cause:
  - `source.config_sha256` hashes the exact bytes of
    `config/l0-deterministic-sweep.json`. Committed blob is LF at `41bf9091`
    and HEAD (`2d727b1a…`, 744 B); the gallery and both test pins were made on
    2026-09-01 from a `core.autocrlf=true` CRLF working copy (`a4703ac1…`,
    763 B). `fab0eccc` (`eol=lf`) made the working copy LF, so the recorded
    hash stopped reproducing. Dataset identity `c0a36ed8…`, the five indices
    and the thrust threshold are unchanged (JSON diff = 1 line; the 6 MB HTML
    differs only by that hash substitution).
- Changes (commit `8466c37a`, rebased once over the concurrent wall-loss v4
  dashboard commits `bc2f8e47..5b85d2ad`):
  - `modern/visualization/design-gallery.json`, `first-results.html`
    regenerated by `build_design_gallery.py` / `generate_first_results.py`.
  - `modern/tests/visualization/test_design_gallery.py`
    `EXPECTED_CONFIG_SHA256` and
    `test_first_results_visualization.py::test_gallery_identities_and_indices_map_to_embedded_sweep`
    pin: `a4703ac1541539829f47f909d24d01d4996ed1da97a9d86e9e2323e54039fbbf` →
    `2d727b1af7d9be9f35f227cc318beae29af6cbd2fbead28842a4c17d67551b6b`
    (provenance pin; CRLF-smudged → committed LF bytes).
  - Dated entries in `modern/visualization/DEVLOG.md` and
    `LEARNING_SCRATCHPAD.md`.
  - Pushed `fix/visualization-stale-gallery`; fast-forwarded
    `origin/feat/sota-foundation` `5b85d2ad..8466c37a` (no force).
- Validation:
  - `pytest tests/visualization -q`: 93 passed (was 65/2F/13E; includes 17
    plasma-topology + 13 wall-loss-v4 dashboard tests). `tests/geometry` 41,
    `tests/fields` 62.
  - `geometry-designs.html`, `axisymmetric-results.html`,
    `plasma-topology-results.html` regenerate byte-identical (not stale).
  - Per-directory sweep (excluding pic/orbit_mc/experiment_runtime): all
    green except `tests/material_fields` (4F, `raw run hash binding failed`),
    `tests/experiments/l1a_geometry_sweep_visualization` (results dir
    untracked + gitignored), `l1a_geometry_sweep_v2_visualization`,
    `l1a_geometry_sweep_v2`, `l1a_field_surrogate_v1`, `l1a_field_surrogate_v2`
    (frozen `protocol.json.sha256` records the CRLF hash `64b2c58c…`; LF blob
    is `2a5ba9e4…`), `l0_surrogate_v3` (2F) and `l0_surrogate_v4` (2F)
    (pre-execution tests assert `results/` absent). None fixed here.
- Follow-up notes / risks:
  - The L1a v2 dashboard failure needs a user decision (EOL-normalised
    verifier with disclosure vs. leaving it red); frozen `results/` must not
    be edited.

## 2026-09-03 02:40 AEST

- Task summary:
  - CFT full-orbit wall-loss v4: offline results dashboard and paper evidence
    chain, in worktree `C:\Users\Angus\Desktop\projects\uni-project-v4-dash`
    (branch `feat/wall-loss-v4-dashboard`, from `origin/feat/sota-foundation`
    at `6922a3cf`).
- Changes:
  - `bc2f8e47 feat(visualization): add CFT full-orbit wall-loss v4 results
    dashboard`: `modern/visualization/generate_wall_loss_v4_dashboard.py`,
    `wall-loss-v4-results.template.html`, generated `wall-loss-v4-results.html`
    (681,963 bytes, cap 1,200,000), `modern/tests/visualization/
    test_wall_loss_v4_dashboard.py`. Whole-bundle verification with the nine
    CRLF-recorded orbit sidecars tolerated via `sha256(LF->CRLF) == recorded`;
    all 4608 endpoints and three reconstructed |B| maps embedded.
  - `ea867bf1 paper: add wall-loss v4 evidence and results subsection`:
    `paper/scripts/generate_wall_loss_v4_evidence.py`, `paper/evidence/
    wall-loss-v4.json` (130 macros with artifact/pointer/format/SHA-256 trace),
    `paper/generated/wall-loss-v4.tex` + provenance sidecar,
    `paper/sections/wall-loss-v4.tex` (draft, macros only, not in
    manuscript.tex) + standalone driver, `paper/tests/
    test_wall_loss_v4_evidence.py`; regenerated stale
    `paper/generated/l0-ranges.provenance.json` (CRLF-era hashes); README,
    paper devlog and learning ledger entries.
  - `5b85d2ad docs(visualization)`: shared visualization DEVLOG/SCRATCHPAD
    entries (convention introduced by the topology dashboard commit).
  - Rebased onto `16670281` (orbit_mc v1.7 sidecar LF fix, v4 post-hoc audit,
    topology dashboard), no conflicts; `feat/sota-foundation` fast-forwarded to
    `5b85d2ad`. `origin/feat/wall-loss-v4-dashboard` left at pre-rebase
    `fb4117e4` (no force push).
- Validation:
  - `pytest tests/visualization/test_wall_loss_v4_dashboard.py`: 13 passed
    (before and after rebase); `tests/visualization` overall 61 passed + 2
    pre-existing failures / 13 errors in design-gallery and first-results
    (stale `design-gallery.json`, untouched).
  - `node --check` on the embedded script: clean. Headless Chrome: 0 runtime
    errors at 1440 px and in a 390 px iframe; no horizontal overflow.
    Screenshots: `%TEMP%\wall-loss-v4-desktop.png`,
    `%TEMP%\wall-loss-v4-desktop-tail-5200.png`, `%TEMP%\wall-loss-v4-narrow.png`.
  - `python paper/scripts/check_paper.py`: passed; `python -m unittest
    discover -s paper/tests`: 19 OK; `python paper/scripts/build.py`: clean
    deterministic manuscript PDF; standalone section: 2 pdflatex passes, no
    errors/undefined refs/overfull boxes.
- Follow-up notes / risks:
  - Manuscript integration of the wall-loss subsection needs a claims.json
    record and a gate/manifest entry; the evidence file records the boundary.
  - Domain exit is 23 mm in the artifacts (task text said 24 mm); the
    dashboard and paper use the artifact value.

## 2026-09-03 01:45 AEST

- Task summary:
  - orbit_mc v1.7: sidecar newline portability fix; v4 wall-loss post-hoc
    audit. Worktree `C:\Users\Angus\Desktop\projects\uni-project-orbit-v17`,
    branch `feat/orbit-mc-v1.7` from `origin/feat/sota-foundation`
    (`6922a3cf`), LF verified (`git ls-files --eol` shows no `w/crlf`).
- Changes:
  - `cc4bd5e1 fix(orbit-mc): v1.7 write sidecars with LF newlines for byte
    portability`: `newline="\n"` in `orbit_mc/artifacts.py` (`write_artifact`
    sidecar, pre-fix line 1484) and `fields/artifacts.py`
    (`_write_canonical_bytes`, pre-fix line 914); every other write in
    orbit_mc / experiment_runtime / fem_reference / coupling / fields is
    binary or a read. `orbit_mc.__version__` 1.6.0 -> 1.7.0; result/
    checkpoint/handoff schema versions unchanged (artifact bytes and sidecar
    text identical, only the sidecar EOL changed). New
    `tests/orbit_mc/test_sidecar_portability.py` (27 tests: CR-free sidecar
    bytes, byte-exact revalidation after simulated `eol=lf` normalisation,
    fail-closed AST newline lint over the five packages with a 3-entry
    non-text allowlist, 22 lint self-checks). v4 experiment tests made
    lifecycle-aware: once `results/manifest.json` exists the frozen 1.6.0
    contract is checked against the recorded bundle + the preregistration
    commit's blobs (`git show 757e365f:...`), and `prepare`/shakedown gate
    are asserted closed for exactly the two orbit_mc binding checks.
    Devlog/learning entries added to `orbit-mc-devlog.md`/`orbit-mc-learning.md`.
  - `258f69b2 add CFT full-orbit wall-loss v4 posthoc audit`:
    `POSTHOC_AUDIT.md`, read-only `audit_sidecar_eol.py`,
    `tests/experiments/cft_orbit_wall_loss_v4/test_posthoc_audit.py` (12).
    Finding: exactly 9 manifest entries (`artifacts/orbits/<case>.json.sha256`)
    differ by EOL only (recorded = CRLF hash, +1 byte); 378 files byte-exact;
    the 9 `.json.gz` orbit artifacts and their content hashes intact; scratch
    copy with CRLF restored makes `_inventory == manifest` and moves
    `validate_bundle` to the by-design `root identity` refusal. Verdict:
    evidence ACCEPTED as
    `collisionless_prescribed_field_test_particle_wall_loss_not_pic`.
  - Pushed `feat/orbit-mc-v1.7`; `origin/feat/sota-foundation` was still
    `6922a3cf` and an ancestor, fast-forwarded `6922a3cf..258f69b2`
    (no rebase, no force).
- Validation:
  - `tests/orbit_mc` 147 (120 + 27), `tests/fields` 62, `tests/coupling`
    143, `tests/fem_reference` 37, `tests/experiment_runtime` 132 + 1
    skipped, `tests/experiments/cft_orbit_wall_loss_v4` 38 (26 + 12).
  - Lint run against the `6922a3cf` blobs reports exactly the two fixed lines.
  - All written tracked files LF, no BOM; `results/` tree `447a5cf7`
    unchanged.
- Follow-up notes / risks:
  - Readers (`load_artifact`, fields `_validate_file_sidecar`) still use
    universal newlines; the strict byte contract lives in the
    experiment_runtime manifest. Left as is (no reader change requested).
  - The nine recorded CRLF hashes remain in the immutable v4 bundle by
    design; reviewers use `audit_sidecar_eol.py`, not `validate_bundle`.
  - Unexpected: the version bump broke 7 frozen-v4 tests until they were
    made lifecycle-aware; any future orbit_mc change relies on that branch.

## 2026-09-03 00:25 AEST

- Task summary:
  - Fixed the `tests/coupling` wall-clock time bomb (9 failures in
    `test_coupling_records.py`, "field map is stale under maximum_age_s").
- Changes:
  - `modern/tests/coupling/test_coupling_records.py`: module alias is now
    `partial(build_screening_proxy, reference_time_utc=NOW)` so every
    freshness check evaluates at the fixture instant `evidence_helpers.NOW`
    (2026-09-01T12:00Z) instead of `datetime.now()`. No product code changed;
    the sanctioned `reference_time_utc` keyword already threads
    `build_screening_proxy -> reverify_accepted_evidence -> validate_provenance`.
  - Commit `4661a7be`, pushed to `origin/feat/sota-foundation` (fast-forward
    from `fab0eccc`, no rebase conflicts).
- Validation:
  - `pytest tests/coupling -q`: 143 passed (was 134 passed / 9 failed).
  - `pytest tests/fields tests/experiments/l1a_plasma_coupling -q`: 70 passed.
  - Ran the 9 tests with `validation.datetime.now` / `v4_records.datetime.now`
    monkeypatched to raise: 9 passed, so the wall clock is never consulted.
  - Edited file has 0 CRLF bytes (LF preserved).
- Audit of other wall-clock-dependent tests (`modern/tests/`):
  - Only product paths defaulting to `datetime.now()` for freshness:
    `coupling/validation._reference_time` and `v4_records` line 1499.
  - All other callers in `tests/coupling`, `tests/experiments/l1a_plasma_coupling`
    already pass `reference_time_utc=NOW` / `ACCEPTANCE_TIME_UTC`.
  - Left unchanged: `test_evidence_boundary.py:34` omits `reference_time_utc`
    but fails closed on evidence type before any freshness check;
    `test_audit_hardening.py:566` uses `datetime.now` only to stamp a record.

## 2026-09-02 23:30 AEST

- Task summary:
  - Investigated why the roadmap is not finishing; identified and started
    fixing the blockers.
- Findings:
  - Orbit wall-loss v1/v2/v3 all failed on code, each after a full one-shot
    preregistration. v3: zero-length step near the wall spins to `max_steps`
    and emits an invalid STEP_LIMIT witness (`orbit_mc` v1.4, `integrator.py`).
  - Uncommitted `orbit_mc` v1.5 fix in main tree passes 102 tests but was
    never exercised on the real P2 field, never committed, never pushed.
  - Main tree diverged from origin (`ahead 1, behind 13`, 84 dirty entries);
    33 worktrees outstanding.
- Changes:
  - No repo code changed by me directly this entry; two background agents
    launched: real-field shakedown + commit/push of v1.5 on
    `feat/orbit-mc-v1.5`; non-destructive reconciliation of main tree.
  - Memory files updated with root cause and guardrails.
- Validation:
  - `pytest tests/orbit_mc` on main-tree v1.5: 102 passed in 7.27 s.
  - No file writes in the repo during the last 4 minutes before launching
    agents (no concurrent editor).
- Reconciliation (23:45):
  - Main tree rebased onto origin; local `746462a8` was a patch-identical
    duplicate of origin `8603a905` and was skipped. 141 dirty entries were
    byte-identical to origin, 12 identical via LFS oid, 12 fem_reference
    text files older than origin (kept only in stash), 35 genuinely new
    files committed in 5 commits and pushed (`25dbeaaf..caf3a04c`).
  - Safety: tag `backup/main-pre-reconcile-20260902-233116`, stash
    `main-tree-pre-reconcile-20260902-233116`, Temp backup of orbit_mc v1.5.
  - Pruned 31 clean, fully-pushed worktrees; 3 remain (main, orbit-v15,
    orbit-wall-loss-v3).
- v1.5 merged (00:06, 2026-09-03):
  - `7cf65053 feat(orbit-mc): v1.5 ...` fast-forwarded into
    feat/sota-foundation; main tree synced (superseded local copies discarded
    after blob-hash proof that origin was a strict superset).
  - Real-field shakedown on v1.5: primary-N/refined-N/enlarged-N/primary-4N,
    512/512 validator passes each, 352 wall_hit / 160 domain_escape, ~40% of
    orbits via the tolerance-close path, 48-60 s per case at N.
  - v3 root cause reproduced on v1.4 (6/64 grazing launches spun; bonus
    ZeroDivisionError on 18/72 near-tangent launches).
  - Open: chord-interpolated event velocity carries up to 6.1e-4 relative
    energy error; 309/512 would fail the 1e-10 gate. Fix = v1.6 (Boris
    sub-push over f*step_dt with midpoint fields, replayable witness).
  - Decided: Warp CUDA not viable for per-particle path (18x slower); CPU.
  - Launched: v1.6 (resume of shakedown agent), v4 scaffold phase 1 with
    mandatory shakedown phase, canvas refresh.
- l1a_plasma_coupling fixed and pushed (`40dcaa4c`, 00:11): adapter now
  loads via serialization v1.2 public loaders; physics bit-identical, only
  provenance hashes moved; 8/8 tests green. Uncovered two latent hazards:
  - CRLF checkout: fixed repo-wide with root `.gitattributes` (`fab0eccc`);
    760 working-tree files re-smudged to LF; fields+orbit_mc 169 tests green.
  - tests/coupling wall-clock time bomb (9 failures since 2026-09-02T12:00Z):
    fix delegated.
- orbit_mc v1.6 merged (`3ab50ef5`, 00:45): event velocity = Boris push of
  f*dt with the step's midpoint fields; witness gains event_velocity /
  midpoint B / midpoint E (required, replayed at 64*eps*|v|). Real-field
  shakedown: max energy error -> 0.0, 1e-10 gate 512/512 on all four cases,
  termination and step counts unchanged. tests/orbit_mc 120 passed. Main
  tree fast-forwarded; `orbit_mc.__version__ == 1.6.0`; 0 CRLF files.
  Hashes differ 1.5 -> 1.6 for every orbit (chord one ulp off v1).
- v4 phase 1 complete (00:46): scaffold in worktree
  `uni-project-cft-orbit-wall-loss-v4` (untracked); shakedown on v1.5 passed
  9/9 cases, 289 validator calls / 0 failures, 118 s wall with 9 parallel
  workers; energy gate failed informationally only (expected pre-v1.6).
  Shakedown exposed a second latent v3 bug: `_convergence` used
  `zip(ordered, ordered[1:], strict=True)` (always raises); v3 would have
  failed assessment even with a fixed integrator. Fixed + regression test.
  21 v4 tests pass. Phase 2 launched (v1.6 contract, re-shakedown, prereg,
  one detached execution, record).
- User request (01:16): see plasma topology and PIC-MCC results.
  - Topology: accepted datasets exist (cft_topology_characterization_v1,
    four_cell_topology_search_v2, l1a_geometry_sweep_v2, coupling v4) but no
    dashboard. Launched `feat/topology-dashboard` agent (HTML + tests +
    browser screenshots, ff-merge when green).
  - PIC-MCC: only a verified 1-D electrostatic foundation exists; no
    thruster run ever. Launched `feat/pic-2d-axisymmetric` agent to build
    `cft_revival.pic2d` (r,z ES PIC-MCC on qualified P2 B-field, Xe MCC,
    Warp GPU kernels, CPU parity, verification tests) and a development
    snapshot experiment + dashboard. Not merged without review.
- v4 ACCEPTED (01:20): first accepted physical-orbit evidence. Commits
  23d37bee (tests) / 757e365f (prereg) / 6922a3cf (result) on
  exp/cft-orbit-wall-loss-v4, fast-forwarded into feat/sota-foundation.
  One execution, 667 s, all 15 binding gates true, 289 validators / 0
  failures, 4608 orbits: wall-hit 0.641-0.645 (Wilson ~[0.60, 0.68]),
  escape 0.355-0.359, reflection 0, timeout 0; convergence changes
  <= 0.0039 vs 0.01 gate; energy error identically 0.0. Per-cell bimodal:
  cells 2-3 100% wall loss, cell 4 (past wall end) 100% escape, cell 1
  direction-dependent. Zero reflections -> mirror picture falsified here.
  Disclosures: sidecar CRLF recording defect (9 files; v1.7 fix + posthoc
  audit launched), duplicated execute refused by lock (no side effects).
  Launched: v1.7 + audit, v4 dashboard + paper evidence, canvas refresh.
- v1.7 + posthoc audit merged (01:52): `cc4bd5e1` (LF sidecars in
  orbit_mc + fields artifacts; AST lint requiring `newline=` on every text
  write; 27 tests) and `258f69b2` (v4 POSTHOC_AUDIT.md + read-only script +
  12 tests: 9 sidecars differ by EOL only, 378/378 other entries byte-exact,
  evidence ACCEPTED). Main tree at 258f69b2, orbit_mc 1.7.0. v4 tests made
  lifecycle-aware (frozen 1.6.0 contract checked against recorded bundle).
- Topology dashboard merged (`16670281`, 02:06):
  `modern/visualization/plasma-topology-results.html`, 72 hashed sources,
  17 tests, headless-Chrome checked at 1440/390/320. Honest headline: the
  strict wall-cusp/cell topology was found NOWHERE — characterization v1
  0/0 stable cusps/cells over 56 designs (1276 nulls excluded), four-cell
  v2 0/128 (preregistered null), coupling v4 never promoted. Positives:
  sweep v2 96/0 (3-5 axis cusps per design), P2 divergent-exit qualified,
  orbit v4 accepted. Stale design-gallery visualization tests -> fix agent.
- 02:22-02:35: v4 dashboard + paper evidence merged (`bc2f8e47`, `ea867bf1`,
  `5b85d2ad`; 130 artifact-bound macros; section not yet admitted to the
  manuscript -> claim-matrix agent running). Stale design-gallery fixed
  (`8466c37a`): CRLF-era `config_sha256` pin, no data change; visualization
  93/93. Sweep found remaining red dirs, all CRLF-era frozen pins or
  lifecycle-unaware tests (l1a_geometry_sweep_v2 protocol sidecar and its
  three dependents, l1a_geometry_sweep untracked results, material_fields
  raw-run binding, l0_surrogate_v3/v4 pre-execution asserts, duplicate
  basename collection clash) -> test-health agent running with the v4
  posthoc-audit pattern (never edit evidence).
- Paper admission merged (`6f3e6dd5`, 03:00): new gate kind
  `numerical-campaign` (opens no physics level; L1-L3 stay closed), claims
  CLM-012..017, typed manifest (29 blob+sha bound files), check_paper now
  flattens \input before prose checks (closed a bypass), Section 7 + abstract
  sentence + Discussion (mirror picture unsupported; four-cell topology open
  question). 33/33 paper tests; 11-page PDF reproducible twice.
- PIC-2D phase 1 delivered (03:09, branch feat/pic-2d-axisymmetric, 4
  commits, NOT merged yet): `cft_revival.pic2d` (r,z) ES PIC-MCC, Warp CUDA,
  58 tests (12 GPU parity), LXCat Biagi-v7.1 Xe cross sections hash-bound,
  P2 level-1 field bound; Poisson order 1.999; orbits vs orbit_mc 2.8e-4
  gyroradii at 62 um. Snapshot v1 (300 V, n_g 5e20, 0.1 A): all four cases
  stopped fail-closed by omega_pe*dt gate at 49-60 ns as n_e passed
  ~1.5e18 m^-3; no plateau; both grids Debye-under-resolved at the end.
  Phase 2 launched: merge, diagnose over-densification, all-GPU step with
  warm-start PCG + ion subcycling, v1.1 operating-point budget, snapshot v2
  to >= 1 ion transit time.
- Test health closed (03:46, `9e68df21..7a30fc2e`): full `modern/tests`
  green in ONE invocation, 1677 passed / 0 failed / 5 documented skips;
  paper/tests 33. Posthoc EOL audits for l1a_geometry_sweep_v2 and
  material_fields (source-code hash bound into artifacts -> re-bound via
  the workstream's replay-guarded tool, isolated commit); l1a_geometry_sweep
  results force-tracked; l0 v3/v4 tests lifecycle-aware; importlib import
  mode. pic2d phase 1 merged (df4b2d77..62de2ca3). Main tree 7a30fc2e.
  Launched: paper admission of L1a sweep v2 + topology nulls; independent
  full-suite confirmation running in background.
- Paper: topology admitted (04:50, `605be5ce` four-cell v2 EOL audit,
  `f171e9ec` admission). New gate kind `numerical-screening` (admits a
  study at its recorded outcome; null stays a null). Three gates (sweep v2
  accepted-screening, four-cell v2 preregistered-null, characterization v1
  recorded), claims CLM-018..028, Section 8 with Tables 4-9, Discussion
  CLM-028 evidence-cited. paper/tests 58/58; 17-page PDF reproducible
  (6b4c6978). Independent full suite on main: 1677/0/5. Main tree f171e9ec.
- PIC-2D phase 2 merged (`3a42bcd7..1cdaae80`, 06:53): v1.1 all-GPU step
  7.5x faster (5.46 ms at 5.4 M particles; 2.0 ms at 1.5 M; WDDM launch
  floor ~1.2 ms). Snapshot v2 at the 0-D operating point: 0.93-1.53 ion
  transit times, NO plateau, NOT converged; kinetic ion loss only 10-35% of
  ionisation after one transit -> density grows ~linearly past the a-priori
  ceiling; coarse grid heats. Every v1 gate trip was an axis node (shot
  noise ~2.2x the window peak). Phase 3 launched: v1.2 operating point sized
  from measured kinetic loss, fine grid only, one detached resumable
  steady-state run with a continuous plateau criterion (12 h budget).
- PIC phase 3 (07:20, `44b7c8dc`): resumable steady-state runner +
  plateau criterion merged; v1.2 sized from measured kinetics
  (tau_i,eff 2.4 us, nu_iz*tau = 2.9 at n_g 1e20 -> genuine avalanche; no
  equilibrium for n_g >= 3.4e19 with static neutrals). v1.2 run at n_g
  1.5e19 does NOT ignite (2.94/3 mA mirror-reflects to exit) -> trivial
  low-density plateau = no-ignition reference (PID 49664, 0.94 ms/step).
  Decision: build v1.3 quasi-steady 0-D neutral inventory (fixed point =
  neutral-balance steady state), finalize the reference, launch v2.
  Final roadmap audit agent launched in parallel (ROADMAP_AUDIT.md + canvas).
- Final roadmap audit committed (`cc7706b2`, 08:03): 63% (58-68) vs canvas
  70%; P0 90->75, P4 94->76, P5 64->50, P6 44->40. Accepted evidence: legacy
  audit, L0 model, sweep v2, P2 divergent-exit, wall-loss v4, two topology
  nulls; development-only: PIC, L1b, all surrogate campaigns, coupling
  promotion; NOT DONE: any MDO run on the new physics, consumer
  integration, converged PIC, external validation, release audit. Fresh
  worktree suite 1702/0/5; paper 58/298; PDF reproducible. pic2d v1.3 +
  steady-state v2 protocol committed (520e6b41, 67b04f87). Launched: first
  preregistered MDO campaign on the corrected L0 with UQ cusp inputs.
- PIC v1.3 merged (`520e6b41..8babb31e`, 09:18): quasi-steady 0-D neutral
  inventory (artificial tau_g 30 ns, physical fixed point), 388 tests green.
  v1.2 reference finalized (cb40f06d): 3.4 transits, 99.8% beam return, no
  ignition. v1.3 attempt 1 (seed 1e16) no ignition; attempt 2 (seed 5e16,
  n_g0 5.5e19, 0.0186 mg/s) launched 09:02 PID 40636 and IGNITING: at
  0.26 transits N_e 310k rising, S 1.8e16/s, n_g 4.28e19 -> fixed point,
  2.46 ms/step; ETA 3 transits ~12:00 AEST. Status file lives in the pic2d
  worktree: uni-project-pic2d/modern/experiments/pic2d_cft_steady_state_v2/results/status.jsonl
- User challenged the subsystem bar chart (10:04): bars were pre-audit;
  re-derivation from the audit delegated with a per-bar reason (building /
  failed gates / blocked by physics / needs data). Launched wall-loss vs
  geometry screening over the 25 non-dominated sweep-v2 designs on L1a
  fields (screening tier) to create a coupling consumer, surrogate training
  data, and a design-dependent MDO loss input.
- MDO L0 campaign v1 ACCEPTED (10:13, `fdc6b37d..e642f38c`, main tree
  synced): first optimisation on the corrected physics. 864 evaluations,
  8/8 gates, 28 min. qLogNEHVI HV 0.003867 +/- 9e-6 beat LHS 3/3 and
  NSGA-III 3/3, reached 1.02x the 8192-point dense reference with 96 evals.
  Robust (CVaR) front far more conservative than nominal (eff 0.255 vs
  0.89). Findings: (1) the v4 collisionless wall-hit estimand is NOT the
  Kornfeld per-cusp probability (using it gives S=7e-8, zero thrust);
  (2) corrected 4-cell solver closes only for p=0 (13/80) -> investigation
  agent launched; (3) geometry excluded because no geometry->L0 map survives
  the audit -> the geometry wall-loss screening is the bridge. Paper
  admission agent launched.
- Four-cell closure analysis merged (`266d8a99`, 10:58): the Kornfeld
  four-cell power balance is INCONSISTENT for any interior cusp
  probability - substituting R00-R26 into the global power row leaves
  R27 = 2(je3(1-p4)+I4)(phi4-Ua) + EI(p1 je0 + p2 je1 + p3 je2) >= 0, so
  roots exist only for p1=p2=p3=0 at phi4=Ua. Cause is Kornfeld assumption
  8 (ionisation booked twice; anode electron term with ion sign), present
  in legacy Power_B_EQs.m; not introduced by the rebuild. Continuation,
  DE (205k evals) and 200 LM starts confirm no branch. Secondary solver
  defect fixed (sorted() -> isotonic PAV; zero-cusp grid 16/16). Proposed
  correction recorded PROPOSED_NOT_ACCEPTED (would free all potentials).
  24 pinning tests. This explains why the legacy lsqnonlin runs "accepted"
  exit-flag-4 residual floors: the system they solved has no solution.
  Paper admission of this finding queued behind the MDO admission (same
  paper files).
- MDO v1 admitted to paper (`ba6875f6`, 11:02): GATE-MDO-L0-V1
  (numerical-campaign), 334 macros, Tables 10-12, Section 9, claims
  CLM-029..035; 83/83 paper tests; 22-page PDF reproducible (e7900c10).
  Launched: paper admission of the four-cell closure analysis (new gate
  kind `analytic-consistency`, recomputes the verification).
- Canvas restructured to the 8-rung stage ladder (11:15): 25 rows, every
  rung cited; 0/25 externally validated, 10 in the paper (3 all-green:
  sweep v2, wall-loss v4, MDO v1), 4 stuck at prereg-recorded (failed/
  superseded), 9 at runs-on-real-inputs (2 RUNNING). `origin/main` is a
  stale one-commit baseline (7ca3dc2d); feat/sota-foundation is 97 ahead,
  nothing ever merged to main -> user decision. Geometry screening was
  preregistered (c86bfca3) and is executing. Launched: independent post-hoc
  audit of the MDO v1 bundle (its accepted rung was caveated: no audit).
- Closure analysis admitted to paper (`d09ffee2`, 12:26): new gate kind
  `analytic-consistency`, generator recomputes the verification from the
  bound plasma package (closed form 2.0e-13; ladder within 6%); Section 10,
  Tables 13-14, claims CLM-036..044 (Discussion ties the three chain
  findings). 111/111 paper tests; 27-page PDF reproducible (6ac978b2).
  Correction: legacy accepted lsqnonlin exit flags 1-3 by status (flag 4
  rejected), TolFun=1e-50. `cft_revival.plasma` is now hash-bound by the
  paper; any change requires re-admission.
- PIC steady-state v2 REACHED PLATEAU (12:18): 3.2 transits, 7.68 us,
  5.12 M steps, 2.8 h; I_d 3.5-4.0 mA, I_beam 2.2-2.4 mA, S ~4e16/s,
  n_g 2.95e19 (fixed point ~3.4e19 predicted), omega_pe*dt 0.114, ~1 M
  particles/species. First PIC plateau in the project (development,
  single seed). Phase 4 launched: finalize/commit, convergence pair
  (seed-b then W/2, sequential), dashboard, campaign proposal.
- Geometry wall-loss screening RECORDED (12:42, `c86bfca3` -> `ab7c2897`,
  merge `22e2156b`; main synced): 96 L1a designs, 100,352 orbits, 6664
  validators / 0 failures, 96/96 converged. P(wall) 0.375-0.869 (median
  0.70); REFLECTIONS IN EVERY DESIGN (22%) -> v4's zero reflections is
  specific to the P2 divergent-exit field; no bimodal cell structure in L1a
  designs. `consumer.py` = first consumer of the coupling-v4.2 export (gap
  closed). Shakedown caught a numpy-bool canonicalization error pre-freeze.
  Launched: geometry surrogate campaign v1 (first physically-varying
  training data), paper admission of the screening, canvas refresh.
- MDO v1 posthoc audit merged (`e9f9af16`, 12:47): ACCEPTED WITH
  DISCLOSURES. 137/137 byte-exact, hash chain closes from Git blobs, all
  864 records + dense 8192 + NSGA-III 3/3 + qLogNEHVI seed 101 replay
  bit-exact on the pinned runtime; independent stats within 1e-12. Six
  disclosures, none changing a number: two non-results files in the result
  commit; hash scope inert/incomplete (active_learning/surrogates bound but
  unused; experiment_runtime/models/kernels/run.py used but unbound);
  Jeffreys scenario rounding (S 6.86e-8 vs rule 8.06e-8); all 8 binding
  gates are integrity gates (acceptance != efficacy); NSGA-III duplicate
  evaluations; two wrong descriptive labels. Lessons for MDO v2: bind the
  hash scope to actual imports; eliminate_duplicates=true.
- PIC phase 4 merged (`24ab82f4..c32dd780`, 13:02): plateau verified
  (drifts I_d +0.08%, N_e +4.98% marginal, n_g -0.18% at 3.2 transits);
  I_d 3.44 mA, beam fraction 0.665, utilisation 46%, n_g on the analytic
  fixed point to 0.03%, peak n_e 1.64e18 = 4.1x n_max at Dz = 3 lambda_D
  (UNDER-RESOLVED at the peak - stated). Cusps z = 6.0/12.0/17.95 mm; wall
  ion flux peaks at the 12.2 mm cusp. seed-b running (PID 49716, ETA ~16:00
  AEST); w-0.7 queued after it. Steady-state dashboard + campaign proposal
  (~110-125 GPU-h) committed.
- Wall-loss geometry surrogate v1 REJECTED honestly (`aa9349a9..bfe123d4`,
  13:39, main synced): pooled RMSE 0.056 vs 0.05 gate and 0.97x ridge
  (gate 2x); cells 0.13; coverage 0.80 (boundary); extrapolation 0.093 met.
  Diagnosis: step discontinuities of the design->geometry map
  (stage_count_selector, exit_length_fraction) carry the signal; smooth
  magnet parameters carry none -> stationary GP on raw design parameters
  cannot represent it. Predictor contract published for audit only.
  Launched v2: derived physical geometry + field-descriptor features, per-
  stage-count GP / one-hot, tree baseline, same partition/gates as v1.
- Geometry screening admitted to paper (`3003325d`, 13:58, main synced):
  GATE-WALL-LOSS-GEOMETRY-SCREENING-V1 (numerical-screening, outcome
  accepted-screening-dataset), Section 11, Tables 15-18, claims
  CLM-045..052, CLM-016 amended; Discussion: mirror statement is
  field-specific. 139/139 paper tests; 33-page PDF (67a531f9). Corrections
  vs my brief: length/radius is NOT a population trend (Spearman -0.05 /
  -0.12; strongest: reflection fraction -0.79); cross-resolution check
  exists for 4 representatives only (screening README overstates -> debt).
- Surrogate v2 rejected but improved (`21118507..783a82c6`, 14:26, main
  synced): derived features cut pooled RMSE 0.056 -> 0.034 (gate passes),
  cells 0.133 -> 0.090 (gate 0.05 fails); ridge on the same features is as
  good (0.033) so the 2x gate is unmeetable; tree did not beat GP; learning
  curve flat beyond 30 designs -> LABEL NOISE (binomial floor 0.035/cell at
  128 launches) is the limit, not design count. Features that matter:
  per-cell cusp/null distances, stage pitch, stage count 5. Decision: stop
  surrogate iteration (two honest rejections + diagnosis); use the 96-design
  catalogue DIRECTLY in MDO v2 (no surrogate). Screening v2 with more
  launches per design (~4x, ~6 h CPU) is the recorded route to surrogate v3.
- MDO L0 campaign v2 ACCEPTED (`19c91a90..0ea33a7e`, 16:59, main synced):
  first geometry-dependent optimisation. 96-design catalogue x operating
  point, per-cell P(wall) from the screening with Jeffreys-Beta UQ, CL-1
  (declared identification, "wins under this closure only") + CL-2
  sensitivity. 1440 evals, 12/12 integrity gates, ~83 min. qLogNEHVI beat
  LHS 3/3 and NSGA-III 3/3 (seed 101 stalled at 0.49x dense; 202/303 at
  1.13x). Robust front = designs 49/50/94 = the three lowest-loss designs
  (five-stage, L 20-29 mm, r_w 1.8-2.1 mm); 77/96 designs ~zero survival
  under CL-1 (saturated cell). CL-1 vs CL-2 fronts share NO design ->
  ranking is closure-dependent. All six v1 audit disclosures closed with
  binding gates. PIC seed-b budget-stopped at 2.5 transits (I_d 4.0 mA,
  I_beam 2.66, n_g 2.97e19). Launched: MDO v2 paper admission, PIC seed-b
  comparison + W x0.7 case, canvas refresh.
- PIC seed-b comparison merged (`41ccb1ef`, `96220ffc`, 17:20, main
  synced): over the common 4.85-6.06 us window seed-b agrees with base to
  <= 1.1% on every plateau quantity (I_d +0.40%, I_beam +0.64%, S +1.05%,
  n_g -0.83%, N_e +0.57%); currents/counts within batch-means noise,
  smooth quantities show a genuine sub-percent seed offset 3-7x above pure
  shot noise (correlated fluctuations). Statistically consistent. `finalize`
  now refuses to downgrade a self-finished run. W x0.7 launched (PID 9856,
  2.7 ms/step, projected budget stop at ~2.7-2.9 transits).
- Literature review 1/3 merged (`66879e00`, 17:49): reduced models / cusp
  loss / topology; 72 verified refs. Findings: Puca et al. 2024 (the only
  independent re-solve of Kornfeld's 28 eqs) also lands on phi4 = Ua with
  unphysical p -> corroborates our closed form; published reduced models
  close potentials with SHEATH rows -> accept R27 corrections only with
  R28-R31 + a declared potential closure; no published closure uses a
  collisionless wall-hit fraction -> relabel as "geometric access fraction",
  add sheath-limited / leak-width closures; in PPM stacks nulls sit on the
  AXIS and the wall cusp is the separatrix-wall intersection (Gildea 2012)
  -> our v1/v2 topology nulls were BY CONSTRUCTION of a non-standard
  definition. The AST 2024 DOI the user gave is a FEEP paper (Yeo 2024),
  not cusped-field; real downstream lineage: Fahey 2017, Yeo 2020/2022,
  Puca 2024. Launched: cusp topology search v3 with the literature
  definition (96 designs + P2), consumer catalogue for cells.
- Literature review 2/3 merged (`ccb22d5d`, 17:53): PIC-MCC; 116 verified
  refs. Closest analogue Brandt et al. 2016 (Greifswald HEMPT 14 mm x
  1.5 mm, 400 V / 0.27 sccm: I_anode 4.3 vs 4.5 mA measured, net ionisation
  24%, beam 2.5 mA, 76 us to quasi-steady, dx = 2 lambda_D). Our 46%
  utilisation is inflated: 59% of ionisation is absorbed at the wall and
  never recycled as neutrals. Recommendations adopted as PIC v1.4: wall-ion
  recycling in the inventory, fail-closed peak-node Debye gate + grid-
  heating triad, CUDA-graph step capture, Bohm-scattering / SEE sensitivity
  hooks, artificial relaxation default off, >= 50 us development case and a
  breathing-mode contingency before freezing the plateau criterion. Agent
  launched (code now; steady-state v3 after W x0.7 frees the GPU).
- Literature review 3/3 merged (`b6bb6215`, 17:54): surrogates / MDO /
  validation; 157 verified refs (345 total). Recommendations: screening v2
  replicating only non-saturated cells to n=512 with scrambled-Sobol
  launches + 10% 2N control; surrogate v3 binomial-likelihood GP with gates
  <= 1.5x binomial floor and split-half reliability ceiling (2x-baseline
  gate unmeetable by construction); MDO v3 >= 10 seeds, HV+IGD+/eps with
  bootstrap CIs, LVGP kernel, Ide-Schobel robust sets, never pool closures;
  validation v0 code-to-code vs Brandt 2016, v1 ion-energy peak (~Ua-15 V)
  and 20 deg RPA angle vs Koch 2011 (needs plume), v2 thrust-voltage vs
  Keller 2015 muHEMPT; preregistration hygiene (registry receipt, RO-Crate,
  gate genealogy). No TU Berlin HEMP dataset exists. Launched: synthesis +
  revised roadmap + canvas; plasma network v2 (sheath closure, new module).
- MDO v2 admitted to paper (`a3793c27`, 18:05, main synced):
  GATE-MDO-L0-V2, 611 macros, Tables 19-22, Section 12 (pp. 27-34), claims
  CLM-053..060; CLM-035/052 amended (geometry link now points at Section
  12). 165/165 paper tests; 41-page PDF reproducible (86796210).
  Corrections: infeasible 88 BO + 3 NSGA-III; seed-101 stall partly an
  initial-design effect (design 49 absent); design 94 has the largest own
  dense HV, 49 the most front members.
- Literature synthesis merged (`8674cc5a`, 18:37, main synced):
  `modern/docs/LITERATURE_SYNTHESIS.md` - 60 recommendations: 53 adopt /
  6 defer / 1 reject (screening v2 option B). Ordered roadmap with
  inputs/outputs/gates/cost/stop per step; 14 paper amendments keyed to
  triggers (topo v3, relabel, network v2, PIC, NASA-STD-7009A scoring).
  Naming fix: CL-3 overloaded -> CL-3-potentials vs CL-3-sheath-limited /
  CL-4-hybrid-area. Net utilisation ~17-18% (gross 46%). Canvas: new
  Literature roadmap tab, 35 ladder rows.
- PIC v1.4 merged (`112bb250`, 19:03, main synced): wall-ion recycling
  (5th ledger; expected fixed point n_g* ~4.1e19, gross 63% / NET ~25%
  utilisation, ~3.3 M particles), peak-node Debye gate (dev 4.5 cells/
  lambda_D, campaign 2.0) + grid-heating triad, whole-step CUDA graph
  bitwise-identical to direct launches (5.2x / 2.7x at small sizes under
  contention; 1-2 M pending), Bohm + SEE hooks (BN parameters provisional).
  pic2d suite 111 green. steady_state_v3 experiment ready; launch deferred
  until W x0.7 finishes (~20:35) - background watcher shell set.
- Plasma v2 merged (`fb5408bf`, 19:06, main synced; check_paper passes,
  v1 files untouched): new `cft_revival.plasma_v2` (31 unknowns, 38 rows,
  53 tests). Findings: corrected R27 is an identity (4.8e-15); sheath rows
  do NOT restore rank for potentials (density cancels) - phi_1 solved via
  anode sheath, interior steps DECLARED; no-emission floating sheath
  (c_s = 5.27) is provably inconsistent with the cascade (0/96 closures;
  closed-form criterion c_s <= (1+I_k/Je_k)/CT); space-charge-limited sheath
  (Hobbs-Wesson, 1.02) closes 73/80 and reproduces Kornfeld DM9.2 Table 3.1
  to rounding with phi_1 = 14.07 V vs printed 14.1. Puca 2024 Table 1 not
  admissible as printed. PIC context: STAIRCASE potential (steps 94/55/125
  V), not flat interior; near-wall T_e ~6 eV vs axis 37-44 eV. CL-4
  leak-width saturates at 300 V (density-dominated).
- User (19:27): snapshot ionisation-rate render looks "sketchy" (shot noise
  on log scale over a 10 ns window) and asked to extend the domain beyond
  the exit to see the plume and get thrust. Launched PIC v2.0: L-shaped
  domain with plume box (~4-5 r_w x 1-1.5 L), thruster body as internal
  boundary, far-field 0 V, off-axis cathode emission region, two-zone
  neutrals, thrust from momentum flux + electrostatic-force closure,
  divergence / IEDF / self-consistent exit potential; dashboard fixes
  (sample-count mask, binned view, linear toggle). Plume dev run after
  W x0.7; channel-only steady-state v3 deferred.
- Cusp topology search v3 -> v3.1 ACCEPTED (19:57; `69159934`/`8cbcdbe6`
  v3 rejected on a held-out extraction defect, audited `9fa6359a`;
  `1600cfd3`/`cec47f12` v3.1 accepted; dashboard `9abbd537`; main synced).
  281 designs, 728 s, 281/281 stable, held-out 56/56 + 96/96. UNDER THE
  LITERATURE DEFINITION THE L1a FIELDS ARE MULTI-CELL: sweep-v2 designs
  carry N-1 wall cusps for N stages (2/3/4 cusps for 30/47/19 designs ->
  19/96 exactly four wall cusps, 47/96 four cells), cusps at the
  inter-magnet gaps (median 0.14 mm); four-cell v2 candidates all have ONE
  cusp (weak even stages never flip the axis field -> v2's 0/128 was by
  construction of definition AND source policy); P2 cusps 6.028/12.000/
  17.972 mm match the PIC planes to 31 um. Every vector null is on the axis
  (Section 8 nulls true under their frozen definition). Launched: paper
  admission + trigger-A amendments; screening v2 on catalogue cells; canvas.
- TWT/PPM review merged (`beb4772c`, 20:08; 51 verified refs + read-only
  PPM fit script). Findings: every recorded field (4 L1a reps + iron P2) is
  a single-harmonic PPM field at the wall; fitted pitch = geometric pitch
  (0.6-1.9%, 11-15% end effect for 3-stage); nulls at gap planes; the wall
  cusp |B| is only I_1(pi r_w/L) = 0.45-0.61 of the axis peak -> in OUR
  catalogue the cusp is the |B| MINIMUM of its cell and Koch's HEMP design
  ratio rho (1.5-10.6) is UNREACHABLE: the original study's parameterisation
  never varied r_w/L into the HEMP regime. Reflections are mirror
  reflections toward the magnet centres (0-1/128 for launches within 0.17
  pitch of a magnet centre vs 32-88/128 farther away) -> v4's zero
  reflections = launch-position effect (launched 0.5 mm from magnet
  centres). All electrons non-adiabatic at wall cusps (mu medians ordered by
  epsilon). Iron matters only near the pole bore -> L1a adequate for topology.
  Launched: L1a sweep v3 extending r_w/L into the HEMP-like regime (hypothesis
  I_1(x_w) >= 1.5). Paper amendments (v4 reflection re-scoping; rho
  unreachable) queued behind the topology admission.
- L1a sweep v3 ACCEPTED (`1923ef76`/`2cfe8223`, dashboard `44d0c63c`,
  21:32; main synced): 128 Sobol designs on a wider box (r_w/L 0.215-1.235)
  + 96 v2 held-out; 11/11 gates; 29 min. Koch rho >= 1.5 at every cusp for
  15/128 (11.7%), first at x_w ~2.25-2.34 (r_w/L ~0.72-0.75, ~20% above the
  I_1 estimate 0.617 because the finite stack's end field raises the
  adjacent axis peaks); I_1(x_w) is an UPPER ENVELOPE (slope 0.69, R^2 0.39,
  H1 not as predicted). v2 region: 0/102 HEMP-like, max rho 0.993 - the
  original study's parameterisation was STRUCTURALLY unable to reach the
  HEMP design ratio. 2 five-stage four-cusp HEMP-like designs (005, 106).
  rho_wall < 1 for all 642 cusps (cusp never the wall |B| maximum). L1b/P2
  material-aware confirmation of the 15 designs queued, not run.
- Topology v3.1 admitted to paper (`13d8ac6a`, 21:41; main synced):
  GATE-CUSP-TOPOLOGY-V3-1 (outcome accepted-topology-screening), v3
  rejection bound as lineage with its audit REPRODUCED from sealed data;
  Section 13 (pp 35-40), CLM-061..068; CLM-028/044 amended per trigger A.
  197/197 paper tests; 49-page PDF (34e11c8e). Correction: the sealed v1
  dataset has 20 off-axis bilinear X roots in 14 wide-bore cases (0.16-0.54
  r_w), none at the wall - so "every null on the axis" is wrong; the
  "by construction" argument rests on the definition + v2 strength ratios.
  Launched: sweep v3 admission + TWT re-scoping of v4 reflections; canvas.
- User (22:17) wants a VIDEO of the whole PIC time series. No run so far
  saved map frames (checkpoints overwritten). Interrupted the PIC agent
  (additive): frame recorder (interval-averaged maps every ~20k steps,
  hash-bound npz) + renderer (MP4/WebM via imageio-ffmpeg in .venv-sota or
  GIF fallback + offline HTML scrubber player with fixed colour scale) must
  land BEFORE the plume run launches so the plume run produces the first
  video.
- PIC v2.0 + frames merged (`ff8e0baa..1fb8561d`, 22:54; main synced):
  plume domain (241x721 nodes, 12x12 mm plume box), internal channel walls,
  off-axis cathode (continuity rule), two-zone neutrals, Maxwell-stress +
  momentum-flux thrust with closure, j_i(theta)/IEDF, plume-boundary gate;
  frame recorder (exact interval averages via cumulative-sum differences,
  every 20k steps, hash-bound npz, resume-safe) + renderer (MP4 via
  imageio-ffmpeg in .venv-sota / ffmpeg pipe / GIF fallback; offline HTML
  scrubber player). 146 pic2d tests. W x0.7 vs base: currents +3.6-5.7%,
  peak n_e -12% -> 3.5-5.7% particle-resolution band. Plume run launched
  22:46 (PID 28860, 4.4 ms/step, frames ON, ETA ~02:50, 100-140 frames);
  early status: cathode 3.0 mA, I_d 0.13 mA, S 3e8/s - ignition not yet;
  45-min ignition check + completion watcher armed.
- Screening v2 ACCEPTED (`cef1ee59`/`26029b72`, merge `066234d9`, 23:08;
  main synced): 97 designs, 377 catalogue cells, 104,832 orbits, 16,549
  validators / 0 failures, 86 min. Allocation: 117 cells topped up, 260
  saturated. FINDING: all 181 interior cells at P(wall) = 1.000 - mid-cell
  launches at 0.65-0.825 r_w sit on wall-connected field lines (as the PPM
  review predicted: field maxima -> no mirroring, direct wall access);
  anode-side median 0.98; exit-side median 0.50 (divergent exits) with
  direction structure; reflections 11.2%, only in exit/anode partials;
  2N bias -0.9e-4. 294/377 cells floor <= 0.02. Disclosures: Windows
  EMFILE (16,957 files > 8192 CRT cap) killed manifest publication after
  the terminal record; tested fail-closed recovery + 4096 pin cap added to
  experiment_runtime; POSTHOC_FINALIZATION.md. Two orbit_mc v1.7 constraints
  forced 128-launch cases and a 1/8 control (Wilson lower>0 round-off for
  734 of the first 4000 n).
  DECISION: the collisionless geometry->loss chain is now exhausted as a
  closure source (interior access ~1 => CL-1 survival ~0 for every design);
  surrogate v3 / MDO v3 on these labels would be degenerate -> deferred.
  Replace with a PIC design mini-sweep (3-5 designs across rho incl. the
  HEMP-like ones, ~20 GPU-h) to calibrate a design-dependent sheath-limited
  closure. Paper admission of screening v2 queued behind the sweep-v3 one.
- Plume run attempt 3 NOT igniting at 1.08 us (N_e 241k -> 174k, S 3e15 ->
  2e14/s, I_d 0.03-0.3 mA, cathode 3 mA steady) -> stopped per contingency
  (36 frames kept for the first real video). PIC agent resumed: diagnose
  (trace field lines from the cathode: do they enter the bore or connect
  to body/boundary/cone?), fix (cathode on channel-connected fringe flux
  tube; declared ignition sequence with exit-plane injection then ramp;
  fail-closed ignition gate x3 S within 0.75 us), relaunch attempt 4.
- Sweep v3 admitted + reflections re-scoped (`88386161`, `ba852122`,
  23:45; main synced): GATE-L1A-SWEEP-V3, Section 14 (pp 41-46), CLM-
  069..076 (H1/H2 recorded NOT held; "the design box of the accepted sweep
  could not contain a HEMP-like cusp"); CLM-016/017/044/052 re-scoped: v4's
  zero reflections = launch-position result (planes 0.083 pitch from the
  magnet centres); Limitations: Mendel alpha 9.9-1190, non-adiabatic.
  228/228 paper tests; 56-page PDF (b2440f6e). Launched: screening v2 paper
  admission; canvas refresh. Memory files are large (scratchpad 119 KB,
  devlog 154 KB) - archive-first rollover due at a quiet moment.
- Plume attempts 3-6 (01:58; main synced to `4dc40390`): attempt 3
  finalized (38 frames, FIRST REAL VIDEO rendered). Diagnosis via new
  `pic2d/fieldlines.py`: cathode annulus on pole-face->far-field lines
  (0/24 connected); channel exit flux tube closes on the front face within
  1.5 mm of the exit; 2 eV electrons cannot cross the 100 V radial drop.
  Fix `eb8585c3`: cathode on the channel-connected tube (24/24), launch-time
  connectivity gate, two-stage ignition gate calibrated on v1.3-2 (an
  ignited run) - "x3 in 0.75 us" would have rejected it. Attempt 4 ignited
  then n_g crashed; attempt 5 same. ROOT CAUSE `4dc40390`: the CUDA-graph
  step baked the MCC neutral density in as a kernel scalar at capture ->
  stale n_g. Fixed (device-resident) + regression test. v1.3 records
  unaffected (pre-graph). Attempt 6 (PID 53824, 01:30, frames ON) igniting
  cleanly: 0.48 us, S 4.2e16/s rising, I_d 3.2 mA, n_g 3.49e19 relaxing,
  phi_exit ~0 V (cathode pins exit potential). Budget ends ~05:30; watcher
  armed. Canvas refresh launched.
- Follow-up notes / risks:
  - When attempt 6 ends: render video, plume dashboard, compare exit
    potential structure vs v1.x (Dirichlet exit forced the drop inside the
    channel), thrust closure, divergence, IEDF; then decide the >= 50 us case.
  - If the plume run does not ignite by ~1 us (S flat, N_e decaying), stop
    and diagnose the cathode coupling (electrons must penetrate the exit
    field from an off-axis plume position) before spending the 4 h budget.
  - Screening v2 launches at catalogue cell CENTRES may repeat the
    launch-position bias (few reflections); if so, v2.1 must stratify
    launch z within each cell. Check when v2 reports.
  - Screening v2 allocation rule (frozen): 128/cell, top-up to 512 where
    Wilson width > 0.10; scrambled Sobol; 10% 2N control.
  - Order after topo v3: screening v2 -> surrogate v3 -> MDO v3; PIC v1.4
    -> steady-state v3 -> >= 50 us case -> breathing decision -> campaign.
  - Paper Section 8 nulls stay true under their definition, but the
    Discussion must say the definition was non-standard once v3 lands.
  - Plasma network v2 (sheath rows R28-R31, CL-3 potentials) is the next
    model change - after the literature synthesis.
  - When W x0.7 ends (~20:35 AEST): common-window comparison, convergence
    statement (seed + particle-weight), dashboard, then re-audit.
  - MDO v2 needs a posthoc audit before its "numerically accepted" rung is
    uncaveated; screening v2 (4x launches) is the recorded route to a
    surrogate v3; PIC campaign v1 proposal awaits the convergence pair.
  - Fix orbit_wall_loss_geometry_screening_v1 README overstatement
    (refined re-solve for representatives only) - docs, not evidence.
  - When seed-b ends: resume pic2d agent to launch w-0.7, then convergence
    statement + dashboard refresh; ROADMAP_AUDIT.md is dated and still
    says steady-state v2 NOT DONE (re-audit at the end).
  - Paper Discussion must scope the "mirror picture unsupported" claim to
    v4's field; the screening shows reflections elsewhere.
  - Merging feat/sota-foundation -> main is the "approved" stage; needs the
    user's explicit go (human gate), not an agent decision.
  - `modern/experiments/cft_topology_characterization_v1/tests/...
    test_accepted_dependencies_match_coupling_v3_commit` fails on base and
    is not collected by the top-level suite (experiment-local tests dir).
  - When the steady-state v2 run ends: finalize, commit small results,
    regenerate pic2d dashboard, canvas refresh, and if plateau reached
    consider the preregistered PIC campaign design.
  - MDO cannot use the mirror-formula cusp probabilities (falsified by v4);
    cusp/wall-loss probabilities are uncertain inputs with declared ranges.
  - With static neutrals the model has only avalanche or no-ignition; a
    physical steady state needs neutral depletion (v1.3).
  - PIC steady-state run will occupy the RTX 5090 for hours; check
    `pic2d_cft_steady_state_v1/results/status.jsonl` before launching other
    GPU work.
  - four_cell_topology_search_v2 `validate_results` refuses the bundle on LF
    checkouts (code, not evidence) - disclosed in its audit; fix if enforced.
  - PIC v1 over-densifies because static neutrals at 5e20 m^-3 give no
    saturation channel before ions reach boundaries (~1 us); operating
    point must be budgeted from a 0-D equilibrium estimate.
  - Next paper admissions: topology null result + L1a sweep v2 (so the
    Discussion can cite them as evidence rather than open question).
  - The four-cell wall-cusp topology targeted by the original design
    parameterisation is undemonstrated in the explored space; the paper must
    say so rather than assume it.
  - PIC-MCC v1 is development/screening, not preregistered; claim boundary
    must be explicit in every artifact.
  - mu-variation (median 0.12, max 0.69) is expected non-adiabatic cusp
    physics; it is a diagnostic and must never be a per-orbit gate.
  - v4 phase 2 must rebase onto >= 3ab50ef5 (includes fab0eccc), re-smudge
    its worktree, adopt the 1.6.0 witness contract, then rerun the shakedown
    before `prepare` binds the orbit_mc source hash.
  - v4 phase 2 (rebase onto v1.6, prepare, one detached execution) waits on
    v1.6 landing and phase 1 shakedown passing.
  - `l1a_plasma_coupling` tests fail (3F/3E/2P): its adapter predates the
    axisymmetric serialization v1.2 migration (`dbcab646`). Needs an adapter
    update or explicit retirement.
  - `l1a_geometry_sweep/results/` and `l1a_plasma_coupling/results/` are
    gitignored but referenced by committed tests; fresh clones will fail
    those tests unless results are force-added.
  - Four same-basename `test_dashboard.py` modules without `__init__.py`
    clash in a single full-suite pytest run.

## 2026-09-02 05:00 AEST

- Task summary:
  - Diagnosed and resumed isolated ML provisioning in `.venv-sota`.
- Changes:
  - Preserved the healthy Python 3.12 venv and upgraded only its pip from 25.0.1 to 26.2.1.
  - Stopped three stale venv launchers; their three child interpreters ended with them.
  - Removed 76 stale pip temporary entries while retaining the pip download cache.
- Validation:
  - `git check-ignore -v .venv-sota/provision-report.txt` matched `/.venv-sota/`.
  - Venv reported isolated prefixes and initially contained only pip.
  - RTX 5090 reported driver 595.97, 32,607 MiB, and compute capability 12.0.
  - Official PyTorch and PyPI package indexes returned HTTP 200; C: had 189.99 GiB free.
- Follow-up notes / risks:
  - Installed PyTorch 2.13.0+cu130, BoTorch 0.18.1, GPyTorch 1.15.2, and pymoo 0.6.2.
  - Verified real synchronized CUDA float64 execution on RTX 5090 sm_120, a CUDA float64 GP posterior, constrained mixed-direction qLogNEHVI/qLogNParEGO optimization, and deterministic NSGA-III/MOEA-D smokes.
  - Global package snapshots remained exactly equal; `pip check` passed.
  - Final audit stopped 12 late stale `torch==2.9.1+cu128` launcher/child
    processes and removed their 1.01 GiB partial download tree, then reran
    the complete GPU/optimizer verification successfully.
  - Recorded commands, inventory, evidence, footprint, and the optional missing-`cl` fallback warning in `.venv-sota/provision-report.txt`.
## 2026-09-03 21:40 AEST - L1a geometry sweep v3: HEMP-like wall-radius-to-pitch regime (ACCEPTED)

- Worktree `uni-project-sweep-v3` on `exp/l1a-geometry-sweep-v3` from `beb4772c`; run
  worktree `uni-project-sweep-v3-run` (detached at the preregistration). CPU only, 6
  workers, GPU untouched.
- Changes: new `modern/experiments/l1a_geometry_sweep_v3/` (pure-Python scrambled Sobol
  with Joe-Kuo direction numbers; v3 builder = sweep-v2 rules on a wider box with a
  2^-40 m length quantum; Koch rho in four readings + I_1(x_w) prediction + wall
  harmonics + profiles; v3.1 topology definition imported; v3 catalogue schema 1.1.0;
  one-shot lifecycle runner), tests (31), dashboard generator/template/html + test (6).
- Commits: code+shakedown `b04d5935`; `preregister L1a geometry sweep v3` `1923ef76`;
  `record L1a geometry sweep v3 result` `2cfe8223` (980 files, 49 MB, terminal
  `accepted_result`, status `accepted_l1a_sweep_v3`, all 11 binding gates true);
  dashboard `44d0c63c`. `feat/sota-foundation` fast-forwarded beb4772c -> 44d0c63c and
  pushed.
- Validation: corner solves for the v2 gates (boundary <= 0.0212); shakedown 9 real
  designs passed first run (86 s, replay bit-identical); execution 224/224 in 29 min
  (design stage 1576 s + assessment 101 s); `validate` accepted in the run worktree;
  37 tests green on the branch; headless Edge 1440/390 px, 0 JS errors.
- Headline (reported, not gated): 15/128 Sobol designs HEMP-like (all cusps rho >= 1.5);
  I_1 predicted 51 (accuracy 0.72; 28 of 36 misses fail only at end cusps); slope of rho
  on I_1 0.689, R^2 0.39, 70 % within +-25 % (H1 not as predicted); realised threshold
  x* 2.34 (r_w/L 0.745) vs 1.94 (0.617); 2 five-stage four-cusp HEMP-like designs (005,
  106); sweep-v2 region 0/102 HEMP-like, max rho 0.993; rho_wall < 1 for all 642 cusps;
  held-out 96/96; all 224 stable, rho resolution sensitivity <= 8 %.
- Follow-ups / risks: L1b/P2 confirmation of rho for r_w/L > 0.5 is queued (15 HEMP-like
  designs are the list); the paper's design-space paragraph should cite this campaign;
  screening v2 can stratify launches by regime through the v3 catalogue; memory files
  need an archive-first rollover.

## 2026-09-03 21:55 AEST - PIC-2D model v2.0: plume domain, cathode region, thrust; dashboards

- Worktree `uni-project-pic2d` on `feat/pic-2d-axisymmetric` (rebased onto
  origin/feat/sota-foundation >= fb5408bf). Commits: W x0.7 record `542496fb`;
  `feat(pic2d): v2.0 plume domain, cathode emission region, thrust diagnostics`
  `f7169279` (21 files, +2353); `feat(pic2d): dashboards - sampling masks,
  binned/linear views, plume panels` `f3732d9a`. Spec `pic2d-model-v2.0.json`;
  campaign proposal Revision 3; foundation spec lists `plume_block_v2_0`.
- W x0.7 (PID 9856) finished and was finalized/compared over the common window vs
  base: I_d +4.7 %, S -4.5 % (seed-b: +0.4 % / +1.1 %) -> the particle-resolution
  band (~5 %) dominates the statistical one; recorded in the proposal and the
  steady-state dashboard.
- v2.0 choices (all cited from `pic-mcc-blockers.md` in the spec): L-shaped plasma
  region = channel + plume box r <= 12 mm (= 4 r_exit = 6 r_bore, the return-yoke
  envelope), 24 <= z <= 36 mm (L_plume = 0.5 L_channel, BOUNDED by the P2 FEM domain
  z <= 36.25 mm; the requested 1-1.5 L_channel needs a new FEM solve - declared
  deviation; Brandt 2016's 20 x 5 mm box was "still too small", hence the pile-up
  gate). Uniform 50 um (no graded solver): 240 x 720 = 172,800 cells, 77,940 plasma
  cells, 78,228 unknowns (v1.x: 28,800). Channel walls/cone internal masked
  dielectric with surface charge; anode Dirichlet 300 V; front face r in (3, 4.4] mm
  dielectric (BN flange + gap), r > 4.4 mm GROUNDED conductor (pole faces on the
  cathode/chamber reference: Kornfeld 2007, Koch 2011); far field Dirichlet 0 V on
  r = 12 mm and z = 36 mm; crossings counted as beam by species with p_z, angle,
  energy. Cathode: annulus r 4.5-6 mm, z 26-28 mm, 2 eV Maxwellian, current-
  continuity rule targeting I_d = I_e,anode - I_i,anode (floor 3 mA, ceiling 15 mA,
  relaxation 4 intervals); legacy exit-plane injection kept as a flag. Neutrals:
  two-zone - channel = v1.4 inventory with recycling; plume = free-molecular cosine
  point-source shape f = 2 A_exit cos(theta)/(3 pi^2 rho^2) capped at 0.5, used only
  as the null-collision density factor; ion-neutral collisions OFF (Miller 2002 CEX
  table not added - not cheap alongside the ledgers). B field: direct FEM sampling
  at the PIC nodes (the bicubic psi interpolation mis-samples material interfaces in
  the plume). Thrust: (a) far-field momentum flux per species + cold-gas effusion;
  (b) momentum balance = -F_on-thruster from the Maxwell stress on solid boundaries
  + absorbed particle momentum; closure % reported; j_i(theta) 90 bins, IEDF 256
  bins to 450 eV, 95 % half-angle, exit-plane axis potential, 90/10 % acceleration
  region, Isp, anode efficiency - all "development numbers". Gates: peak-node Debye
  (4.5 cells/lambda_D) + triad over the whole domain; plume-boundary charge pile-up
  gate (net charge at far-field nodes / peak n_e <= 25 % after 2.4 us).
- Cost: 4.2 ms/step measured at 0.55 M particles on the RTX 5090 (CUDA-graph step;
  the 482 sequential row-block matvecs of the direct solve set a ~3 ms floor);
  projected 5-7 ms/step at 3-5 M -> a 4 h budget reaches ~1.0-1.4 transit times:
  the plume run is EXPECTED to stop on the wall budget, resumable from checkpoint.
- Tests: v2.0 suite 19 (L-shaped masks, Poisson vs manufactured solution on the
  masked domain, cosine neutral shape, cathode continuity, momentum-ledger closure
  on a synthetic case, plume gate, tiny GPU parity); runner plume-protocol test;
  plume dashboard 5 (tiny CPU run under tmp_path); pic2d suite 138 passed.
- Dashboards: shared map view (event-count mask N = 20 default with the N^-1/2
  error in the caption; 2x2/3x3 binning; linear/log toggle; window duration in
  caption; log colourbar labels as values) applied to snapshot + steady-state;
  new `generate_pic2d_cft_plume.py` (full-domain maps with the body drawn, cold
  test-ion trajectories from the mean field labelled as post-processing, thrust +
  closure series, currents, axis potential, j_i(theta), IEDF, literature context
  labelled model-to-model). Headless Chrome screenshots under
  %TEMP%\pic2d_dashboard_shots (ss-log-nomask / mask20 / mask20-bin3 / linear,
  snap-log-mask20-bin2, plume-map-*, plume-performance-thrust-closure, plume-plots).
- Deferred: channel-only steady-state v3 (v1.4 model) runs AFTER the plume
  development run; L_plume = 1-1.5 L_channel needs a P2 FEM re-solve; Miller 2002
  CEX table as a sensitivity flag; clean 1 M / 2 M ms/step measurement.

## 2026-09-03 22:55 AEST - PIC-2D frame recorder + time-series video renderer; plume run launch 3

- Additive request: a VIDEO of the whole run (maps evolving), not only the final
  window-averaged maps. Launch 2 of the plume development run (PID 38948, still in
  the host factorisation, no stepping) was stopped and its results archived under
  %TEMP%; the protocol hash changes with the new `frame_recorder` block anyway.
- Frame recorder (`cft_revival/pic2d/frames.py`, `numerics.frame_recorder`,
  default OFF): every 20000 steps (30 ns) the EXACT interval averages of the
  full-domain maps (n_e, n_i, phi, T_e from the drift-corrected moments, ionisation
  rate, electron sample counts), the wall/exit/far-field flux profiles, j_i(theta),
  the IEDF, the instantaneous surface charge and the scalar series record at the
  frame end. Method: differences of the window-accumulator SUMS run through the
  same `to_arrays` as the window average (both backends expose `diagnostic_sums`;
  nothing added to the step kernels; one device->host copy per frame). Alignment
  contract fail-closed at start: cadence = k x sync, divides checkpoint and window.
  Storage: one compressed `frames/frame-NNNNNN.npz` per frame, atomic replace,
  written BEFORE the checkpoint that follows; resume/finalize drop frames past the
  checkpoint step; manifest (count, bytes, range, SHA-256 over per-file digests,
  config) hash-bound in `summary.json -> artifacts.frames`. Size on 241 x 721:
  4.9 MB/frame uncompressed (float32 x 7 arrays) -> 0.5-0.7 GB in the 4 h budget,
  1.26 GB at 7.7 us (257 frames): no spatial downsampling needed.
- Renderer `visualization/render_pic2d_video.py`: MP4 per map (H.264, CRF 18) via
  `imageio_ffmpeg` (installed into .venv-sota ONLY: imageio 2.37.4 + imageio-ffmpeg
  0.6.0 with bundled ffmpeg 7.1) or the `ffmpeg` executable on PATH (system Python:
  Gyan ffmpeg 8.1.2 via winget), GIF via Pillow otherwise; fixed colour scales across
  frames (log floor max/1e4 for densities and ionisation; full range phi; T_e to the
  99.5th percentile), sample mask < 20 grey, body dark, mask outline + cusp planes,
  synchronised I_d / I_beam / N_e / n_g / thrust strip with cursor. Offline player
  `pic2d-<run>-timeseries.html`: 2x block-mean palette PNGs embedded, scrubber /
  play / speed / map selector / arrows, hash-bound payload with the claim boundary.
  Sample renders from a 2000-step tiny CPU run under
  %TEMP%\pic2d_scratch\plume_tiny_frames\video{,_gif,_imageio} (all 3 backends) and
  screenshots %TEMP%\pic2d_dashboard_shots\player-*.png.
- Tests: 7 new (frames 3: contract + size envelope, exact interval algebra on a
  synthetic accumulator, runner cadence/shapes/manifest/stray-frame removal/
  contiguity/byte-identical fresh run/OFF default; renderer 4: palette mapping, fixed
  scales, downsample/outline, writers, deterministic render_run + CLI). pic2d suite
  146 passed incl. GPU parity (Warp `_host_accumulator` refactor).
- W x 0.7 (finished earlier: plateau at 7.32 us, 3.05 tau, 4.88 M steps) was
  finalised/compared and committed in 9c7d944a: vs base over the plateau windows
  I_d 3.640 vs 3.444 mA (+5.7 %), I_beam 2.373 vs 2.291 mA (+3.6 %), S 3.75e16 vs
  3.93e16 /s (-4.8 %), n_g 3.09e19 vs 2.97e19 (+3.9 %), mean n_e 2.03e17 vs 2.13e17
  (-4.8 %), peak n_e 1.44e18 vs 1.64e18 (-12 %), phi_max 331.5 vs 336.6 V, T_e 8.21
  vs 8.17 eV; three-case spread 3.5-5.7 % on the currents (12.7 % on peak n_e):
  a particle-resolution band, not a plateau-noise band.
- Plume development run launch 3 WITH frames: PID 28860, 22:46:51 AEST, results at
  modern/experiments/pic2d_cft_plume_v1/results (run.log, status.jsonl, frames/);
  4 h budget -> expected stop on wall clock at ~1.0-1.4 transit times (resumable).
- Commits: 1fb8561d feat(pic2d): frame recorder and time-series video renderer
  (after ff8e0baa ceiling fix, 6bd5e5b0 dashboards, e3e9167d v2.0, 9c7d944a W x 0.7);
  pushed; feat/sota-foundation fast-forwarded to 1fb8561d (origin + main checkout).
- Deferred: render the plume video and link it in the plume dashboard when the run
  ends; channel-only steady-state v3 after the plume run.

## 2026-09-04 00:20 AEST - PIC-2D plume attempt 3 no ignition: field-line diagnosis, cathode on the channel flux tube, ignition gate; attempt 4

- Attempt 3 (PID 28860) did not ignite: at 1.08 us N_e 241k -> 174k, S 3.0e15 ->
  2.0e14 /s, I_d 0.03-0.32 mA, cathode 3.0 mA steady, 36 frames. Stopped by hand right
  after the step-760000 checkpoint (23:46 AEST; 38 frames kept, 0 removed on finalize),
  finalized from the checkpoint (stop_reason finalized_from_checkpoint, 1.14 us), the
  directory renamed results-attempt3-no-ignition/ (the protocol hash changes with the
  fix), small artifacts + frames manifest committed (2b3372a0 on feat/sota-foundation;
  978000c4 on the pic branch). Video rendered on the real run with the new renderer:
  5 MP4s (n_e, n_i, phi, T_e, ionisation; 0.5-2.0 MB each) + the 1.6 MB HTML player
  under results-attempt3-no-ignition/video/ (untracked, size).
- Diagnosis (new `cft_revival/pic2d/fieldlines.py`: RK2 field-line tracer on the
  bilinear node field with event classification channel / anode / wall / body /
  far_field / axis / null / length; annulus connectivity; channel-connected flux tube
  traced OUTWARD from the exit aperture). On the run's own P2 node field the channel's
  exit flux tube balloons to r = 4.3 mm at the front face and closes on the front face
  / exit pole within 1.5 mm of the exit plane (r band <= 2.9 mm at z = 24.9 mm, <= 1.6
  mm at 25.3 mm; B_z sign change on axis at z = 25.45 mm, |B| 0.6 mT there): NO plume
  volume beyond z ~ 25.7 mm is magnetically connected to the bore. The attempt-3
  annulus (r 4.5-6 mm, z 26-28 mm; |B| 72-79 mT) sat on lines from the pole face at
  r ~ 5.1-5.3 mm to the far field (0 of 24 samples connected). phi along those lines:
  -1.4 ... +0.9 V end to end (the far field and the grounded face are both 0 V) while
  the axis sat at 239 V (exit) / 105 V (z = 27 mm) - a 100 V radial drop the
  magnetised 2 eV electrons (r_L ~ 60 um) cannot cross. Electron budget of the last
  interval: 2.85 of the 3.01 mA emitted left through the far field, 0.48 mA hit the
  body face, 0.06 mA reached the anode; channel electrons (seed) decayed 1.47e10 ->
  0.97e10 while the plume held 0.07e10. Seed was present (N_e(0) = 241k, channel).
  v1.3 comparison: its exit-plane injection put the 3 mA INSIDE the channel on channel
  field lines (S reference 1.7e16 vs 2.9e15 here - the seed alone at the same
  density); attempt 3 = the v1.3 attempt-1 failure class (S ratio 0.59 there, 0.23
  here). Literature: Brandt 2016 fed electrons from the far outer boundary at 1 eV in a
  model WITH Bohm transport; Szabo 2001 / Charoy 2019 inject at a cathode plane tied
  to the current leaving; the review (blocker 4d) says nothing about cathode placement
  vs field topology - the electrostatic collisionless-plume model has no cross-field
  path from an off-axis neutraliser.
- Fix within the declared model (eb8585c3 / c6219bf3): (i) cathode region moved onto
  the channel-connected tube, r 0.5-2.0 mm, z 24.3-25.0 mm (24/24 samples connected,
  one half-line into the bore, the other onto the dielectric flange; |B| 19-64 mT;
  8.3 mm^3), declared as the ARRIVAL of the neutraliser electrons into the channel
  flux tube, not a neutraliser position; fail-closed launch check
  `cathode.require_channel_connected_fraction = 1.0` re-traced on the run's field and
  recorded in summary.json cathode_connectivity. (ii) the exit-plane ignition sequence
  (keeper) is declared in the spec as the contingency, NOT implemented - attempt 4
  tests whether (i) suffices. (iii) seed unchanged. Ignition gate
  `stopping_rule.ignition_gate`: trailing 0.15 us means of S and N_e over their
  0.05-0.2 us reference means, >= 0.8 / 1.1 at 0.75 us and >= 1.2 / 1.4 at 1.5 us,
  else stop_reason no_ignition (<= 40 min burnt). Calibrated on the series: v1.3
  attempt 2 (ignited) 1.07 / 1.29 at 0.75 us and 1.41 / 1.76 at 1.5 us; v1.3 attempt 1
  (failed) 0.59 / 1.03; plume attempt 3 0.23 / 0.83. The suggested "x3 within 0.75 us"
  would have rejected the ignited run (early S e-fold ~2.8 us; the 1.1 us e-fold is
  the later N_e growth), hence the two-stage rule. Frames now also carry the side
  electron current-density profile (attempt 3 lacked it; the series had the totals).
- Tests: 7 new in tests/pic2d/test_pic2d_fieldlines_ignition.py; pic2d suite 153
  passed (4 min, CPU, alongside the GPU run).
- Attempt 4 launched 00:07:22 AEST, PID 53756, stepping from ~00:11:30 at 4.2-4.3
  ms/step (4 h budget -> ~3.2 M steps ~ 4.8 us if it runs out the clock; the gate
  decides at 0.75 us ~ 00:47 and 1.5 us ~ 01:23). 3-minute check (00:14, t = 0.062
  us): N_e 247.8k -> 256.1k rising, S 2.0e16 -> 2.58e16 /s rising (attempt 3: 7e15
  falling), I_d 1.67 -> 2.24 mA (attempt 3: -0.10). NOTE phi_exit (axis, exit plane)
  fell to 3.4 V from 255 V in attempt 3: the cathode on the exit flux tube pins the
  exit potential near the reference - the acceleration region now sits inside the
  channel/cone as in v1.x with its Dirichlet exit plane. To be read against the
  v1.x-vs-plume claim boundary once the window averages exist.
- Git: feat/sota-foundation had moved (ba852122, paper stream); the two commits were
  cherry-picked onto it (2b3372a0, eb8585c3) and pushed (fast-forward, no force);
  origin/feat/sota-foundation merged back into feat/pic-2d-axisymmetric (f5582255).

## 2026-09-04 01:35 AEST - PIC-2D plume attempts 4-5: ignition, neutral crash, ROOT CAUSE = stale MCC density in the CUDA-graph step; attempt 6

- Attempt 4 (PID 53756, cathode on the channel flux tube) IGNITED: S 1.4e16 -> 7.3e16 /s,
  I_d 1.5 -> 7.0 mA (the continuity cathode following it 3.0 -> 7.05 mA), T_e 6.7 -> 12.9
  eV, N_e 248k -> 512k in 0.39 us; 3-min check S 2.0 -> 2.6e16 rising, 20-min check S at
  1.0e17 = 1.26 x the feed, n_g 5.5e19 -> 7.8e18 -> 4.2e18. Then S collapsed x17 in one
  30 ns frame, the 7 mA electron flow charged the cone to -70 V (z = 21 mm) and the
  discharge never recovered although n_g refilled to 5.9e19 within 0.1 us (S 3e15 at
  T_e 11 eV, N_e 484k). Ignition gate: S/S_ref 0.11 (min 0.8), N_e/N_ref 1.75 -> stopped
  no_ignition at step 520000 (0.78 us, 40 min, 26 frames) - the gate worked as declared.
  Record: results-attempt4-neutral-crash/ (committed small artifacts + frames manifest;
  video rendered into its video/).
- First reading (WRONG as the root cause, kept as a guard): the artificial 30 ns
  relaxation toward the NEGATIVE fixed point (Q + R - S)/c when S > Q + R emptied the
  channel in one interval. Fix committed (ad5be433 / 0f583df9): suspend the relaxation
  when n* < 0 (conservative balance only). Attempt 5 (PID 40140) reproduced the crash
  exactly (n_g 1.3e19 at util 1.05, 7.8e18 at 0.375 us, S then stuck at 1.4e16 with n_g
  back at 5.5e19) -> stopped by hand at 0.56 us; results-attempt5-stale-scale/ untracked.
- ROOT CAUSE from the attempt-4 frames: S/(n_e n_g) in the channel fell x100 between
  frames 12 and 13 with the density-weighted T_e distribution unchanged (12.9 -> 12.8 eV,
  same histogram) - the MCC was not seeing the inventory's n_g. warp_backend passed
  mcc.neutral_density_per_m3 as a kernel SCALAR to mcc_kernel; the v1.4 CUDA-graph step
  (default on with device-direct Poisson; the options line prints step_graph_active
  BEFORE the first capture, i.e. always false - misleading) bakes scalars at capture time,
  and recaptures only on a particle-array reallocation. So the MCC ionised at the n_g of
  the last reallocation: during ignition S grew unchecked by the falling inventory (hence
  S > Q, the negative fixed point and the crash), and after the crash it kept the trough
  density (~7e17) forever. Fix (3fe66bde / 4dc40390): the density is a one-element device
  array read by the kernel; set_neutral_scale copies into it (the emission rate was
  already graph-safe that way). Regression test: direct vs graph bitwise while n_g falls
  every interval + a zero density set after capture reaches the replayed graph; fails on
  the old backend (graph n_g 5.477e20 vs direct 5.514e20 after 200 steps). pic2d suite
  155 passed. Affected: plume attempts 3-5 only (attempt 3's n_g stayed 5.6-6.3e19; its
  coupling diagnosis stands). Unaffected: all v1.3 records (base, seed-b, W x 0.7 launched
  17:05 AEST 2026-09-03, before the 19:05 graph commit 112bb250), CPU paths.
- Attempt 6 launched 01:30:25 AEST, PID 53824 (cathode on the channel flux tube +
  connectivity gate + ignition gate + relaxation guard + graph-safe MCC density), frames
  ON, 4 h budget. The 4-5 min host factorisation precedes stepping; the ignition gate
  decides at 0.75 us (~02:12) and 1.5 us (~02:50).

## 2026-09-04 02:30 AEST - paper: admit wall-loss geometry screening v2 (catalogue cells)

- Worktree `uni-project-paper-scr2` on `paper/screening-v2-claim` from
  `origin/feat/sota-foundation` (`ba852122`); no work in the main tree, `results/`,
  frozen files, `FYP/` and the GPU untouched.
- Changes (24 files, +53,373): `paper/scripts/generate_wall_loss_geometry_screening_v2_evidence.py`
  (380 `\Wlh` macros; byte-verifies the 16,957-file bundle, replays the two-stage
  allocation rule and the paired N->2N control orbit by orbit from the endpoint tables,
  recomputes every interval/floor/design average/headline and the v1 comparison from the
  bound v1 dataset, verifies `POSTHOC_FINALIZATION.md` hashes/counts against the bundle
  and the 4096 pin cap in `lifecycle.py`, recomputes the 734/1238 Wilson-exactness scan;
  five ArtifactClaim tables), `paper/sections/wall-loss-geometry-screening-v2.tex` +
  standalone, typed manifest `paper-orbit-cell-screening-manifest` (111 source files,
  131 metrics, 6 reference files, 4 disclosure sources), gate
  `GATE-WALL-LOSS-GEOMETRY-SCREENING-V2` (`accepted-screening-dataset` reused, 107
  constraints, `disclosure_revision`), claims CLM-077..085 (CLM-085 = Discussion
  interpretation: collisionless chain exhausted as a closure source, surrogate/MDO
  iteration deferred, kinetic PIC sweep = future work not evidence), contract item,
  schema type, `check_paper.py` (`_check_geometry_screening_v2`, metric/policy maps,
  renderer, required section), manuscript (Section 15 pp 46-53, Section 3.1 seventh gate,
  Section 11 now cites Section 15, Discussion para p57, Limitations, L1 note, data
  availability, Conclusion), tests (+29; three pinned-wording tests updated), README,
  notation, author checklist, supplementary outline S4j, paper devlog + learning ledger.
- Numbers verified from the artifacts: 97 designs / 377 cells / 1,105 cases / 104,832
  orbits; 16,549 validators, 0 failed; 117 topped up (32/83/2), 260 saturated; all 181
  interior cells at 1.000 in 96/96 designs; anode median 0.984 (min 0.307); exit median
  0.500 (0.248-1.000) = a direction split (wall side = last-stage polarity in 82/90);
  10,407 reflections (11.2 %) in 65 exit + 1 anode cells; control 2/11,648 discordant,
  bias -8.6e-5 +- 8.6e-5; v1 vs v2 Spearman +0.15, mean diff +0.038, |diff| 0.113,
  overlap 45 %; P2 row 0.605/1.0/1.0/0.170, 350 reflections in the 28 um cell; floors
  median 0.0055 / max 0.0242, 294/377 ready; 16,957 files vs 8,192 descriptors; 85.9 min.
  Deviations from the brief noted in the paper devlog (4 of 6 straight designs at 1.0;
  direction fractions one-sided, not banded).
- Validation: `check_paper.py` green on the commit (107 s warm); `unittest discover -s
  paper/tests` 257 tests (final run recorded below); `verify_reproducible_build.py` two
  clean builds byte-identical, 65 pages, 734,328 bytes, SHA-256
  `ba7441c972a345e9390fd548ae58cd41fa43c15a984a653d38f9a2dd56f534b6`, no overfull box /
  LaTeX error / undefined reference; pages rendered to `%TEMP%\paper-scr2-pages\`.
- Commit `8e13364c` `paper: admit wall-loss geometry screening v2 to the claim matrix and
  manuscript` on `paper/screening-v2-claim` (rebased onto `4dc40390` after the pic2d
  stream moved upstream; check_paper re-run green); pushed; `feat/sota-foundation`
  fast-forwarded 4dc40390 -> 8e13364c on origin and in the main checkout (ff-only).
  Final suite after the amend: 257 tests OK; final two-clean-build hash unchanged.
- Follow-ups / risks: the scratchpad rollover is overdue (deferred for concurrent
  streams); the checker adds ~23 s to `check_paper.py`; a surrogate v3 / MDO v3 on the v2
  labels would contradict CLM-085 unless the closure changes.
- Attempt 6 checks: 3 min (t = 0.083 us) S 1.84e16 -> 2.84e16 /s rising, N_e 247.7k ->
  260.7k, I_d 1.6-2.5 mA; 20 min (t = 0.43 us, PAST the 0.375 us crash point of attempts
  4-5) S 4.4-4.8e16 /s still rising with no runaway, N_e 407k, N_i 421k, I_d 2.5-3.1 mA
  steady, n_g relaxing smoothly 5.5e19 -> 3.55e19 (util 0.5, as the v1.3 plateau), peak
  n_e 6e17 at 1.5 cells/lambda_D, phi_exit -0.5 V, q_far 0, I_beam still 0 (ions need
  ~3 us to cross). Wall budget ends ~05:30 AEST; resumable.
