"""Lambda Cloud (8x H100) tooling for the PIC-MCC campaign: bootstrap, per-GPU concurrency benchmark, job scheduler.

Nothing in this package changes an experiment protocol or the ``cft_revival`` package code (the PIC
checkpoints are bound to the package code identity, ``cft_revival.pic2d.artifacts.code_identity``).
"""
