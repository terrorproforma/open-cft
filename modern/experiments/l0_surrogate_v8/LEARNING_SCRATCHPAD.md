# L0 surrogate v8 learning scratchpad

## Before execution

- V7's row overcoverage is expected under simultaneous group-max intervals and
  is not itself invalidity; retain the row lower sanity bound and report upper
  values diagnostically.
- Width gates must reject inefficient uncertainty bands without shrinking a
  coverage-valid interval. Fixed engineering-range limits are median 0.25 and
  p90 0.40 normalized full width.
- OOD mean error is addressed prospectively with physics-structure features and
  budgets through 224, selected without V7 or V8 assessment labels.
- The configured learning-scratchpad-loop and devlog-loop skills are not
  available in this agent's skill catalog; their required persistent artifacts
  and phase updates are maintained directly in these two files.
