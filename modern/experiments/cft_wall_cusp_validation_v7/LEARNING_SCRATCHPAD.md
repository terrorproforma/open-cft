# Learning scratchpad

## 2026-09-02 — retained v6 preregistration lessons

- `COMMITTED`: this scratchpad is experiment-local evidence.
- [user] Preserve v1-v5 and exclude `wcval-v5-s05-p0-r0-neg` plus its exact
  coordinate; keep the six-cusp scientific requirement unchanged.
- [self] A value accepted directly by `RunContext.write_json` must never first
  pass through runtime `normalize`; its reserved envelopes are an encoded
  representation, not domain JSON.
- [self] Tuple and dataclass callback values are now converted to lists and
  named dictionaries by `_plain_domain_json`; boundary nulls and ambiguous
  assessments have explicit serializers.
- [self] Test callback shapes, not only public dataclass types: resolved,
  ambiguous zero-cell/orbit, boundary-nonempty, and full rejection summaries
  all use the same `_write_callback_json` path as assessment.
- [tool] Foundation `b46e263` fixes inventory sorting globally. Same-stem
  file/directory names remain avoidable, so production fields use a distinct
  `preflight/production-fields/` directory.
- [tool] Git line-ending conversion would invalidate canonical field bytes;
  result-local `.gitattributes` therefore declares `* -text` and is an approved
  runtime placeholder.
- [self] A numerical rejection must write its outcome immediately before the
  next case. Deferred aggregate promotion work must not erase atomic evidence.
- [user] After preregistration, execute once from clean detached HEAD and commit
  that exact valid terminal bundle without patching or rerunning.

## 2026-09-02 — v7 prospective orbit correction

- [user] Preserve v1-v6 and disclose v6 as development evidence, never as a
  source for post-hoc v7 threshold tuning.
- [user] Six wall cusps remain mandatory; exclude every prior accessed ID and
  full coordinate tuple from the fresh v7 family.
- [self] Orbit `converged` encodes numerical trajectory convergence only.
  μ/rho/LB/curvature failures are physical `NONADIABATIC` evidence.
- [self] Preserve finite instantaneous and gyro-averaged μ diagnostics on
  numerical and physical rejection paths.
- [self] Bilinear ψ creates artificial Bz variation for quadratic radial flux.
  Quadratic-radial/linear-axial ψ interpolation reproduces a uniform field and
  retains the within-cell cylindrical-divergence identity.
- [self] Compare N/2N/4N at shared nodes and exactly equal terminal time.
  Guiding-centre/parallel observables remove irrelevant gyrophase error.
- [self] Cross-map convergence requires three distinct map hashes for each
  seed/direction/sample group.
- [self] Empty/nonfinite probabilities are uncertainty-not-evaluable; invalid
  finite bounds are a distinct failure.
- [tool] Runtime `b46e263` owns publication validation. A repeated launcher
  validation risks placeholder-policy mismatch and is unnecessary.
- [user] Commit/push preregistration, execute once detached, then commit/push
  unchanged terminal results; no patch or rerun is permitted.
