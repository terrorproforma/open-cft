# L0 surrogate v2 development log

## 2026-09-02 — preregistration

- Added immutable protocol/config, input-only partition identities, tests, and
  an empty result placeholder under the isolated v2 paths.
- Predeclared three group-split replicates, disjoint per-stratum calibration
  and assessment sets, fixed 96-row campaigns, split-conformal intervals, and
  per-output/per-scope gates.
- Final assessment labels and results are absent. Execution validation and
  outcomes will be recorded only after the preregistration commit is pushed.
