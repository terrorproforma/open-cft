# CFT topology characterization development log

## 2026-09-02 preregistration phase

- Bound the study to accepted coupling-v3 commit
  `f80a360fd740a30017cdac1874cedbfa2806874a` and unchanged accepted geometry,
  L1a fields, magnetics, and specifications.
- Declared 56 non-optimized Cartesian family cases spanning stage counts 2–8,
  4.5/6.0 mm pitch, 6.5/9.0 mm channel radius, and both first polarities.
- Added primary 48x144, refined 72x216, and 1.25x enlarged 60x180 independent
  maps with strict accepted artifacts and field gates.
- Added complete root audit records: physical coordinates, raw detector method,
  one mesh-scaled cluster, symmetry-aware finite-difference Jacobians,
  topological index/class, two-sided constant-psi connectivity, nearest stage,
  hardware/channel zone, eligibility, and exclusion reason.
- Added global 2D assignment with unmatched alternatives before any stability
  count, plus separate raw, clustered, eligible cusp, eligible cell,
  correspondence, and shift outputs.
- Added canonical semantic JSON and normalized-text identities, Git-blob
  dependency closure, exclusive execution lock, absolute solver-scale residual
  replay, separate field-equality replay, transition representatives, and
  pre/post-run lifecycle validation.
- Added manufactured X/O Jacobian-index, double-well separatrix, clustering,
  global assignment, geometry/domain, provenance, dependency, publication
  boundary, and lifecycle tests before preregistration.
