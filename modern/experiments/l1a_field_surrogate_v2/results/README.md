# Immutable v2 result destination

Before execution this directory contains only this notice. The one-run lock,
pre-solve provenance closure, numerical/model/calibration/assessment records,
representative predictions, and terminal success or failure bundle are written
once after the preregistration commit is pushed.
