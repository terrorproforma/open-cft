"""Cusp topology search v3.1: the corrected re-preregistration of v3 (same literature definition).

v3 (preregistration 69159934, result 8cbcdbe6) ended ``assessment_rejection`` because its
held-out reference dropped sealed v1 axis clusters whose centroid is not exactly on the axis
(``../cusp_topology_search_v3/POSTHOC_AUDIT.md``). v3.1 changes only that extraction; the
definition, tolerances, design sets, gates and field pipeline are unchanged.

Classification: ``SCREENING_L1A_FIELD_SEPARATRIX_CUSP_TOPOLOGY`` for the L1a design sets and
``P2_QUALIFIED_FIELD_SEPARATRIX_CUSP_TOPOLOGY`` for the single P2 divergent-exit row.
Nothing here is a plasma, mirror-probability or performance claim.
"""
