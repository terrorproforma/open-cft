# Cusp topology search v3.1 - learning scratchpad

- [self] Carried from v3: extract a held-out reference by the SOURCE's own semantics (member
  detection method), never by a float identity on a derived centroid.
- [self] A shakedown must include the reference's edge case (multi-member clusters); v3.1 adds
  `topology-s05-p0-r0-neg` for exactly that reason.
- [self] A corrected re-preregistration is a new experiment directory with the prior bundle
  disclosed and never cited; the prior results stay immutable.
