# Immutable v3 result destination

Only this notice exists before the detached one-run execution. Runtime
provenance, staged checkpoints, freezes, terminal artifacts, and sidecars are
created after the experiment branch preregistration is pushed. Working
checkpoints are removed before terminal validation and are never committed.
