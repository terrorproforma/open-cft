# Devlog

## 2026-09-02 — Infrastructure-corrected v3 preregistration

- Added a frozen 24-case v3 family disjoint from development and every
  v1-accessed ID/coordinate; disclosed the v1 first case as no longer held out.
- Bound coupling commit `f10d8213117fbafd8c2b69bdc103b6ef7b5d6d8c`,
  schema 4.1, wall-cusp/cell/path/orbit/uncertainty policies, field gates,
  all-case primary-map GPU replay, topology diagnostics, failure taxonomy,
  semantic identities, Git-blob closure, and one-shot lifecycle.
- Added one canonical normalize/hash/persist path with object-path errors,
  deterministic aware-UTC normalization, explicit tagging, exact raw-stream
  metadata, and strict unsupported-type rejection.
- Preflighted 44 public coupling v4/v3 dataclasses, production records, 21
  actual control writer cases, five injected failures, and the actual GPU
  runtime payload before held-out access.
- Replaced the polyline proxy with an exact-map-bound guiding-centre verifier
  that records B, `mu`, energy, pitch, mirror and nested-timestep evolution;
  polyline defect remains separately named.
- Added phase-atomic dependency/map/prerecord/replay/diagnostic checkpoints,
  access logging, full lock finalization, and strict typed failure validation.
- Validation run: 64 manufactured v3/coupling-v4 tests passed; compileall
  passed. No held-out map was accessed.
- Run status: pending the preregistration commit and clean detached execution.

