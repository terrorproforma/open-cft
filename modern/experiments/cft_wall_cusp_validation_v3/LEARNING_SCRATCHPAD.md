# Learning scratchpad

## File policy

- `COMMITTED`: this experiment-local scratchpad is part of the preregistration.

## Preflight guardrails

- [user] Preserve the accepted coupling/package/spec/FYP/prior-experiment trees.
- [user] Keep the 56-case characterization explicitly development-only.
- [user] Run exactly once from a clean detached commit; do not patch or rerun.
- [user] Make promotion a numerical/source-consistency claim, never hardware truth.
- [self] Bind every case, family, map artifact, evidence fingerprint, path,
  orbit implementation, and validation artifact before opaque projection.
- [self] Treat X/O/null/island results as diagnostics separate from wall-cusp
  membership.
- [tool] PowerShell on this host does not accept `&&`; use separate statements.
- [self] Never treat polyline-length convergence as magnetic-moment
  conservation; bind a path to its exact map and evaluate B, energy, pitch,
  mirror state, and `mu` directly.
- [self] Persist accepted map bytes and prerecord summaries before replay or
  topology serialization so later exceptions retain authoritative phase data.
- [self] Canonically serialize dataclasses recursively; boundary-null results
  are typed `BoundaryNullDiagnostic` values, not iterable coordinate pairs.
- [self] A lock payload must be converted to final canonical bytes before
  opening its exclusive path; acquired and finalized locks are distinct files.
- [self] Raw stdout/stderr remain exact bytes and are bound by a canonical
  metadata envelope rather than passed through the JSON serializer.
- [self] Reject unsupported runtime objects with full object paths; never
  silently stringify mapping keys, paths, device objects, or numeric wrappers.

## Session entry — 2026-09-02

- Task: preregister corrected coupling-v4 schema-4.1 held-out validation v3.
- Working pattern: isolate every new artifact under this experiment directory
  and stage only that path.
- Risk checkpoint: no field case may be probed before the preregistration
  commit; only manufactured tests are allowed before the one-shot run.
- Preflight finding: strict stage-spacing arithmetic rejected pitch 7.3 mm at
  first-centre fraction 0.83; before any held-out access it was replaced with
  the independently chosen representable 7.5 mm/fraction 0.875 geometry.
- Provenance finding: unrelated `material_fields` specs changed after the
  accepted coupling commit. They remain untouched and inventoried at v2 HEAD;
  coupling/field/geometry/magnetics code and relevant specs still match the
  exact accepted commit.
- V2 failure incorporated: direct `json.dumps` on a lock containing an aware
  UTC datetime created a zero-byte lock. V3 has one strict canonical writer,
  pre-create canonicalization, separate lock finalization, and injected
  acquisition/write/truncation/finalization failure checks.
- V3 preflight covers 44 public v4/v3 dataclass types, 21 production control
  write cases, five failure injections, an actual RTX 5090 runtime payload,
  and zero held-out accesses.
- V3 geometry preflight rejected the proposed 5.8 mm pitch under accepted
  strict spacing arithmetic; it was replaced before preregistration or field
  access by the new representable 5.5 mm pitch.

