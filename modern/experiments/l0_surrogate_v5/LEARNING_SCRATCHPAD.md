# L0 surrogate v5 learning scratchpad

- A blind successor needs genuinely new continuous inputs, not merely new
  group assignments on the inherited 8,192-row design.
- Interval-family selection and final conformal quantile estimation are
  separate statistical roles; each receives independent groups.
- The detached worktree, dependency tree and retained atomic lock make the
  actual one-shot execution mechanically distinguishable from development.
