# L0 surrogate v9 learning scratchpad

## Before execution

- V8's heuristic `sqrt(1+double_share)` charge proxy is not the L0 momentum
  mixture. The accepted identity is `1 + (sqrt(2)-1)*double_share`.
- Ionized utilization and axial divergence multiply both thrust and Isp; mass
  flow multiplies thrust only.
- Exact leading residual/ratio GPs retain a learned correction while exposing
  the documented L0 equations directly.
- The configured scratchpad/devlog loop skills are unavailable in this agent's
  skill catalog, so their persistent phase records are maintained directly.
