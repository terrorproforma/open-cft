# L0 surrogate v9

V9 is a same-domain prospective validation informed by V8's valid failed
development result. It corrects V8's heuristic physics features with the exact
accepted L0 momentum equations and explicit electrical power boundaries.

Raw, exact-leading residual, and exact-leading ratio ARD Matérn GPs are compared
at 64/96/128/160/224 rows using method labels only. The smallest passing model
freezes before final group calibration; calibration freezes before exactly-once
assessment. V7's exact group-count conformal ranks and V8's lower-only row
coverage plus engineering width gates are unchanged.
