# Visualization learning scratchpad

## 2026-09-02

- The four failure counts are overlapping taxonomy, not a partition of 128.
- All representative files must be validated even though only the two compatible
  candidates are embedded for detailed comparison.
- Tiny nominal loss-cone probabilities are misleading without their much larger
  uncertainty-supported upper bounds; both must remain visible together.
- The selected plasma attempts may expose residual, conservation, convergence,
  and rank evidence, but state vectors, powers, and screening performance do not
  belong in this visualization.
- Jacobian rank 22 is displayed as non-identifiable and screening-only, even for
  converged and feasible residual outcomes.

## 2026-09-02 corrected v1 semantics

- The semantic correction preserves numerical values and selection identities
  while superseding the prior publication labels.
- `residual_root_found`, `conservation_diagnostics`, `declared_gates`,
  `protocol_status`, `identifiability`, and `semantic_correction` are the
  authoritative dashboard vocabulary.
- The corrected summary is six residual roots, zero identifiable states, and
  zero performance publications.
- Coupling v2's same-z mirror proxy is deprecated; its mirror and probability
  values remain audit-visible but cannot support physical mirror claims.
- Replacing focused table rows during filtering requires stable case IDs and
  explicit focus restoration with scrolling suppressed.
