# Immutable result destination

This directory is empty before the one authorized execution except for this
notice. The execution lock, frozen selections, model artifacts, single-use
assessment, numerical records, manifest, sidecars, and viewer predictions are
written once after the pushed preregistration commit.
