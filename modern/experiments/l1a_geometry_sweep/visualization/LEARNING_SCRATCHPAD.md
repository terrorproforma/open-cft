# Visualization learning scratchpad

Policy: `COMMITTED` with this experiment-owned visualization artifact.

## Retained guardrails

- Never weaken or reinterpret the experiment's field-only claim boundary.
- Validate file sidecars, manifest bindings, and canonical payload hashes before
  deriving display data.
- Pin the reviewed manifest and dataset file/payload SHA-256 identities.
- Recompute case identity bindings and the exact constrained nondominated front.
- Keep runtime diagnostics, timestamps, machine paths, and secrets out of
  deterministic HTML.
- Draw sweep cases on canvas; do not create one DOM node per point.

## 2026-09-02 — standalone sweep dashboard

- [user] Ownership was restricted to new files under the visualization and its
  dedicated test directory; experiment sources/results and shared workstreams
  remained read-only.
- [tool] Direct `file://` browser navigation was prohibited, and the browser
  harness did not retain a newly created tab for localhost navigation.
- [self] Browser behavior is therefore documented as an unexecuted manual plan,
  not reported as verified.
- [tool] `--import-mode=importlib` preserved compatible test collection; 235
  experiment, geometry, fields, optimization, and visualization tests passed.
- [self] Keep the embedded payload compact: all 96 case records include inputs,
  QoIs, identities, residual/gates and available parity, while only the five
  representative downsampled maps/profiles/geometries are embedded.
